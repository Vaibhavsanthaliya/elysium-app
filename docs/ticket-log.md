# Elysium — Full EA Ticket Log

Full implementation notes for every completed ticket. Summary table lives in `CLAUDE.md` section 8.

---

| Ticket | Description |
|---|---|
| EA-1 | Product and technical blueprint. Analysis only — no code changes. Confirmed evolutionary refactor direction. |
| EA-2 | Identity pass. App renamed to Elysium in manifest, title, auth screen, footer, and service worker cache name. |
| EA-3 | Elysium design tokens added to `styles.css` (both light and dark) without any visual redesign. |
| EA-4 | Restrained copy pass. "Routine" language lightly shifted to "ritual" across visible UI text and notifications. |
| EA-5 | Temple tab added as read-only home summary. Made default active tab. Renders ring, status, cycle name, streak, and quick-action rows. |
| EA-6 | Chronicle tab added between Cycle and Progress. One note per day stored in `chronicle.notes` via existing state/save/sync flow. |
| EA-8 | Care module language introduced. Four copy-only changes: Today tab hero label, Temple "Care cycle" quick action, Settings "Care ritual" section heading, and morning push notification title. |
| EA-9 | Renamed project identity across GitHub and Vercel to elysium-app; local origin remote updated and external tooling verified. |
| EA-10 | Temple visual polish. Six `#pane-temple`-scoped CSS rules: larger hero card (padding, radius, shadow, gap), 100px ring, `--divine-accent` ring stroke, `--bronze` eyebrow label and chevrons, `--oracle-text` quick-action names. No HTML or JS changes. |
| EA-11 | Chronicle visual polish. CSS-only, Chronicle-scoped: upgraded card (16px radius, `--ritual-surface`, `--shadow-md`, separator border), focus-within ring via `--divine-accent`, textarea breathing room (padding, line-height, `--oracle-text`), placeholder via `--muted-text`, status margin, full-width `--divine-accent` save button. No HTML or JS changes. |
| EA-13 | Obsidian Temple foundation tokens. CSS-only, additive-only: 21 new custom properties added to `:root` and dark-mode block — obsidian depth layers, alabaster surface, bronze-soft fill, ink text material, warm separator, temple gradient, bronze glow, card shadow, radius scale (sm–2xl), spacing scale (xs–xl). No existing variables changed, no selectors changed, no HTML or JS changes. |
| EA-14 | Stacked Medallion layout for Temple tab. CSS-only, appended `#pane-temple`-scoped EA-14 block: hero card converted to column/centered layout, `--alabaster` surface in light mode / `--temple-gradient` in dark, `--shadow-card` + `--shadow-bronze`, `--separator-warm` card border and ring track, 120px ring, `--bronze` ring stroke, centered hero text, quick-action card radius/shadow/separator upgraded. No HTML or JS changes. |
| EA-15 | Bottom tab bar active state bronze. CSS-only, one-property change: `.tab.active` color swapped from `var(--tint)` (iOS system blue) to `var(--bronze)` (warm metallic). No layout, icon, animation, HTML, or JS changes. |
| EA-16 | Today/Care Obsidian Temple polish. CSS-only, `#pane-today`-scoped block appended: hero card `--alabaster` surface (light mode) + `--separator-warm` border + `--shadow-card`; dark-mode hero card override restores `--bg-elevated`; ring track `--separator-warm`; hero label `--bronze`; task list cards `--radius-lg` + `--shadow-card`. No HTML or JS changes. |
| EA-17 | Chronicle Obsidian Temple polish. CSS-only, `#pane-chronicle`-scoped block appended: chronicle card `--alabaster` surface (light mode) + `--separator-warm` border + `--shadow-card`; dark-mode card override restores `--bg-elevated`; focus ring changed to `--bronze`; save button `--radius-lg`. No HTML or JS changes. |
| EA-18 | Progress / Stars Obsidian Temple polish. CSS-only, `#pane-progress`-scoped block appended: stat cards and calendar card `--alabaster` (light) + `--separator-warm` border + `--shadow-card`; dark-mode overrides restore `--bg-elevated`; today calendar ring `--bronze` (logged-today white ring preserved); milestone list `--radius-lg` + `--shadow-card` + `--separator-warm` rows; active milestone dot `--bronze` + `--bronze-soft` glow; photo items `--radius-lg`. No HTML or JS changes. |
| EA-19 | Settings Obsidian Temple polish. CSS-only, `#pane-settings`-scoped block appended: setting and install cards `--alabaster` (light) + `--separator-warm` border + `--shadow-card` + `--radius-lg`; dark-mode override restores `--bg-elevated`; row separators `--separator-warm`; action rows `--oracle-text` (removes iOS blue); danger rows explicitly re-declared `--red`; chevrons `--bronze`; danger chevrons `--red`; Reset text button `--bronze`. No HTML or JS changes. |
| EA-20 | Global atmosphere pass. CSS-only. Token-level (light mode only): `--bg` `#f2f2f7` → `#ede8df` (warm stone canvas); `--separator` → bronze-warm `rgba(95,70,28,.13)`; `--bg-input` → warm `rgba(100,75,30,.10)`. Global override block appended: header + tab bar warm glass + `--separator-warm` borders (light mode); `.task-list` + `.cycle-card:not(.active)` + `.info-card` → `--alabaster` with dark-mode restore to `--bg-elevated`; `.btn-row` → `--oracle-text`; `.text-btn` + `.task-info-btn` → `--bronze`; `.field-input:focus` → bronze ring; `.btn-primary` → `--obsidian` (light mode); `.app` ambient warm radial glow (light mode). No dark-mode token changes. No HTML or JS changes. |
| EA-21 | Obsidian Temple typography identity pass. Added Google Fonts loading for DM Sans, Spectral, and JetBrains Mono in `index.html`; added `--font-sans`, `--font-display`, and `--font-mono` variables; applied DM Sans to base UI, JetBrains Mono to meta labels/version, Spectral italic bronze treatment to ring numbers/stat values, Spectral italic Chronicle textarea, and lighter 500-weight headings. Updated `CLAUDE.md` only for handoff. No app.js, service worker, state, storage, sync, schema, layout, or feature changes. |
| EA-22 | Temple home concept-card rebuild. Replaced Temple-only hero/quick-action markup with a featured Care ritual card, medallion/seal progress ring, ritual-card quick-action buttons, mono state labels, and restrained bronze hairlines/glow. Preserved all `renderTemple()` IDs and quick-action button IDs. Added only `#pane-temple` scoped CSS plus this handoff log. No app.js, state, storage, sync, schema, service worker, dependency, or other-tab changes. |
| EA-23 | Bronze state pass. CSS-only, single appended block: Today `#pane-today .ring-fg` stroke → `--bronze`; `.task-item.done .task-check` fill+border → `--bronze`; `.task-check svg` color → `var(--bg)` for mode-adaptive checkmark contrast; `.task-item.done .task-text` strikethrough → `rgba(201,165,107,0.3)`; `.cycle-card.active` border → `--bronze`, fill → `--bronze-soft`; `.cycle-active-badge` background → `--bronze`, label color → `var(--bg)`; `.cal-day.logged` background → `--bronze`, day numeral color → `var(--bg)` (EA-18 today-and-logged white inset ring preserved); `.streak-pill` background → `--bronze-soft`, color → `--bronze`; `.task-info-btn` color → `--bronze`. No HTML, no app.js, no service worker, no manifest, no global token changes, no EA-22 Temple block changes. |
| EA-24 | Dark mode token correction. CSS-only. Dark `@media :root` block: 18 token corrections — `--bg` → `#0E0E12` (obsidian floor, not pure black); `--bg-elevated`/`--bg-grouped` → `#1A1A21` (removes iOS cool cast); `--bg-input` → warm `rgba(236,230,217,0.10)`; `--label` → `#ECE6D9` (warm ink, not cold white); `--label-secondary`/`--label-tertiary` → warm `rgba(236,230,217,...)` basis; `--separator` → `rgba(236,230,217,0.08)` (7.5× reduction, warm hairline); `--bronze` → `#C9A56B` (champagne gold, not orange-amber); `--obsidian`/`--obsidian-bg` → `#0E0E12`; `--obsidian-elevated` → `#131318`; `--marble` → `#1A1A21`; `--bronze-soft` → champagne RGB basis; `--ink`/`--ink-secondary` → concept warm values; `--separator-warm` → `rgba(236,230,217,0.10)` (warm-white hairlines replace orange-tinted borders); `--shadow-bronze` → champagne basis with `-4px` spread. Header/tab bar dark glass updated from `rgba(0,0,0,...)` to `rgba(14,14,18,0.88)`. Dark ambient temple glow added to `.app`. No light mode changes, no HTML, no app.js, no service worker, no manifest. |
| EA-25 | Sign-in / sign-up Obsidian Temple redesign + Aperture logo. `index.html`: replaced inner markup of `#auth-screen` with new `.auth-shell` structure — inline Aperture SVG mark (circle + golden-ratio horizon hairline + bronze dot) inside a bronzed medallion, "OBSIDIAN TEMPLE" mono eyebrow, `Elys` DM Sans 500 + italic `ium` Spectral bronze wordmark, italic tagline, underline hairline fields (`.auth-field` / `.auth-field-input`), bronze pill CTA (`.auth-cta`, mono uppercase "Continue" / "Begin" / "Send reset link" / "Go to sign in" / "Update password"), quiet mono links, mono reassurance footer. All five `auth-view` sub-views preserved; every existing element ID preserved verbatim. `styles.css`: appended single EA-25 block — full auth shell styling. Old `.auth-card`/`.auth-glyph`/`.auth-title`/`.auth-switch`/`.btn-link`/`.field`/`.field-input` rules retained (inert) for easy rollback. No app.js, service worker, manifest, favicon, app-icon, Supabase, storage, IndexedDB, skincare task, cycle, or milestone changes. |
| EA-26 | Mobile polish — boot shell, scroll reset, auth keyboard fix, scrollbar hiding, tab bar stabilisation, sign-out tab reset. `index.html`: added `#boot-shell` (Aperture mark + Elys*ium* wordmark, first child of body) as first-paint startup cover; updated `theme-color` metas to `#0E0E12`/`#ede8df`. `app.js`: added `hideBootShell()` (idempotent, null-guarded); called at all 6 exit paths of `applyAuthSession`; 4s safety-net setTimeout fallback; `window.scrollTo(0,0)` in `switchTab()`; `switchTab('temple')` in sign-out handler. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-27 | Modal Obsidian Temple alignment. CSS-only, single block appended, all selectors scoped to `.modal` context. Corrected: handle background, title weight/tracking, field-label mono bronze, field-input underline-only, field-input:focus bronze underline, select arrow bronze, btn-primary dark mode, btn-secondary rust, btn-text muted. No HTML, app.js, state, cycle, milestone, storage, sync, or schema changes. |
| EA-29A | JS modularization with browser-native ES modules. Monolithic `app.js` (~1842 lines) split into 18 ES modules + `main.js` entry point under `js/` tree. Key patterns: callback injection breaks circular deps; `initState()` wraps module-level init; `Object.assign(state, newState)` preserves live ESM binding. `index.html`: `<script src="app.js">` → `<script type="module" src="main.js">`. `sw.js`: CACHE_NAME `elysium-v1` → `elysium-v2`, all module paths added. `app.js` deleted. No CSS, state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, DEFAULT_DATA, or behavior changes. |
| EA-28 | Temple ritual-domain entry screen. `index.html`: replaced `#pane-temple` inner content with featured Care card, 6 domain cards (Chronicle active + 5 dormant), secondary mono nav row. `styles.css`: replaced EA-22 block with EA-28 scoped block. `app.js`: `renderTemple()` updated circumference, cycle blurbs, chronicle note state; added `#temple-goto-chronicle` event listener. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-29B | CSS modularization. Monolithic `styles.css` (2,796 lines) split into 11 feature-based files under `css/`. `index.html` loads all 11 in documented cascade order. `sw.js` ASSETS updated, CACHE_NAME bumped `elysium-v2` → `elysium-v3`. `styles.css` deleted. No visual changes, no new tokens, no JS logic changes. |
| EA-30 | Progress tab Stars/Constellation foundation. `index.html`: added "The *stars*" page heading, `.section-hd` pattern, `star-field-card` class, meta span IDs. `css/progress.css`: star dot states (logged=bronze, partial=ring, empty), constellation SVG line styles, milestone roman numeral. `js/render/progress.js`: added `ROMAN`, `isPartial`, `renderConstellationLines()` (proximity-threshold SVG), roman milestone indices, stage meta. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-34 | Stars/Progress deeper refinement. `js/render/progress.js`: constellation algorithm changed from chain to all-pairs nearest-neighbor; maxDist `cellUnit * 2.5` → `cellUnit * 1.9`. `css/progress.css`: milestone badges → transparent mono bronze; logged star glow strengthened; partial state closer to prototype; constellation line opacity `0.35 → 0.42`. No HTML, state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, or photo logic changed. |
| EA-33 | Today/Care task surface refinement. CSS-only, EA-33 block in `css/today.css`: task-list strips card wrapper; task-item separator-led rows; task-text 15px; task-check 20px; section-icon quiet dim glyph; section-title h2 Spectral italic 17px; cycle-tag/comfort-tag → transparent mono bronze. No HTML, JS, state, task IDs, cycle logic, milestone, storage, sync, or IndexedDB changes. |
| EA-32 | Chronicle prompt card and writing atmosphere. `index.html`: added `.chronicle-heading`, `.chronicle-prompt` dashed card, cleaned `.chronicle-card`. `css/chronicle.css`: heading, prompt, status, save button styles. `js/render/chronicle.js`: added `PROMPTS` array (7 questions), `renderChronicle()` updates question by day of week. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, DEFAULT_DATA, or save/sync logic changed. |
| EA-31 | Cycle tab Astrolabe foundation. `index.html`: replaced 3-card stack with `.astrolabe-wrap` (4 concentric rings + center disc + legend). `css/cycle.css`: astrolabe ring variants, center disc, node positioning/animation, legend. `js/render/cycle.js`: 3 `.al-node` buttons JS-positioned at -90°/30°/150° on 102px radius; click handler unchanged. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-35 | Ritual philosophy pass. Temple domain card subtitles → interaction-primitive verbs (`remember`, `witness`, `arrive`, `hold`, `notice`, `close`); `.temple-domain-archetype` mono archetype names added. Chronicle → Drift: rebuilt to reading-first with `#chronicle-drift` JS-rendered past entries above writing surface; `renderDrift()` with year-ago surfacing and HTML escaping. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-37 | Global header restraint pass. `index.html`: demoted header to compact daily-status rail; removed visible `Today` h1 and flame icon; preserved `#date-label`, `#streak-pill`, `#streak-num`. `css/layout.css`: reduced padding/glass, `.streak-pill` → quiet mono meta. `css/components.css`: removed unused `.greeting` selector. No JS, state, storage, sync, Supabase schema, IndexedDB, task IDs, cycle logic, milestone logic, service worker, manifest, or dependencies changed. |
| EA-39 | Progress Photos section heading alignment. `index.html`: replaced old `.section-header` + circular add affordance with `.section-hd` pattern; preserved `#add-photo-btn` and `#weekly-photo-input`. `css/progress.css`: `.photo-add-action` text-button styling. `js/services/photos.js`: copy-only empty-state update. No photo logic, IndexedDB, state, storage, sync, Supabase schema, progress/calendar/constellation logic, service worker, manifest, or dependencies changed. |
| EA-41 | Settings page heading alignment. `index.html`: added Settings page heading with mono eyebrow `ACCOUNT & RITUAL` and Spectral italic `Settings`. `css/settings.css`: heading styles matching Obsidian Temple pattern. No JS, state, settings behavior, auth behavior, storage, sync, Supabase schema, IndexedDB, service worker, manifest, or dependency changes. |
| EA-43 | Sleep bedtime domain, modal-first. Added `sleep: { entries: {} }` state with migration; `js/domains/sleep.js`; Sleep Temple domain card activated; `#sleep-modal` for bedtime entry + 7-night history; union merge on sign-in; scoped CSS; service worker `elysium-v4`. No new tab, no dependencies, no Supabase schema change, no IndexedDB change, no Care task IDs, no 3-day cycle change, no milestone threshold change. |
| EA-44 | Light Witness domain, modal-first. State `light: { entries: { "YYYY-MM-DD": { witnessedAt: "HH:MM" } } }`; `js/domains/light.js`; Temple Light card active with `#temple-light-state`; `#light-modal` with 7-day dot history and `I'm here` action; `mergedLightEntries` union merge in `js/sync.js`. No new tab, no dependencies, no Supabase schema change, no IndexedDB change, no Care task IDs, no 3-day cycle change, no milestone threshold change. |
| EA-46 | Today/Care emoji section icon removal. Removed emoji glyphs (`☀`, `☾`, `◐`) from section headings; removed unused `#pane-today .section-icon` override from `css/today.css`. All task/cycle/state behavior preserved. |
| EA-47 | Today/Care ritual detail transformation. `index.html` `#pane-today`: removed hero card; added `.care-heading`, `.care-meta-row` (72px mini ring + kv pairs), `.care-section-hd` pattern, `.care-cycle-strip`. `css/today.css`: EA-47 block. `js/render/common.js`: `renderTodayCycle()` updated — `care-tonight` + `care-cycle-strip` rebuild. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-48 | Sleep + Light modal ritual philosophy redesign. Sleep modal → two-state closure ritual (State A: writing surface with Hypnos eyebrow, italic heading, textarea, cue section; State B: goodnight screen with crescent glyph). Light modal → atmospheric sky gradient header, sun disc + time-cursor, elapsed-time figure, `I'm here` / `Witnessed` action. `js/domains/sleep.js`: `saveSleepClosure()`. `js/ui/modals.js`: rewrote `openSleepModal`/`saveSleepModal`. `main.js`: rewrote `renderLightModal()`. CSS in `css/modal.css` and `css/temple.css`. No state schema change. |
| EA-51 | Quieted the global shell by removing the header `#streak-pill` markup, its `css/layout.css` styles, the `main.js` click shortcut, `renderHeader()` streak-number write, and stale `#streak-num` updates from Today and past-day Progress paths. `getStreak()` and the Stars/Progress `#stat-streak` KPI remain unchanged. No state, storage, sync, Supabase schema, IndexedDB, Care task, cycle, milestone, service worker, manifest, or dependency changes. |
| EA-52 | Stars reframe. `index.html`: removed the four Progress KPI stat cards (`Streak`, `Logged`, `This week`, `21-day goal`), changed the eyebrow to `STARS`, and renamed the milestone section to `Ritual stage`. `js/render/progress.js`: removed stat DOM writes and rendered one quiet stage line from the existing milestone threshold result (`Stage I–IV of IV — foundation/acne control/marks and texture/maintenance`). `css/progress.css`: removed KPI/stat and milestone badge-row styles, added scoped quiet stage-line typography. `renderConstellationLines()`, weekly photo behavior, past-day modal behavior, loggedDays/checks, state/storage/sync, cycle logic, Care tasks, and milestone thresholds unchanged. Ticket creation skipped for later batch Jira creation. |
| EA-53 | Care smart-feedback rewrite. `js/render/temple.js`: replaced chain-count, missed-day, and productivity feedback with cycle-turn copy (`Night I/II/III`, protocol in motion, kept turn + tomorrow's protocol), and made the Temple featured Care card lead with the current cycle label while keeping the percent arc. `js/render/common.js`: Today/Care cycle meta and night tag now render `Night I/II/III · protocol`. `js/render/today.js`: completion toast now uses the same kept-turn copy. `index.html`: initial Temple/Today fallback labels changed from generic completion wording to cycle-aware text. No state keys, task IDs, task text, task completion logic, cycle values, milestone thresholds, storage, sync, Supabase schema, IndexedDB, service worker, manifest, dependencies, or unrelated screens changed. Ticket creation skipped for later batch Jira creation. |
| EA-54 | Sleep closure enforcement. `js/domains/sleep.js`: added `isClosedForToday()` and `reopenSleepClosure()` using the existing `sleep.entries[YYYY-MM-DD]` entry as the only closed-day source of truth. `js/state.js`: migration now preserves optional Sleep `note`. `js/ui/tabs.js`: tab switching is guarded while closed and redirects to Sleep State-B. `js/ui/modals.js`: State-B is non-dismissable while closed and reopen deletes today’s Sleep entry, saves locally, refreshes Temple, and hides the modal. `main.js`: startup/post-sync closed checks, closed-day handler registration, `Reopen the day` wiring, and immediate Supabase flush after reopen. `index.html`/`css/modal.css`: replaced State-B `Close` with restrained `Reopen the day`. No new closure key, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change. Ticket creation skipped for later batch Jira creation. |
| EA-55 | Light Witness honesty pass. `index.html`: removed the fake duration figure, duration label, and placeholder-only sun footer from `#light-modal`; eyebrow simplified to `THE FIRST HOUR`; witness button fallback copy simplified to `I'm here`. `main.js`: removed the elapsed-duration helper and all elapsed-minute writes; witnessed button now displays `Witnessed · HH:MM` using existing `witnessedAt`, while current-time cursor positioning remains. `css/temple.css`: removed unused Light metric/footer selectors and tightened eyebrow spacing. `witnessLight()`, `light.entries[date].witnessedAt`, Temple Light card rendering, storage/sync behavior, geolocation/weather/API absence, Care tasks, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, manifest, and dependencies unchanged. Ticket creation skipped for later batch Jira creation. |
| EA-56 | Chronicle subtractive pass. `index.html`: removed the visible Chronicle save button, fixed the prompt to `What asked something of you today?`, softened the textarea placeholder, and made the status line initially hidden. `js/render/chronicle.js`: replaced rotating prompts with one fixed prompt constant, added dirty-guarded autosave on 800ms input idle and blur, removed Chronicle toast feedback, kept empty textarea clearing through existing `upsertChronicleNote(todayStr, body)` delete semantics, hid empty Drift copy, expanded Drift from 3 to 12 rendered entries, and added progressive quieting classes. `main.js`: wired textarea input/blur listeners and flushes pending Chronicle edits before page-hide Supabase flush only when the dirty flag is set. `css/chronicle.css`: removed obsolete Chronicle save-button styles and added quieting rules for older Drift entries. No `chronicle.notes` shape change, no multiple notes per date, no Supabase schema/sync-path change, no IndexedDB change, no Care task/cycle/milestone changes, no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-57 | Care De-Checklist. `index.html` `#pane-today`: moved the three-night cycle strip above task groups, placed Night before Morning, changed Night to a protocol-labeled primary section, demoted Morning, and folded existing Habit tasks into a `Supporting rituals` details surface while preserving `#list-habit` and `data-section="habit"`. `css/today.css`: added an EA-57 scoped block for the primary Night surface, quieter Morning rows, and folded support styling. `js/render/common.js`: `renderTodayCycle()` now updates `#night-protocol-label` (`Tonight · Niacinamide`, `Tonight · Salicylic`, `Rest night`) and keeps `#cycle-tag` to `Night I/II/III`. No state keys, task IDs/text, task completion logic, edit/info behavior, comfort mode behavior, cycle logic, milestone thresholds, storage/sync, Supabase schema, IndexedDB, dependencies, or unrelated screens changed. Ticket creation skipped for later batch Jira creation. |
| EA-59 | Tab bar consolidation — Temple as home. `index.html`: removed Cycle, Chronicle, and Progress `<button class="tab">` elements from the bottom nav; 3 remaining buttons (Temple, Today, Settings) space evenly with existing `flex: 1 / space-around`. `js/ui/tabs.js`: made tab-button lookup null-safe — when `switchTab(name)` is called for a pane with no tab button, Temple's button stays active as the parent surface rather than throwing. All panes (`#pane-cycle`, `#pane-chronicle`, `#pane-progress`) remain in DOM; all render functions, Temple domain cards, and secondary nav links unchanged. No state, storage, sync, Supabase schema, IndexedDB, Care tasks, cycle logic, milestone thresholds, service worker, manifest, or dependency changes. Ticket creation skipped for later batch Jira creation. |
| EA-58 | Reminders into domains — naming and framing pass. `index.html`: Settings section heading `Reminders` → `Practice cues`; row labels `Morning reminder` → `Morning cue`, `Night reminder` → `Night cue`, `Evening check-in` → `Evening cue`; hint paragraph `Reminders fire while…` → `Practice cues fire while…`; Sleep modal TONIGHT row `Bed` → `Night cue` (existing `nightTime` value already displayed there). `js/services/notifications.js`: toasts `Reminders on/off` → `Practice cues on/off`; `fireNotification` confirmation title/body updated; scheduled evening cue title `Daily check-in` → `Evening cue`. No `state.reminders` shape change, no notification scheduling logic change, no permission logic change, no CSS change, no modal behavior change, no Supabase schema change, no IndexedDB change, no Care task/cycle/milestone changes. Ticket creation skipped for later batch Jira creation. |
| EA-60 | Mind domain — Session primitive. `js/constants.js`: added `mind: { sessions: [] }` to `DEFAULT_DATA`. `js/state.js`: added `mind.sessions` migration guard — validates `id` (string), `date` (isYmd), `durationMinutes` (positive number), `completed` (boolean); clamps `reflection` to 500 chars. `js/domains/mind.js`: new module — `getMostRecentSession()`, `getTodaySession()`, `beginMindSession(durationMinutes)`, `endMindSession(sessionId, reflection)`. `js/render/temple.js`: imported `getMostRecentSession`; added Mind card state write to `renderTemple()` — shows `Held · Xm` after any completed session, `—` otherwise. `js/sync.js`: union merge for `mind.sessions` by `id` (local wins on conflict). `index.html`: Mind card `disabled` removed, `id="temple-goto-mind"` added, `id="temple-mind-state"` added; `#mind-modal` added with three states: State A (duration selector 25/40/90, "Begin" primary CTA), State B (quiet "Hold." Spectral italic, duration mono label, `.mind-end-btn` quiet text action — backdrop locked during hold), State C (optional reflection textarea, "Complete" CTA). `css/modal.css`: EA-60 block appended — eyebrow, title, sub, duration row/buttons/unit, State B centered holding layout, `.mind-end-btn` quiet mono style, State C reflection textarea matching Sleep note pattern. `main.js`: imported `beginMindSession`/`endMindSession`; wired `openMindModal`/`closeMindModal` (backdrop-locked during `_mindHolding`), duration button selection, Begin/End/Complete flow with `saveState()` + `renderTemple()` on complete. `sw.js`: `js/domains/mind.js` added to ASSETS; CACHE_NAME bumped `elysium-v4` → `elysium-v5`. No new tab, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change. Ticket creation skipped for later batch Jira creation. |
| EA-62 | Doc sync + dead cleanup. `AGENTS.md`: replaced 249-line stale monolith-era snapshot (referencing deleted `app.js`/`styles.css`, pre-EA-29A constraints, incomplete ticket log) with a 5-line pointer to `CLAUDE.md` as the authoritative handoff document. `js/domains/care.js`: removed `getStreak()` and `getMissedDays()` — confirmed zero imports anywhere in the module tree or `main.js`. `js/state.js`: in `migrateState`, added a second `.filter()` pass on `mind.sessions` that drops any session where `completed === false` and `sess.date` is not today (calculated inline via `ymd(new Date())` since module-level `todayStr` is not yet set at migration time); completed sessions and today's in-progress session are always preserved; session shape unchanged. `CLAUDE.md` + `docs/ticket-log.md`: EA-62 entries added. No UI changes, no state shape changes, no Supabase schema changes, no IndexedDB changes, no task/cycle/milestone changes, no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-63 | Decommission Care Percentage — Cycle-State Care. `index.html`: replaced the Today numeric label/suffix and Temple mini-arc markup with three-night cycle-state indicators. `css/today.css` and `css/temple.css`: added scoped mark/state styling for untouched, in motion, kept, and resting states. `js/render/temple.js`: replaced numeric Care card arc logic with cycle-turn state derivation from existing night task checks; Temple now renders `Night I/II/III · protocol` plus protocol state copy. `js/render/common.js`: Today cycle strip and compact indicator now reflect current turn state without numbers. `js/render/today.js`: task toggles still use existing morning/night completion and `loggedDays` rules, then refresh Today and Temple Care state. `CLAUDE.md` + `docs/ticket-log.md`: EA-63 entries added. No state shape changes, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no task IDs/text change, no 3-day cycle change, no milestone threshold change, no dependencies, and no Sleep/Light/Mind behavior change. Ticket creation skipped for later batch Jira creation. |
| EA-64 | Past-Day Modal Kept-Turn Rewrite. `js/render/progress.js`: removed the Stars modal checkbox/task rendering path and historical toggle handler; selected non-future dates now render a read-only `Day record` with `MONDAY · 18 MAY` style date meta, cycle context (`Night I/II · protocol` or `Rest night`), and status derived only from existing `loggedDays`/`checks` (`kept`, `partial`, `not kept`, `quiet` for today with no checks). Chronicle notes for the date surface as a small italic excerpt when present. `css/modal.css`: replaced past-day task-list section styling with scoped record/status/excerpt styling using JetBrains Mono, Spectral italic, warm separators, and restrained bronze. `index.html`: updated the modal title fallback to `Day record`. `CLAUDE.md` + `docs/ticket-log.md`: EA-64 entries added. No state shape changes, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no task IDs/text change, no 3-day cycle change, no loggedDays/checks persistence rule change, no Today/Care task completion change, and no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-65 | Mind Hold-the-Time. `js/domains/mind.js`: added timestamp-derived held-duration helpers, most-recent-reflection lookup, and preservation of an existing `endedAt` when saving reflection after the hold ends. `js/ui/modals.js`/`main.js`: moved Mind modal wiring into the modal module; State B now starts a quiet elapsed timer, updates a bronze hairline progress variable, disables `End session` with `Hold a little longer` for the first 60 seconds, resumes today's incomplete hold if present, and stamps `endedAt` when the user manually ends the hold rather than after writing the reflection. `index.html`/`css/modal.css`: added a restrained recent-reflection line in State A, a held elapsed line + bottom hairline in State B, and softened State C copy to `Held.` / `Save line`. `js/render/temple.js`: Temple Mind card now renders actual held duration from `startedAt`/`endedAt` instead of selected `durationMinutes`. `CLAUDE.md` + `docs/ticket-log.md`: EA-65 entries added. No state shape expansion, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change, no analytics/totals/streaks/charts, and no dependencies. Ticket creation skipped for later batch Jira creation. |
