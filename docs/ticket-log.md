# Elysium — Full EA Ticket Log

Full implementation notes for every completed ticket. `CLAUDE.md` section 8 shows a summary of recent tickets only (EA-95+); the complete history is here.

---

| Ticket | Description |
|---|---|
| EA-202 | Light as Apollo: Morning Threshold Alignment. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/README.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, and the available Pantheon direction file at `elysium-app-handoff/elysium-app/project/Elysium - Pantheon Direction.html` (the requested root-level `Elysium - Pantheon Direction.html` was not present). Source files inspected: `index.html`, `js/domains/light.js`, `js/render/temple.js`, `js/ui/modals.js`, `css/temple.css`, `css/modal.css`, `sw.js`, plus `js/ui/flow.js`, `js/constants.js`, `js/render/common.js`, `js/render/today-plan.js`, `js/state.js`, and `js/sync.js` to verify current Light behavior and storage boundaries. **Motivation:** Light should read as Apollo through clarity, first light, attention, and the first visible shape of the day, without growing into a planner, weather surface, history surface, or analytics surface. **`index.html`:** Morning Flow Light step eyebrow changed `MORNING FLOW` -> `APOLLO`; Light step title changed `Light` -> `Let the day come into view.`; daylight/orientation prompt changed `Will the day meet your skin?` -> `What needs light at the threshold?`; primary action changed `Entered` -> `Let it be seen`. All Light-related IDs and `data-daylight` values were preserved. **`js/constants.js`:** `LIGHT_OPENING_INVITATIONS` rewritten around clarity/first-light threshold language; `DAYLIGHT_CONFIRMATIONS` softened away from generic exposure status; `APOLLO_PROTECTION_LINES` kept as quiet Care/SPF consequences while using first-light/protection language. **`js/ui/flow.js`:** already-entered copy changed to `First light has already been named.`; no-selection fallback confirmation changed to `What needed light has been seen.`; selection, save, skip, auto-advance, and flow-session guard behavior unchanged. **`js/render/temple.js`:** Light witness daily line changed to `First light has entered the room.`; Day Thread Light trace changed to `First light entered.` **`CLAUDE.md`:** added the EA-202 recent-ticket summary. **`sw.js`:** `CACHE_NAME` bumped `elysium-v107` -> `elysium-v108`. **What was not changed:** no new state keys, no `DEFAULT_DATA` changes, no `migrateState` changes, no `STORAGE_KEY` change, no Supabase schema or sync path changes, no IndexedDB, no new Light tabs/cards/history/calendar/search, no AI suggestions, no mood tracking, no scores/counts/streaks/trends, no new analytics, no Care/Today/Chronicle/Sleep/Mind/Body/Water behavior changes, and no Light behavior changes beyond visible copy. Patron Test: Utility remains the existing morning witness/daylight threshold that can still affect Care's quiet protection line; Form is now Apollo-shaped through first light, clarity, threshold, and seeing rather than decorative god-labeling. |
| EA-200 | Temple as Olympus: Pantheon Surface Calibration. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/README.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, and the available Pantheon direction file at `elysium-app-handoff/elysium-app/project/Elysium - Pantheon Direction.html` (the requested root-level `Elysium - Pantheon Direction.html` was not present). Source files inspected: `index.html`, `js/render/temple.js`, `js/render/today-plan.js`, `js/constants.js`, `css/temple.css`, `sw.js`, plus `main.js`/`js/ui/tabs.js` to confirm Today remains the default landing pane. **Motivation:** Today/Kairos is the practical front door; Temple should read as the quiet Olympus/Pantheon room rather than a second dashboard. **`index.html`:** Temple Care hero eyebrow changed `CARE · HYGIEIA` -> `CARE · ASCLEPIUS`; domain section label fallback changed `The other paths` -> `OLYMPUS`; Chronicle subtitle changed `write what remains` -> `keep what remains`; Sleep subtitle changed `park what remains` -> `let the day rest`; Body subtitle changed `jaw · shoulders · breath` -> `return the weight`. **`js/render/temple.js`:** period section headings now render `OLYMPUS`; fallback daily lines and earned daily lines were softened to room/patron language; Day Thread and evening closing strings were adjusted away from `still open`, `still waits`, `has not closed`, and dashboard-style status phrasing; Temple-only card state copy now uses `Nearby`/`Resting`, `Named`, and `Weight` where appropriate. Domain ordering logic, period buckets, warmth/presence algorithms, trace behavior, Day Thread rendering, and closing-section visibility were preserved. **`sw.js`:** `CACHE_NAME` bumped `elysium-v106` -> `elysium-v107`. **What was not changed:** no new state keys, no `DEFAULT_DATA`, no `migrateState`, no `STORAGE_KEY`, no Supabase schema or sync path, no IndexedDB, no Today Plan behavior, no Today/Kairos copy, no Care/Chronicle/Sleep/Mind/Body/Water behavior, no tab order, no new sections, no counters, no recaps, no summaries, no new atmospheric algorithms, no new ritual arrays, and no new cards. |
| EA-197 | Sleep as Hypnos. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/README.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, and the available Pantheon direction file at `elysium-app-handoff/elysium-app/project/Elysium - Pantheon Direction.html` (the requested root-level `Elysium - Pantheon Direction.html` was not present). Source files inspected: `index.html`, `css/modal.css`, `js/ui/modals.js`, `js/domains/sleep.js`, `sw.js`. **Motivation:** Sleep already owns closure, parking note, unfinished-intention carry/pass, and the morning handoff; this ticket tightens the copy so Hypnos reads as permission to stop carrying the day rather than a sleep tracker, bedtime coach, or productivity shutdown system. **Patron Test:** Utility Gate passes because the surface remains useful without the word Hypnos: write an optional parking note, decide what waits, close the day, and reopen if needed. Form Gate passes because the revised language uses quiet, release, and rest (`go quiet`, `does not need your hands`, `may rest`) rather than decorative mythology or optimization language. **`index.html`:** Sleep modal eyebrow changed `HYPNOS / CLOSURE` -> `HYPNOS`; Sleep modal title changed `Let the day <em>end</em>` -> `Let the day <em>go quiet</em>`; Sleep modal subline changed `What remains can wait until morning.` -> `What remains does not need your hands tonight.`; Sleep modal handoff line changed `HYPNOS WILL HOLD WHAT WAITS` -> `WHAT CAN WAIT MAY REST`; Sleep modal save button changed `Let the day end` -> `Let it rest`; Night Flow's existing Sleep close CTA changed `Let the day end` -> `Let it rest` to keep the same closure action aligned without touching flow logic. Parking placeholder `What should not follow you into sleep.` and Today morning handoff copy `HYPNOS HELD` / `Let it rest` were retained. **`js/ui/modals.js`:** updated the `openSleepModal()` eyebrow reset string from `HYPNOS / CLOSURE` to `HYPNOS`, keeping reopen/open behavior unchanged while preventing stale copy after JS render. **`sw.js`:** `CACHE_NAME` bumped `elysium-v102` -> `elysium-v103`. **What was not changed:** no state keys, no `DEFAULT_DATA`, no `migrateState`, no `STORAGE_KEY`, no Supabase schema or sync path, no IndexedDB, no Sleep parking-note save behavior, no Sleep shutdown carry/pass behavior, no Today carryover behavior, no Sleep handoff/dismiss behavior, no Chronicle Well behavior, no CSS, no new UI section, no bedtime/wake-time/duration/score/streak/consistency language, and no AI suggestions. |
| EA-196 | Chronicle as Mnemosyne. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/README.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, and the available Pantheon direction file at `elysium-app-handoff/elysium-app/project/Elysium - Pantheon Direction.html` (the requested root-level `Elysium - Pantheon Direction.html` was not present). Source files inspected: `index.html`, `sw.js`. **Motivation:** Chronicle already owns writing, archive, plain substring search, and resurfacing; this ticket aligns the surface language with Mnemosyne as the patron of memory without changing any functionality. **Patron Test:** Gate 1 passes because the plain utility remains Chronicle: write, remember/read, search, and receive remembered fragments. Gate 2 passes because Mnemosyne now shapes the surface labels directly through memory verbs (`Remember`, `Remembered`, `Search what remains`) rather than decorative mythology or added explanation. **`index.html`:** changed Chronicle eyebrow `CHRONICLE` → `MNEMOSYNE`; changed `#chronicle-mode-read` text `Read` → `Remember`; changed `#chronicle-search` placeholder `Search the archive` → `Search what remains`; changed `.chronicle-resurfaced-kicker` text `Resurfaced` → `Remembered`. **`sw.js`:** `CACHE_NAME` bumped `elysium-v101` → `elysium-v102`. **What was not changed:** no JS behavior, no Chronicle search logic, no archive/read/write behavior, no resurfacing selection logic, no CSS, no HTML structure, no UI elements, no state shape, no `DEFAULT_DATA`, no `migrateState`, no `STORAGE_KEY`, no Supabase schema or sync path, and no IndexedDB. Constitution compliance remains under §15.4A and §22.5A: search stays literal substring only, read mode remains a user-opened archive, resurfacing remains one quiet user-authored fragment when available, and no counts, ranking, tags, summaries, sentiment, or derived observations were introduced. |
| EA-195 | Atlas: Body Desk Relief Lookup. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md` (§19D). Source files inspected: `js/constants.js`, `js/domains/body.js`, `js/ui/modals.js`, `index.html`, `css/temple.css`, `sw.js`. **Context:** EA-177 implemented the foundational chip-based Body relief lookup — replacing the paced somatic step-through with five area chips and a single curated reset per selection. EA-195 applies the Atlas patron framing formally: updating the relief copy to the ticket's curated phrasing and updating the modal sub to invoke load/carrying language. The chip selection logic, Returned button gate, `arriveBody()` behavior, and no-state-stored guarantee all remain exactly as EA-177 left them. **Patron Test:** Gate 1 — without the word Atlas, the feature is still useful: choose a body area while at a desk, read one safe reset instruction, tap Returned. Gate 2 — Atlas shapes the interaction through load, weight, carrying ("Where does the body carry its weight?"), and return — the five areas are where a desk worker carries physical load (neck, shoulders, back, wrists, eyes); the reset instructions are what Atlas does to set the burden down briefly. **`js/constants.js`:** updated `BODY_RELIEF_AREAS` with the ticket's curated copy — neck: "Let the chin lower slightly. Turn the head a little to one side, then the other. Do not force the range."; shoulders: "Lift both shoulders once, then let them fall. Let the collarbones widen."; back: "Sit a little taller. Let the lower back find the chair again. Breathe once into the ribs."; wrists: "Open the hands. Turn the palms up once, then down. Let the fingers soften."; eyes: "Look away from the screen. Let the eyes rest on something far, then blink slowly." All copy is non-medical, desk-safe, and non-prescriptive. **`index.html`:** updated Body modal sub from "Choose what needs returning." → "Where does the body carry its weight?" — invoking load and weight (Atlas carrying framing) while still directing the user toward area selection. Eyebrow remains "ATLAS", title remains `<em>Relief</em>`. **`sw.js`:** `CACHE_NAME` bumped `elysium-v100` → `elysium-v101`. **What was not changed:** `BODY_SOMATIC_INVITATIONS`, `getBodySomaticInvitations()` (still used by Work Flow body step in `flow.js`); chip HTML structure (`body-area-chips`, five `.body-area-chip` elements); `_bodySelectedArea` module state, chip click delegation, relief text population, Returned button visibility logic in `modals.js`; `openBodyModal()` reset behavior (chips clear, relief text clears, Returned hidden); `closeBodyModal()` + `applyTempleTrace('body')` path; `arriveBody()`, `getBodyArrivals()`, `state.body.arrivals` shape; horizon marks; `renderBodyHorizonMarks()`; Day Thread Body trace; Temple Body state; `DEFAULT_DATA.body`; `migrateState()` body section; `STORAGE_KEY`; Supabase schema; IndexedDB. **§19D compliance:** selected area is session-only (not stored); closing before Returned saves nothing; no history of which areas were selected; no frequency counts; no pain scales; no medical language; no symptom tracking. |
| EA-194 | Hypnos: Night Closure Payoff. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `js/domains/sleep.js`, `js/domains/today-plan.js`, `js/render/today-plan.js`, `js/ui/modals.js`, `css/today.css`, `css/modal.css`, `sw.js`. **Motivation:** Sleep had the structural handoff mechanics (EA-152 parking note, EA-176 morning handoff) but the copy didn't yet communicate that Hypnos is the keeper of what was safely placed down — that the room holds it and releases it in the morning. This ticket is a targeted copy and visual refinement with no logic changes. **Patron Test:** Gate 1 — without the word Hypnos, the feature is still useful: a parking note you write before sleep resurfaces once the next morning and then disappears. Gate 2 — Hypnos shapes the copy through the verbs of sleep, release, and safe holding ("What should not follow you into sleep.", "HYPNOS WILL HOLD WHAT WAITS", "HYPNOS HELD") — not through decorative mythology. **`index.html`:** (1) Sleep modal State A sub — changed "Nothing has to be solved before the room goes quiet." → "What remains can wait until morning." (communicates the handoff promise more directly). (2) Sleep modal parking textarea placeholder — changed "What can wait until morning." → "What should not follow you into sleep." (reframes the action as release, not deferral). (3) Sleep modal handoff line (`.sleep-handoff`) — changed "WHAT REMAINS CAN WAIT" → "HYPNOS WILL HOLD WHAT WAITS" (names the holder without mythological exposition). (4) Today morning handoff eyebrow — changed "LAST NIGHT LEFT" → "HYPNOS HELD" (signals that the note was safely held overnight, not merely left behind). **`css/today.css`:** (1) `.today-sleep-handoff-eyebrow` color — changed `var(--muted-text)` → `var(--bronze)` (bronze mono eyebrow matches patron-eyebrow convention used elsewhere: Asclepius, Kairos, Apollo guard; makes the night origin visually distinct from the Mind "HELD NEARBY" section). (2) `.today-sleep-handoff-text` color — changed `var(--oracle-text)` → `var(--muted-text)` (quieter nocturnal feel; the note has been held, not asserted; consistent with Spectral italic muted treatment in other contextual surfaces). **`sw.js`:** `CACHE_NAME` bumped `elysium-v99` → `elysium-v100`. **What was not changed:** `saveSleepClosure()`, `getSleepEntry()`, `getMorningSleepHandoff()`, `dismissSleepHandoff()` — all logic unchanged. `state.sleep.entries` shape unchanged. `state.today.sleepHandoffDismissedFor` behavior unchanged. Empty-note → no handoff behavior unchanged. Sleep shutdown carry/pass behavior unchanged. Today intentions, carryover, and `renderTodayPlan()` unchanged. Care/cycle, Chronicle, Temple, Body, Water, Mind, Progress, Settings untouched. No new state keys. `DEFAULT_DATA` unchanged. `migrateState()` unchanged. `STORAGE_KEY` unchanged. Supabase schema unchanged. IndexedDB untouched. **Constitution compliance:** §19C parking note is user-authored text displayed once (§22.5A, felt-not-read); no counts (no-counter); dismiss hides permanently for that date (reversibility); no note → section hidden (silence); not a task, not actionable (non-actionable); no score/dashboard/history (productivity-app). |
| EA-193 | Apollo: Daylight Protection. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md` (§19A, §22.5A, §27.8, §27.9). Source files inspected: `index.html`, `js/constants.js`, `js/state.js`, `js/domains/light.js`, `js/ui/flow.js`, `js/render/common.js`, `css/today.css`, `css/temple.css`, `sw.js`. **Motivation:** Apollo/Light was failing the Patron Utility Gate — the tone chooser (Soft/Clear/Steady/Guarded) was ephemeral, changed nothing in the app, and passed the patron no real job. Apollo's genuine domain — daylight, the morning threshold, SPF protection, and care-after-actives awareness — maps directly onto something Care already cares about. This ticket converts Light from a beautiful tap into a patron that shapes how the morning-skin-protection job gets done. **Patron Test:** Gate 1 — removing "Apollo" leaves "a morning question about sun exposure that turns on an SPF reminder in your skincare routine, with extra caution after exfoliation" — useful and missed. Gate 2 — Apollo shapes the timing (morning threshold), the restraint (one quiet line, no alarm), the hierarchy (defers to Care's SPF step), and the naming ("Apollo ·" prefix). Both gates pass; neither ditch (decoration or generic utility) applies. **`js/constants.js`:** added `DAYLIGHT_CONFIRMATIONS` object (`{ inside, some, strong }` → confirmation copy strings); added `APOLLO_PROTECTION_LINES` object (`{ some, strong, afterExfoliation }` → Care morning line strings). `LIGHT_OPENING_INVITATIONS` unchanged. **`js/domains/light.js`:** added `saveState` to import from `state.js`. Added module-level `_VALID_DAYLIGHT` Set (`inside`, `some`, `strong`). Added `saveDaylight(dateStr, level)` — creates the entry if missing (identical guard pattern to `witnessLight`), writes `entry.daylight = level` when `level` is valid, deletes `entry.daylight` otherwise, calls `saveState()`. Added `getDaylight(dateStr)` — reads `state.light?.entries?.[dateStr]?.daylight`, returns the value if valid, null otherwise. All existing exports (`getLightPeriodLabel`, `getLastWitness`, `getLightOpeningInvitation`, `witnessLight`) untouched. **`js/state.js` — `migrateState()`:** added one defensive line inside the existing light-entries loop immediately after the witnesses filter: if `entry.daylight` is present but not one of `['inside','some','strong']`, it is deleted. Valid `daylight` values pass through unchanged; old entries without `daylight` are unaffected (field is optional). **`index.html`:** (1) Morning Flow Light step — replaced the `<div id="flow-tone-chooser">` block containing four tone buttons (Soft/Clear/Steady/Guarded) with a `<div id="flow-daylight-section">` wrapper containing: a `<p class="flow-daylight-question">Will the day meet your skin?</p>` label and a `<div class="flow-daylight-chooser" id="flow-daylight-chooser">` with three `<button class="flow-daylight-btn" data-daylight="…">` chips (Mostly inside / Some sun / Strong sun). `flow-step-light` eyebrow, invitation paragraph, Entered button, and Skip button are all unchanged. (2) Care morning section — added `<p class="care-apollo-line" id="care-apollo-line" hidden></p>` between the `care-section-hd` closing `</div>` and the `care-morning-keeper` div. **`js/ui/flow.js`:** (1) imports — added `DAYLIGHT_CONFIRMATIONS` to constants import; added `saveDaylight` to light.js import. (2) Removed `FLOW_TONE_CONFIRMATIONS` constant. (3) Renamed `let _selectedTone = null` → `let _selectedDaylight = null`. (4) `renderLightStep()` — `_selectedTone = null` → `_selectedDaylight = null`; `querySelectorAll` updated from `#flow-tone-chooser .flow-tone-btn` to `#flow-daylight-chooser .flow-daylight-btn`; `toneChooser` variable renamed to `daylightSection`, bound to `#flow-daylight-section` (hides both question and chips together when `alreadyEntered`). (5) `registerMorningFlow()` — replaced `flow-tone-chooser` click handler with `flow-daylight-chooser` click handler: event-delegates to `.flow-daylight-btn`, uses `btn.dataset.daylight` and `_selectedDaylight` (same toggle pattern as before). (6) `flow-light-enter` click handler — added `if (_selectedDaylight) saveDaylight(todayStr, _selectedDaylight)` call between `witnessLight` and `saveState`; confirmation line now uses `DAYLIGHT_CONFIRMATIONS[_selectedDaylight]` falling back to `'The threshold is crossed.'`. Advance-to-Care path, 700ms delay, and flow session guard are all unchanged. **`js/render/common.js`:** (1) imports — added `APOLLO_PROTECTION_LINES` to the constants import; added `import { getDaylight } from '../domains/light.js'`. (2) Added `renderCareApolloLine(cycleDay)` — reads `getDaylight(todayStr)`; hides and clears `#care-apollo-line` when daylight is unset/`inside`; for `some`/`strong`, selects line: `cycleDay === 2` (tonight is Rest night = morning after Salicylic exfoliation) → `afterExfoliation` line; `strong` (not cycleDay 2) → `strong` line; `some` (not cycleDay 2) → `some` line; sets `el.textContent` and `el.hidden = false`. (3) `renderTodayCycle()` — calls `renderCareApolloLine(cycleDay)` after `renderNightProtocolControl`, before `renderCareProtocolMemory`. **`css/today.css`:** added `/* ── EA-193: Apollo protection line ── */` block — `.care-apollo-line`: `margin: 0 0 14px`, `font-family: var(--font-display)`, `font-style: italic`, `font-size: 13.5px`, `line-height: 1.55`, `color: var(--muted-text)`, `opacity: 0.82` (matches `.care-protocol-note` exactly — consistent Care morning tone, not alarming). `#pane-care .care-apollo-line[hidden] { display: none }`. **`css/temple.css`:** replaced `.flow-tone-chooser`/`.flow-tone-btn`/`.flow-tone-btn.is-selected` CSS blocks with: `.flow-daylight-question` (DM Sans 13px/center/oracle-text 0.60 opacity, `margin: 0 24px 10px` — quiet label, lighter than the Spectral invitation above it); `.flow-daylight-chooser` (flex/gap 8px/center/`margin: 0 20px 24px` — same layout as tone chooser); `.flow-daylight-btn` (flex:1/9px 4px padding/transparent bg/bronze border 0.20 opacity/radius-8/DM Sans 12.5px/oracle-text/0.65 opacity/cursor pointer/0.15s transition — practical sans-serif chips, not atmospheric italic); `.flow-daylight-btn.is-selected` (border-color bronze 0.55/bg bronze 0.07/opacity 0.90 — same selected state). **`sw.js`:** `CACHE_NAME` bumped `elysium-v98` → `elysium-v99`. **What was removed:** `FLOW_TONE_CONFIRMATIONS` constant; `_selectedTone` module-level var; tone chooser event listener; four `.flow-tone-btn` HTML elements; `flow-tone-chooser` HTML div; `.flow-tone-chooser`/`.flow-tone-btn` CSS rules. **What was preserved:** `witnessLight()` — still called on Entered, unchanged; `getLastWitness()` — still gates `alreadyEntered` path; `getLightOpeningInvitation()` — still populates the invitation paragraph; `state.light.entries` shape — `witnesses` array intact, `daylight` is an optional addition; `DEFAULT_DATA.light` — unchanged (the optional `daylight` field needs no DEFAULT_DATA entry); `LIGHT_OPENING_INVITATIONS` in constants.js — unchanged; Day Thread "Light entered." trace via `getLastWitness` in `temple.js` — unchanged; Temple atmosphere contributions from Light witnesses — unchanged; all Care task IDs, cycle logic, `advanceCycleIfNeeded`, `loggedDays`, `checks`, keepers, protocol notes, Asclepius guard, protocol memory — all untouched. **Constitution compliance:** §19A.5 — no calendar, no witness grid, no frequency display for Light; `daylight` is same-day context only, never surfaced as a history item. §22.5A — the Care apollo line reflects the user's own same-day daylight choice back to them (the room holds what they placed inside it). §27.2 Six Tests — (1) felt-not-read: a quiet italic sentence, not a metric; (2) no-counter: no sunny-day count, no SPF count, nothing counted; (3) reversibility: chip is optional and toggle-deselectable; `inside` or unset → nothing shown; (4) silence: default state is hidden, `inside` is also silent; (5) non-actionable: care line reinforces existing m2 SPF step, is not a new task; (6) productivity-app: no score, no dashboard, no trajectory. No new top-level state key. `STORAGE_KEY` unchanged. Supabase schema unchanged (JSONB `data` column accepts nested `light.entries[date].daylight`). IndexedDB untouched. |
| EA-192 | Complete the Water Retirement. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md` (including §19A, §19B, §19C, §27.8), `design/obsidian-temple/README.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, `elysium-app-concept/Elysium - Ritual Philosophy.html`, `elysium-app-handoff/elysium-app/project/Elysium - Pantheon Direction.html`, `elysium-app-concept/philosophy-presentation.jsx`, and `elysium-app-concept/philosophy-sketches.jsx`; the requested root-level `design-system.md`, `implementation-map.md`, philosophy HTML, and philosophy JSX files were not present, so the matching project/reference copies were read. Source files inspected: `js/ui/flow.js`, `index.html`, `css/temple.css`, `css/modal.css`, `js/domains/water.js`, `js/render/temple.js`, `js/render/today-plan.js`, `js/state.js`, `js/constants.js`, `sw.js`, and runtime Water references via `rg`. **Motivation:** Water remained as the last active user-facing action inside Work Flow even after standalone Water retirement. It failed the Utility Gate and Form Gate, so the runtime flow needed to become honest: Mind → Body → Complete. **`js/ui/flow.js`:** removed the dormant runtime import of `holdWater`; changed `WORK_STEPS` from `['work-mind','work-body','work-water','work-complete']` to `['work-mind','work-body','work-complete']`; deleted `renderWorkWaterStep()`; deleted `flow-work-water-hold` and `flow-work-water-skip` event handlers; body completion still uses the existing per-open `_flowSession` token and 700ms delayed transition safety, but now advances directly to `work-complete`; Body skip also goes directly to `work-complete`. Mind and Body standalone domain flows were untouched. **`index.html`:** removed the obsolete Work Flow Water step markup (`#flow-step-work-water`, `#flow-work-water-step`, `#flow-work-water-hold`, `#flow-work-water-skip`), leaving no visible or unreachable Water container in the flow modal. **`js/render/today-plan.js`:** removed the `hasHeldToday` import and changed the midday Focus row completion condition from Mind + Body + Water to Mind + Body only, so Today no longer treats Water as part of Focus completion. **`js/render/temple.js`:** removed the `hasHeldToday` import and removed the Day Thread line `Water was held.`; historical Water holdings remain in `collectTemplePresenceDays()` so old presence data can still contribute to ambient room warmth without surfacing Water as a current action. **CSS:** no Water-specific Work Flow CSS remained; shared `#flow-modal` and Body/Mind styles were left unchanged. **Compatibility preserved:** no changes to `state.water`, `water.holdings`, `DEFAULT_DATA.water`, `migrateState()` Water validation, `syncFromSupabase()` Water merge, `STORAGE_KEY`, Supabase schema, IndexedDB, or dormant `js/domains/water.js`; `sw.js` kept `./js/domains/water.js` in `ASSETS`. **`sw.js`:** `CACHE_NAME` bumped `elysium-v97` → `elysium-v98`; `ASSETS` unchanged. No Care, Today intention, Chronicle archive/search, Sleep handoff, Mind offload, Body standalone relief, schema, migration, or storage behavior changed. |
| EA-184 | Patron Doctrine. Documentation-only ticket. Added Patron Doctrine to `CLAUDE.md`: every domain is a patron, a patron is the reason a surface has its shape, every surface must pass the Utility Gate and Form Gate, both failure modes are forbidden (decoration and generic utility), patron map added, and the tab-label rule clarified so navigation stays plain while patron names live in eyebrow/subtitle/context. Added `docs/continuity-constitution.md` §27.9 Patron Test: Patron Test governs whether a surface deserves to exist; the Six Tests govern how a surface remembers; Patron Test comes before implementation; Gate 1 is Utility; Gate 2 is Form; the Two-Ditch Rule names drift-one as decoration and drift-two as generic utility; any future ticket must check both. Updated `README.md` product description to describe Elysium as a patron-led daily operating system anchored in skincare, Today, Chronicle, and closure without adding mythology exposition. No app code changed. No HTML/CSS/JS changed. No service worker bump. No state, storage, schema, or cache version changes. No behavior changes. |
| EA-183 | Post-Water Fold Stabilization & Surface Cleanup. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`; `implementation-map.md` was requested but is not present at the project root. Source files inspected: `index.html`, `main.js`, `js/ui/modals.js`, `js/ui/flow.js`, `js/render/temple.js`, `js/render/today-plan.js`, `js/domains/water.js`, `js/state.js`, `js/constants.js`, `css/temple.css`, `css/today.css`, `css/modal.css`, and `sw.js`. Audit result: no runtime standalone Water card/modal IDs, modal exports, registration calls, event listeners, imports, or CSS selectors remained to remove. Preserved intentionally: Work Flow Water step markup and handlers (`flow-step-work-water`, `flow-work-water-step`, `flow-work-water-hold`, `flow-work-water-skip`), `holdWater()` / `hasHeldToday()` / `state.water.holdings`, Today midday Focus completion check, Temple warmth contribution, and Day Thread text `Water was held.`. Temple ordering is stable because Water is absent from `TRACE_CARD_IDS` and `RHYTHM_CARD_IDS`; `queueTempleTrace('water')` from Work Flow remains safe because `applyTempleTrace()` returns early when no card ID exists. No `DEFAULT_DATA`, migration, `STORAGE_KEY`, Supabase, IndexedDB, Work Flow behavior, Today Plan design, or Temple design changed. `sw.js`: `CACHE_NAME` bumped `elysium-v93` -> `elysium-v94`; `ASSETS` unchanged. |
| EA-181 | Light Role Decision — Option B: fold Light into Morning Flow only; removed standalone Light Temple card (`#temple-goto-light`) and standalone Light modal (`#light-modal`) from `index.html`; removed `witnessLight`/`getLightOpeningInvitation` imports and all Light modal code (`renderLightModal`, `openLightModal`, `closeLightModal`, `_lightWitnessedThisSession`, `_selectedLightTone`, `LIGHT_TONE_CONFIRMATIONS`, five event listener registrations) from `main.js`; removed `'light'` from `TRACE_CARD_IDS` and `RHYTHM_CARD_IDS` in `temple.js`; removed `lightDone` variable from `getRhythmMap()` and all `light:` entries in all period branches; removed `lightStateEl` / `temple-light-state` render block from `renderTemple()`; `js/domains/light.js` and `LIGHT_OPENING_INVITATIONS` in `constants.js` kept intact (still consumed by Morning Flow Step 1 in `flow.js`); `getLastWitness` import in `temple.js` kept (still used by `getDailyLine()` and `deriveTodayTraces()`); `state.light.entries` shape, `DEFAULT_DATA.light`, and `migrateState()` Light migration all unchanged (historical entries continue feeding warmth coefficient, quiet-stretch, presence days); Morning Flow Step 1 (Light tone chooser + invitation + "Entered") untouched in `flow.js` and `index.html`; no state/migration/schema/IndexedDB/Care/cycle/Today/Chronicle/Sleep/Temple atmosphere changes. SW → `elysium-v92`. |
| EA-178B | Mind Modal Rebuild — Mental-Load Offload v2. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `js/constants.js`, `js/state.js`, `js/domains/mind.js`, `js/domains/today-plan.js`, `js/render/today-plan.js`, `js/render/temple.js`, `js/ui/modals.js`, `css/modal.css`, `css/temple.css`, `sw.js`. **Motivation:** EA-178A introduced a 2-state modal (Set it down / Keep nearby + State B reflection). The "Set it down" / "Keep nearby" distinction was unintuitive. The State B reflection surface was rarely used and added friction. This ticket collapses Mind to a single offload surface with three clear actions and adds a practical "Send to Today" path. **State:** No `DEFAULT_DATA` change. No `migrateState` change. `state.mind.thread` backing store retained exactly as EA-178A left it — same migration, same expiry-at-day-end behavior. `state.mind.sessions` / `state.mind.arrivals` unchanged. **`js/domains/mind.js`:** added `saveState` to import from `state.js` (was not previously imported). Added three new exports: `getMindOffload(dateStr)` — reads `state.mind.thread`, returns `{ text, updatedAt }` if thread exists for the given date and has a non-empty text string, else returns null; `saveMindOffload(text)` — trims + caps text to 300 chars, writes `state.mind.thread = { date: todayStr, text, keptNearby: true, updatedAt }`, calls `arriveMind(todayStr)` to mark presence, calls `saveState()`, returns boolean; `clearMindOffload()` — sets `state.mind.thread = null`, calls `saveState()`. Existing `getMindThread`, `saveMindThread`, `clearMindThread` retained (still used by internal code; `getMindThread`/`clearMindThread` now wrapped by new exports). **`index.html`:** Mind modal block replaced. Old structure (two `<div id="mind-state-a">` / `<div id="mind-state-b">` children) replaced with a flat structure: `ATHENA / THREAD` eyebrow, `h3.mind-title` "Set one thing *down.*", `p.mind-offload-sub`, `textarea#mind-offload-input` (rows 3, placeholder "What is taking space.", autocorrect/autocapitalize/spellcheck attrs), `p#mind-offload-message[hidden]`, `div.mind-offload-actions` containing three `type="button"` buttons (`#mind-offload-save` "Set it down", `#mind-offload-today` "Send to Today", `#mind-offload-clear` "Clear"), `div#mind-threads[hidden]` (past reflection history, retained for Chronicle Well). Removed: `#mind-state-a`, `#mind-state-b`, `#mind-one-thing`, `#mind-sub`, `.mind-ephemeral-field`, `.mind-action-row`, `#mind-set-down`, `#mind-keep-nearby`, `#mind-confirm-text`, `.mind-reflect-section`, `#mind-reflection`, `#mind-complete`. **`js/ui/modals.js`:** (1) Import — removed `arriveMind, saveMindReflection, getMindThread, saveMindThread, clearMindThread`; added `getMindOffload, saveMindOffload, clearMindOffload`; added `addTodayIntention` to today-plan import; added `renderTodayPlan` import from `../render/today-plan.js`. (2) Removed: `showMindState()`, `resetMindRitualFields()`. (3) Added `showMindMessage(text)` — sets `#mind-offload-message` text and unhides it. (4) `openMindModal()` rewritten — reads `getMindOffload(todayStr)`, populates `#mind-offload-input` with today's text or empty, clears and hides message element, calls `renderMindThreads()`, shows modal, focuses input with 180ms delay. (5) `closeMindModal()` rewritten — hides modal, applies temple trace when `_mindArrivedThisSession`. (6) `registerMindModal()` rewritten — `mind-offload-save`: empty check → "Nothing to set down."; else calls `saveMindOffload(text)` (which saves state + arriveMind internally) → marks `_mindArrivedThisSession = true` → `renderTemple()` → shows "Set down."; `mind-offload-today`: empty check → "Nothing to place."; duplicate check (compares `i.text === text` against current intentions) → "Already placed in Today."; calls `addTodayIntention(text)` → if full → "Today is already holding three."; if ok → `clearMindOffload()` (which saves state internally) + clear textarea + mark arrived + `renderTemple()` + `renderTodayPlan()` + "Placed in Today."; `mind-offload-clear`: `clearMindOffload()` + clear textarea + "Cleared.". **`js/render/today-plan.js`:** (1) Removed `saveState` from state.js import (was only used in the thread release handler, now moved inside `clearMindOffload`). (2) Swapped imports: `getMindThread, clearMindThread` → `getMindOffload, clearMindOffload`. (3) `renderMindThread()` rewritten — calls `getMindOffload(todayStr)` instead of `getMindThread`; the old `if (!thread?.keptNearby)` gate changed to `if (!offload)` so any today-scoped offload (regardless of `keptNearby` flag) shows the section; `textEl.textContent = offload.text`; release button `onclick` calls `clearMindOffload()` (which saves internally), no explicit `saveState()` call needed. **`css/modal.css`:** replaced Mind modal CSS between `.mind-title em` and `/* Past reflections surface */`. Removed: `.mind-sub`, `.mind-ephemeral-field`, `::placeholder`, `:focus`, `.mind-action-row`, `.mind-set-down-btn`, `:active`, `#mind-state-b[hidden]`, `.mind-confirm`, `.mind-reflect-section`, `.mind-reflect-label`, `.mind-reflection`, `.mind-reflection::placeholder`. Added: `.mind-offload-sub` (Spectral italic 14px/1.5/muted-text, margin 0 0 18px); `.mind-offload-input` (full-width, transparent, no border, border-bottom separator-warm, 10px 0 11px padding, Spectral italic 16px/1.55, oracle-text, no resize/outline, bronze caret); `.mind-offload-input::placeholder` (muted-text 0.52); `.mind-offload-input:focus` (border-bottom rgba(201,165,107,0.48)); `.mind-offload-message` (Spectral italic 14px, muted-text 0.80 opacity, margin 12px 0 0); `.mind-offload-actions` (flex column, gap 8px, margin-top 20px). `.mind-threads`, `.mind-threads-label`, `.mind-thread*` blocks retained (Chronicle Well still populates these). **`docs/continuity-constitution.md`:** §18.8 description updated — "Set it down" now means save (not clear); "Clear" now means release; "Send to Today" option described; "held nearby" Today surface still mentioned. No other constitution change. No new article, no new amendment. **`sw.js`:** `CACHE_NAME` bumped `elysium-v90` → `elysium-v91`. **What was removed:** State A / State B structure; "Set it down" (old meaning: clear) and "Keep nearby" buttons; 180ms focus-after-state-switch; `showMindState()` function; `resetMindRitualFields()`; `saveMindThread` direct call in modals.js (now inside `saveMindOffload`); direct `arriveMind` call in modals.js (now inside `saveMindOffload`); direct `saveState` calls for mind actions in modals.js (now inside domain functions). **What was preserved:** `state.mind.thread` backing store and migration; `state.mind.sessions` and `getMindReflectionEntries()` / `renderMindThreads()` / Chronicle Well feeding; `arriveMind()` called on save or send-to-today (presence still marks correctly); `applyTempleTrace('mind')` on close-after-action; Temple mind-state/mind-line render unchanged; Today "HELD NEARBY" section UI unchanged (HTML, CSS, eyebrow text, release button all retained from EA-178A); `hasMindArrival` used by Temple and Today renderOpenItems unchanged. **Constitution compliance:** same-day user text returned to user (§22.5A); no counts (no-counter); offload expires daily, user can Clear (reversibility); no offload → textarea empty, message hidden (silence); Today section is single text read, not archive navigation (non-actionable); no outcome states, no performance framing (productivity-app). |
| EA-180 | Today Intention Inline Edit. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `js/constants.js`, `js/state.js`, `js/domains/today-plan.js`, `js/render/today-plan.js`, `main.js`, `css/today.css`, `sw.js`. **`js/domains/today-plan.js`:** added `updateIntention(id, text)` export after `deleteIntention` — finds intention by id, trims and caps text to `MAX_TEXT` (120), returns `false` without saving when intention not found or text is empty after trim, otherwise mutates `intention.text` in place (preserving `id`, `kept`, `addedAt`) and calls `saveState()`, returns `true`. **`js/render/today-plan.js`:** added `data-id="${escapeHtml(i.id)}"` attribute to the `.today-plan-intention` row `<div>` in `renderIntentions()` so that click delegation in `main.js` can retrieve the intention ID from the row without traversing child buttons. **`main.js`:** (1) added `updateIntention` to the import from `./js/domains/today-plan.js`; (2) refactored the `today-plan-intentions` click handler — the existing `[data-action]` button branch now has an explicit `return` after handling so clicks on mark/release buttons cannot propagate to the text-edit path; added a `.today-plan-text` branch that calls `_startInlineEdit(row)` when `e.target.closest('.today-plan-text')` resolves and its parent row is found; (3) added private `_startInlineEdit(row)` function near the other Today handlers — reads `row.dataset.id` and `row.querySelector('.today-plan-text').textContent`; creates an `<input type="text" class="today-plan-inline-input">` pre-filled with current text (maxLength 120, autocomplete off); calls `textEl.replaceWith(input)` then `input.focus()` + `input.select()`; a `done` boolean prevents double-fire when Enter triggers both `keydown` and subsequent `blur`; `save()` calls `updateIntention(id, input.value)` only when value is non-empty after trim, then always calls `renderTodayPlan()`; `cancel()` calls `renderTodayPlan()` without saving; `keydown` handler fires `save()` on Enter and `cancel()` on Escape; `blur` handler fires `save()` (empty blur therefore reverts to previous text by re-rendering unchanged state). **`css/today.css`:** appended EA-180 Intention inline edit block — `#pane-today .today-plan-inline-input`: `flex: 1`, `background: transparent`, `border: none`, `border-bottom: 1px solid var(--bronze)` (signals edit mode), `padding: 0 2px 2px`, `font-family: var(--font-sans)`, `font-size: 15px`, `font-weight: 400`, `color: var(--oracle-text)`, `letter-spacing: -0.005em`, `outline: none`, `caret-color: var(--bronze)`, `line-height: 1.4`, `min-width: 0`. Font and size match `.today-plan-text` exactly; no layout shift occurs when the input replaces the span. The bronze underline is the only visual cue that edit mode is active. **`sw.js`:** `CACHE_NAME` bumped `elysium-v88` → `elysium-v89`. No new state keys. No `DEFAULT_DATA` change. No `migrateState` change. No `STORAGE_KEY` change. No Supabase schema change. No IndexedDB change. No HTML change. Carryover, sleep shutdown carry/pass, `bringForwardIntention`, `letIntentionPass`, `normalizeTodayPlanIfNeeded`, kept toggle, release/delete, and add-intention flows all unchanged. Care/cycle logic, Chronicle, Sleep, Temple, Progress, Settings untouched. |
| EA-179 | Water Demotion / Ambient Pause Simplification. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `js/constants.js`, `js/domains/water.js`, `js/ui/modals.js`, `js/ui/flow.js`, `js/render/temple.js`, `js/render/today-plan.js`, `css/temple.css`, `sw.js`. **Motivation:** Water's previous step-through sequence (`WATER_RESET_STEPS`, Continue button, rotation) made it feel like a mandatory ceremony. It has no honest utility that avoids hydration tracking or counts. This ticket demotes it to a minimal ambient pause: one screen, one instruction, one button. **`js/constants.js`:** removed `WATER_CONTACT_INVITATIONS` (5-item array), `WATER_RITUALS` (27-item array), and `WATER_RESET_STEPS` (7-item array × 3-step arrays). **`js/domains/water.js`:** removed `getDayOfYear` from utils import (no longer needed); removed constants import line entirely; removed `getWaterRitual()`, `getWaterContactInvitation()`, and `getWaterResetSteps()` functions. Retained: `ensureWaterState`, `holdWater`, `getWaterHoldings`, `getWaterHoldCount`, `hasHeldToday`. **`js/ui/modals.js`:** removed `getWaterRitual, getWaterResetSteps` from water.js import (only `getWaterHoldings, holdWater` remain); removed module-level vars `_waterResetStep = 0` and `_waterResetSteps = []`; replaced `openWaterModal()` — new version resets hold button visibility, sets `water-step-text` to static `'Notice the temperature of what you are holding.'`, calls `renderWaterSurfaceMarks()`; replaced `registerWaterModal()` — removed `water-step-continue` event listener block entirely; `water-hold-btn` handler now calls `holdWater`, sets completion text `'The pause has been held.'`, hides button. **`js/ui/flow.js`:** removed `getWaterResetSteps` from water.js import; removed module-level vars `_workWaterStep = 0` and `_workWaterSteps = []`; replaced `renderWorkWaterStep()` — new version sets single static instruction on `flow-work-water-step`, shows `flow-work-water-hold` and `flow-work-water-skip` directly; removed `flow-work-water-continue` event listener block. **`index.html`:** Water modal — changed eyebrow `HYDROS` → `HYDROS / PAUSE`; changed title `<em>Held.</em>` → `<em>Hold for a moment.</em>`; removed `<p class="water-ritual" id="water-ritual"></p>`; removed `<button class="btn-primary" id="water-step-continue">Continue</button>`; removed `hidden` from `water-hold-btn`. Work Flow water step — updated comment; removed `<button class="btn-primary" id="flow-work-water-continue">Continue</button>`; removed `hidden` from `flow-work-water-hold`. **`css/temple.css`:** removed `#water-modal .water-ritual { … }` CSS block (8 lines, dead after `water-ritual` element removed). **What remains:** `holdWater()`, `getWaterHoldings()`, `getWaterHoldCount()`, `hasHeldToday()` unchanged; horizon marks (`.water-surface-wrap`, `.water-marks`, `.water-holding-mark` CSS) unchanged; Temple card state "Unstirred"/"Held" unchanged; Day Thread "Water was held." trace unchanged; warmth coefficient Water contribution unchanged; `.water-ritual` CSS removed (element gone); `water-step-text` element stays for static instruction + completion phrase; `state.water.holdings` shape unchanged; no migration; no new state keys; Supabase schema unchanged. **Constitution note:** Water remains an ambient presence-only domain; it does not track hydration, quantity, recurrence, or history. **`sw.js`:** `CACHE_NAME` bumped `elysium-v87` → `elysium-v88`. |
| EA-178A | Mind Mental-Load Offload. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `js/constants.js`, `js/state.js`, `js/domains/mind.js`, `js/ui/modals.js`, `js/render/temple.js`, `js/render/today-plan.js`, `css/modal.css`, `css/today.css`, `sw.js`. **`docs/continuity-constitution.md`:** added §18.8 — Mind may hold one user-authored thread for the current day; it is a momentary offload (not a task, not a goal, not a performance target); expires at end-of-day automatically via `migrateState`; may surface quietly in Mind and Today as "held nearby"; user may clear it at any time; no history of held threads retained across days; no outcome state attached; does not feed counts, streaks, or analytics. **`js/constants.js`:** added `thread: null` to `DEFAULT_DATA.mind` (new sub-field alongside `sessions` and `arrivals`). **`js/state.js` — `migrateState()`:** added thread validation block after `mind.arrivals` migration — accepts `{ date: _todayStr, text: string, keptNearby, updatedAt }` only; clears to `null` if date doesn't match today, text is missing/blank, or shape is invalid; text trimmed and capped at 500 chars. **`js/domains/mind.js`:** added three exports: `getMindThread(dateStr)` — returns today's thread object or null; `saveMindThread(dateStr, text)` — writes `{ date, text, keptNearby: true, updatedAt }` to `state.mind.thread`; `clearMindThread()` — sets `state.mind.thread = null`. **`index.html`:** (1) Mind modal completely replaced — old 3-state HTML removed (Gathering/Holding/Inscribing eyebrows, stage labels, `#mind-begin`, `#mind-end`, `#mind-held-thread`, `#mind-holding`); new 2-state HTML: State A — `ATHENA / THREAD` eyebrow, "Set one thing *down*" title, Spectral italic sub, `#mind-one-thing` textarea (3 rows, autocorrect/autocapitalize/spellcheck attrs), `.mind-action-row` with `#mind-set-down` ("Set it down") and `#mind-keep-nearby` ("Keep nearby") buttons, `#mind-threads` past reflections; State B — `ATHENA / THREAD` eyebrow, `#mind-confirm-text` paragraph, `.mind-reflect-section` with `.mind-reflect-label` and `#mind-reflection` textarea, `#mind-complete` "Leave" button. (2) Today pane — added `div.today-mind-thread#today-mind-thread[hidden]` block above `#today-sleep-handoff`; contains `.today-mind-thread-eyebrow` "HELD NEARBY", `p#today-mind-thread-text`, `button#today-mind-thread-release` "Set it down". **`js/ui/modals.js`:** (1) updated import — added `getMindThread, saveMindThread, clearMindThread` from `domains/mind.js`; (2) removed `_mindHoldTimer = null` module-level var; (3) `renderMindThreads()` label changed "Threads kept nearby" → "Earlier threads" (resolves naming conflict); (4) `showMindState()` now handles only `'a'`/`'b'` (State C removed); (5) `resetMindRitualFields()` simplified — clears `mind-one-thing`, `mind-reflection`, `mind-confirm-text`; (6) `openMindModal()` — pre-fills `mind-one-thing` with today's held thread text if one exists; (7) `closeMindModal()` — timer cleanup removed (timer eliminated); (8) `registerMindModal()` completely rewritten — `mind-set-down` click: calls `clearMindThread()`, `arriveMind()`, sets confirm text "The thread has been set down.", shows State B, focuses reflection; `mind-keep-nearby` click: reads textarea, calls `saveMindThread()` if text present, calls `arriveMind()`, sets confirm text "The thread is held nearby.", shows State B, focuses reflection; `mind-complete` click: calls `saveMindReflection()` if reflection text, closes modal; `mind-begin`, `mind-end` listeners removed. **`js/render/today-plan.js`:** (1) added `saveState` to `state.js` import; (2) added `getMindThread, clearMindThread` to `domains/mind.js` import; (3) added `renderMindThread()` — reads `getMindThread(todayStr)`, hides section when no `keptNearby` thread; sets `textEl.textContent` (XSS-safe); wires `releaseBtn.onclick` to `clearMindThread()` + `saveState()` + hide section; (4) `renderTodayPlan()` calls `renderMindThread()` after `renderMasthead()`. **`css/modal.css`:** Mind modal section completely replaced — removed: `.mind-stage`, `.mind-stage-label`, `.mind-stage-invitation`, `#mind-state-b` flex-center layout, `.mind-holding` 44px display, `.mind-holding-invitation`, `.mind-end-btn`, `.mind-end-btn.is-waiting`, `#mind-state-c[hidden]`, `.mind-held-thread`, `.mind-held-thread[hidden]`, `#mind-state-b .mind-eyebrow` special margin; added: `.mind-action-row` (flex, gap 12px, margin-top 20px), `.mind-set-down-btn` (transparent bg, separator-warm border, radius-lg, DM Sans 14px, muted-text, 44px min-height), `#mind-state-b[hidden] { display: none }`, `.mind-confirm` (Spectral italic 20px/1.4, oracle-text, margin 4px 0 28px), `.mind-reflect-section` (border-top, padding-top 16px, margin-bottom 20px), `.mind-reflect-label` (Spectral italic 13px, muted-text 0.70 opacity); `.mind-ephemeral-field` updated (font-size 16px, caret-color bronze); `.mind-reflection` updated (font-size 15px, caret-color bronze). **`css/today.css`:** appended EA-178 Mind held-nearby block — `#pane-today .today-mind-thread[hidden] { display: none }`; `.today-mind-thread` (border-top/bottom separator-warm, padding 13px 0, margin-bottom 22px); `.today-mind-thread-eyebrow` (mono 9px/0.20em/uppercase/bronze/0.72 opacity, margin-bottom 7px); `.today-mind-thread-text` (Spectral italic 15px/1.5, oracle-text 0.88 opacity, margin 0 0 10px); `.today-mind-thread-release` (transparent btn, DM Sans 12.5px, muted-text). **`sw.js`:** `CACHE_NAME` bumped `elysium-v86` → `elysium-v87`. **Old behavior removed:** 3-state Gathering→Holding→Inscribing ceremony; 60-second `_mindHoldTimer` hidden-button wait; "Enter the hold" button; "Hold." 44px center-aligned display; State C (separate inscribing screen); "MIND / GATHERING", "MIND / HOLDING", "MIND / INSCRIBING" eyebrows; stage labels; `.mind-stage`/`.mind-holding`/`.mind-end-btn` CSS. **What survives:** `arriveMind()` — called on both actions; `saveMindReflection()` — optional from State B; `getMindReflectionEntries()` / `renderMindThreads()` — past reflections still shown in State A, label changed; Temple mind-state/mind-line unchanged; Chronicle Well feeding via reflection unchanged. **No state changes beyond `mind.thread`:** `DEFAULT_DATA` otherwise unchanged; `STORAGE_KEY` unchanged; Supabase schema unchanged (JSONB accepts new nested field); IndexedDB untouched; Care cycle/task/check/loggedDays untouched; Chronicle, Sleep, Temple, Progress, Settings untouched. **Constitution §18.8 six-tests:** thread is user's own text returned to them (§22.5A, felt-not-read); no counts anywhere (no-counter); thread expires daily, user can clear anytime (reversibility); no thread → section hidden (silence); shown as Spectral italic text, no task completion state (non-actionable); offloading one thought is not performance optimization (productivity-app). |
| EA-178 | Today/Sleep Bug Fix Batch. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, Obsidian Temple design references. Source files inspected: `index.html`, `main.js`, `js/sync.js`, `js/ui/tabs.js`, `js/render/today-plan.js`, `js/ui/modals.js`, `css/base.css`, `css/layout.css`, `css/today.css`, `css/modal.css`, `css/temple.css`, `sw.js`. **Bug 1:** first-launch Today was still activated and rendered before boot reveal; previous scroll and `.app` `min-height` nudges did not replay the pane lifecycle that actually corrects the layout on tab return. `main.js` now activates Today through `switchTab('today', { skipClosedDayCheck: true })` before Supabase can reveal the boot shell, and registers a boot-reveal layout callback. `js/ui/tabs.js` adds `refreshActivePaneLayout()` which re-renders the active pane, resets scroll, briefly replays its display layout, and finishes the pane animation immediately to avoid visible flicker. `js/sync.js` replaces the failed app-height reflow with a one-time `registerBootRevealLayoutCallback()` call on the reveal frame and makes `hideBootShell()` idempotent. Existing `switchTab()` behavior is unchanged for normal navigation. **Bug 2:** `css/today.css` tightens Today spacing only: masthead bottom, section-header padding, empty/add spacing, carryover/open row padding, Open section margin, footer padding, and Today footer spacer. EA-171A naked rows, separators, Spectral labels, and no-card direction remain intact. **Bug 3:** `css/modal.css` removes duplicate Sleep divider lines by stripping invitation row separators, keeping one calm boundary after invitations/shutdown, grouping unfinished intentions with flex gap instead of table-like borders, and tightening the parking/handoff spacing. **Cache:** `sw.js` bumped `elysium-v84` → `elysium-v85`. No state shape changes, no DEFAULT_DATA/migration changes, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB change, no Care cycle/task/check/loggedDays changes, no Chronicle search changes, no Today intention CRUD/carryover semantic changes, and no Sleep closure/parking/carry-pass behavior changes. |
| EA-177 | Body Desk Relief Lookup. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `sw.js`, `js/constants.js`, `js/domains/body.js`, `js/ui/modals.js`, `js/ui/flow.js`, `css/temple.css`. **`docs/continuity-constitution.md`:** added §19D · The Body Relief Exception (after §19C.6, before Article V) — permits same-session area choice (neck/shoulders/back/wrists/eyes), one fixed curated reset instruction per area, existing Body arrival presence; explicitly forbids pain scales, symptom history, body maps over time, frequency counts, medical language, HealthKit, past-behavior recommendations, or any claim the app knows what is wrong with the user's body. **`js/constants.js`:** added `BODY_RELIEF_AREAS` object — 5 keys (`neck`, `shoulders`, `back`, `wrists`, `eyes`) each mapped to a single short, safe, non-medical reset instruction string. No other constants added or removed; `BODY_RITUALS` and `BODY_SOMATIC_INVITATIONS` retained (still used by `body.js` for `getBodyRitual`/`getBodySomaticInvitations`, and by `flow.js` for Work Flow Body step). **`js/ui/modals.js`:** (1) import line: removed `getBodyRitual, getBodySomaticInvitations` from `body.js` import; added `BODY_RELIEF_AREAS` to `constants.js` import; (2) removed module-level vars `_bodyResetStep` and `_bodyResetInvitations`; added `let _bodySelectedArea = null`; (3) rewrote `openBodyModal()` — resets `_bodyArrivedThisSession = false`, `_bodySelectedArea = null`, clears `.is-selected` on all chips, empties `#body-relief-text`, hides `#body-arrive-btn`, calls `renderBodyHorizonMarks()`, shows modal; (4) rewrote `registerBodyModal()` — backdrop/close handlers unchanged; replaced `body-step-continue` click handler with `body-area-chips` event-delegated click handler that finds the tapped `.body-area-chip`, removes `.is-selected` from all chips, adds `.is-selected` to tapped chip, writes `BODY_RELIEF_AREAS[area]` to `#body-relief-text`, shows `#body-arrive-btn`; arrive handler now writes 'The body has returned.' to `#body-relief-text` (was `#body-step-text`). **`index.html`:** replaced Body modal inner content — removed `<p class="body-step-text" id="body-step-text">`, `<p class="body-ritual" id="body-ritual">`, `<button id="body-step-continue">Continue</button>`; changed title from `<em>Standing</em>` to `<em>Relief</em>`; added `<p class="body-sub">Choose an area.</p>` and `<div class="body-area-chips" id="body-area-chips">` with 5 `<button class="body-area-chip" data-area="…">` elements; added `<p class="body-relief-text" id="body-relief-text"></p>`; horizon wrap, Returned button, Close button structure unchanged. **`css/temple.css`:** removed `.body-step-text` and `.body-ritual` rule blocks; removed `.btn-primary.is-still` rule block (class no longer applied); added `.body-sub` (DM Sans 14px, center, muted-text, margin 0 20px 20px); added `.body-area-chips` (flex, wrap, gap 10px, center, margin 0 20px 24px); added `.body-area-chip` (transparent bg, separator-warm 1px border, 20px radius, 6px 16px padding, DM Sans 14px, oracle-text, cursor pointer, 0.15s transition); added `.body-area-chip.is-selected` (bronze border-color, bronze color); added `.body-relief-text` (Spectral italic 17px/1.55, center, oracle-text 0.88 opacity, min-height 54px, margin 0 24px 28px). **`sw.js`:** `CACHE_NAME` bumped `elysium-v89` → `elysium-v90`. **No state changes:** `DEFAULT_DATA` unchanged; `migrateState()` unchanged; `STORAGE_KEY` unchanged; Supabase schema unchanged; IndexedDB untouched. **No regressions:** `arriveBody()` and `getBodyArrivals()` behavior unchanged; horizon marks retained and rendered on open; `closeBodyModal()` + `applyTempleTrace('body')` path unchanged; `_bodyArrivedThisSession` flag logic unchanged; Work Flow Body step in `flow.js` still uses `getBodySomaticInvitations()` (unaffected). Care, Today, Chronicle, Sleep, Temple, Progress, Settings untouched. §19D compliance: no area is stored; closing before Returned saves nothing; no history of which areas were selected; no frequency counts. |
| EA-176 | Sleep Shutdown → Morning Handoff. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `sw.js`, `js/constants.js`, `js/state.js`, `js/domains/today-plan.js`, `js/render/today-plan.js`, `js/domains/sleep.js`, `js/ui/modals.js`, `js/render/temple.js`, `js/render/common.js`, `css/today.css`, `css/modal.css`, `css/temple.css`. **`docs/continuity-constitution.md`:** added §19C · The Sleep Shutdown Exception (after §19B.6, before Article V) — permits Sleep modal to read and display today's unkept Today intentions; permits carry/pass decisions per intention (session-local, written on Sleep save); permits next-morning Today to surface last night's parking note as a one-time user-dismissible handoff; explicitly forbids carry counts, carry history, sleep quality/duration/bedtime data, recurrence tracking, AI suggestions, pattern observations. **`js/constants.js`:** added `sleepDecisions: []` and `sleepHandoffDismissedFor: null` to `DEFAULT_DATA.today`. No other DEFAULT_DATA changes. **`js/state.js` — `migrateState()`:** added two sanitizers in the `s.today` block after `carryoverDate` — `s.today.sleepDecisions` filtered to valid `{id: string, carry: boolean}` objects or reset to `[]`; `s.today.sleepHandoffDismissedFor` validated as a YMD string or reset to `null`. **`js/domains/today-plan.js`:** (1) added `ymd` to utils import; (2) extended `normalizeTodayPlanIfNeeded()` — when `gap === 1`, if `plan.sleepDecisions` is non-empty, computes a `passIds` Set from decisions with `carry: false` and filters unkept intentions against it (only explicitly passed items are excluded; undecided and explicitly carried items carry through); clears `sleepDecisions = []` after applying; gap > 1 branch also clears `sleepDecisions`; fallback (no decisions) preserves existing auto-carry-all behavior; (3) added export `saveSleepShutdownDecisions(decisionsMap)` — always writes to `state.today.sleepDecisions` (even when map is empty, to clear any stale decisions), saves state; (4) added export `getMorningSleepHandoff()` — computes yesterdayStr, returns null when `sleepHandoffDismissedFor === yesterdayStr`, otherwise returns `state.sleep?.entries?.[yesterdayStr]?.note?.trim()` or null; (5) added export `dismissSleepHandoff()` — sets `sleepHandoffDismissedFor = yesterdayStr`, saves state. **`js/ui/modals.js`:** (1) added import `saveSleepShutdownDecisions` from `domains/today-plan.js`; (2) added module-level `let _sleepShutdownDecisions = new Map()`; (3) added private `renderSleepShutdown()` — reads `Array.isArray(state.today?.intentions)` defensively, filters unkept, hides `#sleep-shutdown` when list is empty, otherwise builds per-intention rows with `textContent` (XSS-safe), each with `type="button"` Carry tomorrow and Let pass buttons; toggle logic: tapping selected button deselects it (removes class), tapping other button selects it (adds `is-carry`/`is-pass`, removes other); `item.classList` reflects current decision visually; (4) extended `openSleepModal()` State A branch — resets `_sleepShutdownDecisions = new Map()` then calls `renderSleepShutdown()` after `renderSleepInvitations()`; (5) extended `closeSleepModal()` — resets `_sleepShutdownDecisions = new Map()` before hiding modal, preventing stale decisions from leaking across sessions; (6) extended `saveSleepModal()` — calls `saveSleepShutdownDecisions(_sleepShutdownDecisions)` first, then resets map, then proceeds with existing `saveSleepClosure` path. **`js/render/today-plan.js`:** (1) added `getMorningSleepHandoff, dismissSleepHandoff` to import from `domains/today-plan.js`; (2) added private `renderSleepHandoff()` — looks up `#today-sleep-handoff` and `#today-sleep-handoff-text`; hides section when `getMorningSleepHandoff()` returns null; otherwise sets `textEl.textContent` (safe) and `section.hidden = false`; wires `dismissBtn.onclick` idempotently to call `dismissSleepHandoff()` and hide section; (3) `renderTodayPlan()` now calls `renderSleepHandoff()` after `renderMasthead()`. **`index.html`:** (1) Today pane — added `<div class="today-sleep-handoff" id="today-sleep-handoff" hidden>` block between the masthead and "Placed" section header; contains `div.today-sleep-handoff-eyebrow` "LAST NIGHT LEFT", `p#today-sleep-handoff-text`, `button[type="button"]#today-sleep-handoff-dismiss` "Let it rest"; (2) Sleep modal State A — added `<div class="sleep-shutdown" id="sleep-shutdown" hidden>` block between `#sleep-invitations` and `#sleep-recent`; contains `div.sleep-shutdown-eyebrow` "UNFINISHED FROM TODAY", `div#sleep-shutdown-list` (populated by JS). **`css/modal.css`:** appended EA-176 Sleep Shutdown block — `#sleep-modal .sleep-shutdown[hidden] { display: none }` (specificity guard); `.sleep-shutdown` (border-top/bottom separator-warm, margin-bottom 18px); `.sleep-shutdown-eyebrow` (mono 8.5px/0.18em/uppercase/bronze/0.70 opacity, padding-top 12px); `.sleep-shutdown-item` (border-top separator-warm, padding 11px 0); `.sleep-shutdown-text` (Spectral italic 15px/1.45, oracle-text 0.86 opacity, display block, margin-bottom 8px); `.sleep-shutdown-actions` (flex, gap 20px); `.sleep-shutdown-carry` (bronze, 12.5px, transparent btn); `.sleep-shutdown-pass` (muted-text, 12.5px, transparent btn); `is-carry` modifier (carry: font-weight 500, pass: opacity 0.40); `is-pass` modifier (pass: font-weight 500/oracle-text, carry: opacity 0.40). **`css/today.css`:** appended EA-176 morning handoff block — `#pane-today .today-sleep-handoff[hidden] { display: none }` (specificity guard); `.today-sleep-handoff` (border-top/bottom separator-warm, padding 13px 0, margin-bottom 22px); `.today-sleep-handoff-eyebrow` (mono 9px/0.20em/uppercase/muted-text, margin-bottom 7px); `.today-sleep-handoff-text` (Spectral italic 14.5px/1.5, oracle-text, margin 0 0 10px); `.today-sleep-handoff-dismiss` (transparent btn, DM Sans 12.5px, muted-text). **`sw.js`:** `CACHE_NAME` bumped `elysium-v82` → `elysium-v83`. No new ASSETS entries (no new files created). No new top-level state keys. `STORAGE_KEY` unchanged. Supabase schema unchanged (JSONB `data` column accepts new nested `today.sleepDecisions`/`today.sleepHandoffDismissedFor` fields). IndexedDB untouched. Care task IDs/cycle logic/loggedDays/checks untouched. Chronicle, Temple, Progress, Settings, all domain modals except Sleep unmodified. Sleep closure save path (`saveSleepClosure`, `getSleepEntry`, `reopenSleepClosure`) untouched. §19C Six Tests: user's own words displayed back (felt-not-read + §22.5A); no counts anywhere (no-counter); decisions cleared after normalization/close, handoff dismissed = gone (reversibility); no unkept intentions → section hidden, no parking note → no handoff (silence); carry/pass are single-session utility actions not continuity navigation (non-actionable); no dashboard/score/analytics (productivity-app). |
| EA-171A | Today Threshold Redesign. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`, `elysium-app-concept/Today Home Screen - Concept.html`. Source files inspected: `index.html`, `css/today.css`, `css/base.css`, `css/layout.css`, `js/render/today-plan.js`, `js/domains/today-plan.js`, `main.js`, `js/ui/tabs.js`, `sw.js`. Implements Direction A — The Threshold from the design concept. Visual and structural redesign only; no logic, state, or storage changes. **`index.html`:** replaced `#pane-today` contents — removed the three `.today-plan-block` card wrappers and their mono block eyebrows (`INTENTIONS`, `FROM YESTERDAY`, `OPEN`); added `.today-plan-masthead` block (`.today-plan-eyebrow` `#today-plan-date` populated by JS, `h1.today-plan-title` "Today's *intentions*" with `.ital` Spectral accent, `p.today-plan-sub` "The day has room for what you place here."); added `.today-plan-section-hd` with `span.today-plan-section-label` "Placed" before `#today-plan-intentions`; all intention form elements (`#today-plan-add-wrap`, `#today-plan-add-form`, `#today-plan-input`, `#today-plan-input-confirm`, `#today-plan-input-cancel`, `#today-plan-limit`) now sit directly in the pane (not inside a card wrapper); changed add button text "Add intention" → "Place another"; carryover section now uses `div.today-plan-carryover-echo-label` "Yesterday left" instead of the block eyebrow, inside `#today-plan-carryover`; added `div.today-plan-section-hd.today-plan-open-hd` "Open" section label with `span.today-plan-section-meta` `#today-plan-open-period` (JS-populated period word); `#today-plan-open-list` sits directly in the pane below. All IDs required by main.js event delegation preserved: `today-plan-intentions`, `today-plan-add-wrap`, `today-plan-add-form`, `today-plan-input`, `today-plan-input-confirm`, `today-plan-input-cancel`, `today-plan-carryover`, `today-plan-carryover-list`, `today-plan-open-list`, `today-plan-limit`, `today-plan-add-btn`. **`css/today.css`:** appended EA-171A block — overrides `.today-plan-block` to strip border/radius/padding/background/margin; adds `.today-plan-masthead` (padding 4px 0 26px), `.today-plan-eyebrow` (mono 9.5px/0.22em/uppercase/bronze), `.today-plan-title` (DM Sans 500/28px/−0.02em/oracle-text, margin 0 0 7px), `.today-plan-title .ital` (Spectral italic bronze), `.today-plan-sub` (Spectral italic 14px/1.4/muted-text); section headers `.today-plan-section-hd` (flex, space-between, baseline, border-top separator-warm, padding-top 16px), `.today-plan-section-label` (Spectral italic 17px/oracle-text), `.today-plan-section-meta` (mono 9px/0.18em/uppercase/muted-text); `.today-plan-open-hd` modifier adds `margin-top: 28px`; intention first-child gains `border-top: 1px solid var(--separator-warm)` (overrides prior `border-top: none`); open item first-child same; carryover resets `margin-bottom: 0`; `.today-plan-carryover-echo-label` (mono 9px/0.20em/uppercase/muted-text, margin 20px 0 6px); `.today-plan-carryover-text` overridden to Spectral italic 14.5px/1.45/oracle-text. **`js/render/today-plan.js`:** added `today` to import from `state.js`; added module-level constants `_MASTHEAD_DAYS`, `_MASTHEAD_MONTHS`, `_PERIOD_WORDS`; added `renderMasthead()` — writes `Friday · 29 May` style date to `#today-plan-date` using `today.getDay()` and `today.getMonth()`; added period meta write in `renderOpenItems()` — writes period word ("Morning"/"Midday"/"Evening"/"Night") to `#today-plan-open-period` from `_PERIOD_WORDS[periodKey]`; `renderTodayPlan()` now calls `renderMasthead()` as first step. `renderIntentions()`, `renderCarryover()`, domain logic, state shape, all IDs, and event handlers all unchanged. **`sw.js`:** `CACHE_NAME` bumped `elysium-v80` → `elysium-v81`. No new ASSETS. No state keys. No DEFAULT_DATA change. No migrateState change. No STORAGE_KEY change. No Supabase schema change. No IndexedDB change. Care/cycle/task IDs untouched. Chronicle, Sleep, Temple, Progress, Settings, all domain modals untouched. Constitution §19B compliance: visual redesign only; no scores, percentages, streaks, counts, or summaries added; static sub-line is qualitative and today-only; date eyebrow is the rendered date, not a performance header; period word is orientation only; "Place another" button text is aesthetic copy change. |
| EA-169 | Care Condition + Reaction Notes. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`. Source files inspected: `index.html`, `main.js`, `sw.js`, `js/constants.js`, `js/state.js`, `js/utils.js`, `js/render/today.js`, `js/render/common.js`, `js/render/progress.js`, `js/domains/care.js`, `js/domains/today-plan.js`, `js/ui/tabs.js`, `css/today.css`, `css/temple.css`, `css/modal.css`, `css/base.css`. **`js/constants.js`:** added `care: { records: {} }` to `DEFAULT_DATA` after `today`. **`js/state.js` — `migrateState()`:** added migration block for `state.care.records` at end of function before `return s` — ensures `s.care` is a plain object; ensures `s.care.records` is a plain object; for each date key: drops invalid dates (`!isYmd`), drops non-object/array values; drops condition values not in `{'calmer','same','irritated'}`; trims/caps morningNote and reactionNote to 300 chars, deletes if empty; drops non-string `updatedAt`; deletes entire record if all three payload fields are absent. **`js/domains/care.js`:** added `saveState` to import from `state.js`; added module-level `_VALID_CONDITIONS = new Set(['calmer','same','irritated'])`; added three exports: `getCareRecord(dateStr)` — returns `state.care?.records?.[dateStr] || {}`; `saveCareCheckIn(dateStr, {condition, morningNote})` — validates date, spreads existing record, applies condition (clears when not in valid set, including empty string), trims/caps morningNote (clears when empty), sets updatedAt, writes record back or deletes if no payload fields remain, calls `saveState()`; `saveCareReactionNote(dateStr, note)` — validates date, trims/caps note (clears when empty), writes or prunes record, calls `saveState()`. Both save functions prune the record when all payload fields are absent after the operation — prevents empty-shell records from accumulating. No cycle logic, task ID, loggedDays, or checks touched. **`js/render/common.js`:** added `todayStr` to state import; added `getCareRecord` to care domain import; appended `renderCareCheckIn()` and `renderCareReactionNote()` exports at end of module; added calls to both at end of `renderTodayCycle()`. `renderCareCheckIn()` — toggles `.is-selected` on all `#care-condition-chips .care-checkin-chip` elements based on `rec.condition`, pre-fills `#care-morning-note` textarea value when element is not the active focused element. `renderCareReactionNote()` — same pattern for `#care-reaction-note`. Focus guard prevents overwriting text the user is actively typing. **`index.html`:** (1) Morning section — added `<div class="care-checkin" id="care-morning-checkin">` after the `</details>` for `#care-morning-steps`, before closing `</div>` of `.care-morning-section`; contains: `<p class="care-checkin-eyebrow">SKIN THIS MORNING</p>`, `<div class="care-checkin-chips" id="care-condition-chips">` with three `<button class="care-checkin-chip" data-condition="…">` elements (calmer/same/irritated), and `<textarea class="care-checkin-note" id="care-morning-note" placeholder="Anything to remember." rows="2" autocorrect="off" autocapitalize="sentences" spellcheck="false">`. (2) Night section — added `<div class="care-reaction" id="care-night-reaction">` after the `</details>` for `#care-night-steps`, before `<div class="care-turn-memory">`; contains: `<p class="care-checkin-eyebrow">REACTION NOTE</p>` and `<textarea class="care-checkin-note" id="care-reaction-note" placeholder="Dryness, redness, burning, or anything to remember." rows="2" autocorrect="off" autocapitalize="sentences" spellcheck="false">`. **`css/today.css`:** appended EA-169 block — `#pane-care .care-checkin` (margin-top 14px, padding-top 14px, border-top separator-warm); `#pane-care .care-reaction` (margin-top 2px, padding-top 12px, border-top separator-warm); `.care-checkin-eyebrow` (mono 9px, 0.22em letter-spacing, uppercase, muted-text, margin-bottom 10px); `.care-checkin-chips` (flex, gap 8px, margin-bottom 12px); `.care-checkin-chip` (transparent bg, separator-warm border, 999px radius, 6px 14px padding, DM Sans 13px, muted-text, cursor pointer, 180ms transitions); `.care-checkin-chip.is-selected` (bronze border 0.50 opacity, bronze color, bronze bg 0.06 opacity); dark-mode `.is-selected` override; `.care-checkin-note` (full-width transparent, no border, border-bottom separator-warm, Spectral italic 14px/1.55, transparent bg, oracle-text, no resize/outline, bronze caret, box-sizing border-box); `::placeholder` (muted-text, 0.55 opacity). **`css/modal.css`:** added EA-169 block after `.past-day-mark-btn:active` — `.past-day-care-notes` (margin-top/padding-top 10px, border-top separator-warm, flex column, gap 8px); `.past-day-care-note` (DM Sans 13.5px, oracle-text, line-height 1.4, no margin); `.past-day-care-note-label` (mono 8.5px, 0.14em letter-spacing, uppercase, muted-text, margin-right 6px inline). **`js/render/progress.js`:** added `getCareRecord` to care domain import; updated `renderPastDayBody()` — reads `getCareRecord(dateStr)` after `getChronicleExcerpt`; builds `careNotesHtml` string only when at least one of `condition`/`morningNote`/`reactionNote` exists (empty otherwise); three conditional `<p class="past-day-care-note">` rows, each guarded individually, each with a `<span class="past-day-care-note-label">` eyebrow and `escapeHtml()`-escaped body; inserted `careNotesHtml` between `.past-day-memory-card` and the chronicle excerpt `div`. **`main.js`:** added import `{ getCareRecord, saveCareCheckIn, saveCareReactionNote }` from `./js/domains/care.js`; added three event listeners after the `care-night-action` listener — `#care-condition-chips` click delegation: finds `.care-checkin-chip`, computes `newCondition` (empty string to deselect when tapping same chip again), calls `saveCareCheckIn(todayStr, { condition: newCondition })`, updates `is-selected` on all chips directly without full `renderTodayCycle` re-render; `#care-morning-note` blur: calls `saveCareCheckIn(todayStr, { morningNote: e.target.value })`; `#care-reaction-note` blur: calls `saveCareReactionNote(todayStr, e.target.value)`. **`sw.js`:** `CACHE_NAME` bumped `elysium-v79` → `elysium-v80`. No new files added to `ASSETS`. **Unchanged:** Care cycle logic (`cycleDay`, `advanceCycleIfNeeded`, `lastCycleDate`); Care task IDs (m1–m3, n1–n8, h1–h3) and task text; `loggedDays` meaning and reconciliation; `checks` structure; `STORAGE_KEY`; Supabase schema (JSONB `data` column accepts new `care` key automatically); IndexedDB/photo logic; Chronicle note shape; Sleep note shape; Today Plan state; Temple domain cards; any other domain. **Constitution §19A compliance:** condition is user-selected, not app-inferred; notes are user-authored prose only; display is read-only per-day record already permitted by §19A.2(f); no trends, no scores, no percentages, no pattern observations; no other domain touched; §19A.5 exclusions verified — Water/Body/Mind/Sleep/Light/Chronicle states untouched. |
| EA-168A | Chronicle Search Stabilization. Source files inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `index.html`, `main.js`, `sw.js`, `js/domains/chronicle.js`, `js/render/chronicle.js`, `js/render/chronicle-archive.js`, `js/utils.js`, `js/state.js`, `css/chronicle.css`, `css/base.css`. **Two bugs found and fixed.** **(1) Dead import in `js/render/chronicle-archive.js`:** `getChronicleEntries` was imported alongside `searchChronicleEntries` but never called directly in the module. After EA-168, all list rendering delegates to `searchChronicleEntries(searchQuery)`, which calls `getChronicleEntries` internally when `q` is empty. The explicit import was dead code. Fix: removed `getChronicleEntries` from the import statement — now only `{ searchChronicleEntries }` is imported from `chronicle.js`. **(2) iOS autocorrect regression on `#chronicle-search`:** `type="search"` with `autocomplete="off"` only suppresses browser autofill suggestions; it does not prevent iOS WebKit from applying keyboard autocorrect and sentence-case autocapitalize. In practice this means the first character of every search term is capitalized (breaking lowercase matches), and autocorrect replaces unfamiliar words with dictionary suggestions (breaking literal search). Fix: added `autocorrect="off" autocapitalize="off" spellcheck="false"` to the input element in `index.html`. **Items verified as correct and not changed:** `escapeHtml` is properly exported from `utils.js` (line 54) and correctly imported in `chronicle-archive.js`; `buildSearchExcerpt` index math verified correct with traced examples; `searchChronicleEntries` empty-query case returns all entries via `getChronicleEntries`; `archiveEventsInitialized` guard prevents double event-listener registration; `setChronicleMode('write')` correctly resets `searchQuery` and clears the DOM input; `renderArchiveListIfActive` correctly skips re-render when detail is open; all Chronicle IDs confirmed present in HTML (`#chronicle-search`, `#chronicle-archive-list`, `#chronicle-archive-detail`, `#chronicle-detail-back`, `#chronicle-detail-meta`, `#chronicle-detail-body`, `#chronicle-mode-write`, `#chronicle-mode-read`); SW ASSETS includes `chronicle.css`, `chronicle.js`, `chronicle-archive.js`, `chronicle.js (domain)`; no state/migration/schema changes; Drift/Well/resurfacing untouched. **`sw.js`:** `CACHE_NAME` bumped `elysium-v78` → `elysium-v79`. |
| EA-168 | Chronicle Plain Search. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Source files inspected: `index.html`, `sw.js`, `js/domains/chronicle.js`, `js/render/chronicle-archive.js`, `css/chronicle.css`. **`js/domains/chronicle.js`:** added `searchChronicleEntries(query)` export — trims query, returns `getChronicleEntries()` unchanged when query is empty, otherwise filters to entries whose `note.body.toLowerCase().includes(q)`; inherits reverse-chronological ordering from `getChronicleEntries()`; no fuzzy matching, no NLP, no relevance scoring. **`js/render/chronicle-archive.js`:** added import of `searchChronicleEntries`; added module-level `let searchQuery = ''` (session-only, not persisted); added `SNIPPET_BEFORE = 40` and `SNIPPET_AFTER = 60` constants; added `buildSearchExcerpt(body, query)` helper — finds first match index in body (case-insensitive), extracts a window of 40 chars before + match + 60 chars after, splits into before/match/after slices each individually escaped via `escapeHtml`, wraps match in `<mark class="chronicle-search-match">` (XSS-safe: only the `<mark>` tag is constructed by this code, all user content passes through `escapeHtml`); updated `renderArchiveList()` to call `searchChronicleEntries(searchQuery)` instead of `getChronicleEntries()`, shows "Nothing answered." when query is non-empty and results are empty (vs "Nothing has been left here yet." when no entries exist), uses `buildSearchExcerpt` for snippets when `hasQuery`, falls back to `escapeHtml(archiveExcerpt(note.body))` when empty query; updated `setChronicleMode()` write branch to reset `searchQuery = ''` and clear `#chronicle-search` DOM input value; added `#chronicle-search` `input` event listener in `initChronicleArchiveEvents()` — sets `searchQuery = e.target.value` and calls `renderArchiveList()`. **`index.html`:** added `<input type="search" class="chronicle-search" id="chronicle-search" placeholder="Search the archive" autocomplete="off" />` inside `#chronicle-archive`, directly before `#chronicle-archive-list`; visible only in Read mode (parent `#chronicle-archive` is `hidden` in Write mode). **`css/chronicle.css`:** appended EA-168 block — `.chronicle-search`: `display:block`, `width:100%`, `box-sizing:border-box`, `background:transparent`, `border:none`, `border-bottom: 1px solid var(--separator-warm)`, `padding: 10px 2px 12px`, `margin-bottom: 4px`, `font-family: var(--font-display)`, `font-style: italic`, `font-size: 15px`, `line-height: 1.4`, `color: var(--oracle-text)`, `outline: none`, `-webkit-appearance: none`; `::placeholder`: muted-text, opacity 0.42, italic; `::-webkit-search-cancel-button`: `-webkit-appearance: none` (hides native search clear icon so the input is unadorned); `.chronicle-search-match`: `background: none`, `color: var(--bronze)` (bronze text, no fill, no yellow browser default). **`sw.js`:** `CACHE_NAME` bumped `elysium-v77` → `elysium-v78`. No new state keys. No `DEFAULT_DATA` change. No `migrateState` change. No `STORAGE_KEY` change. No Supabase schema change. No IndexedDB change. No new files (no new ASSETS entry needed). Chronicle note shape (`body`, `updatedAt`, `period`, `cycleDay`) unchanged. Drift, Well, Resurfaced, and writing surface fully preserved. Today Plan, Care/cycle logic, Sleep note shape, all domain modals untouched. Constitution §15.4A compliance: (a) date + excerpt only — no word count, no entry count; (b) reverse chronological; (c) entry detail opens same read-only view from EA-167 (no annotations, no tags); (d) literal substring match only; (e) no observations derived from results; (f) no aggregate quantification of writing practice. §27.2 Six Tests: user retrieves their own words by literal text (felt-not-read — §27.2.6 clarification explicitly permits plain Chronicle search); no counts displayed (no-counter); search state is session-only ephemeral (reversibility); no-match state is one quiet line (silence); user opens entries at will (non-actionable per §15.4A(c)); §27.2.6 clarification explicitly names plain Chronicle search as not failing productivity-app test. |
| EA-167D | First Launch Bottom Spacing Fix: Tab Lifecycle Correction. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `implementation-map.md`. Source files inspected: `index.html`, `main.js`, `js/ui/tabs.js`, `js/sync.js`, `js/render/today-plan.js`, `css/base.css`, `css/layout.css`, `css/today.css`, `css/temple.css`, `css/modal.css`, `sw.js`. **Why the EA-167C rAF `hideBootShell` fix failed:** It (and all three prior attempts) assumed the page was scrolled down on first launch and that resetting scroll would close the gap. It does not, because `document.scrollingElement.scrollTop` is already `0` on first launch — the blank space below the fixed nav is not a scroll offset, it is stale layout geometry. `scrollTo(0, 0)` is a no-op when scrollTop is already 0, so none of the four scroll attempts could ever affect the gap. **First-load vs post-tab-switch difference found:** The *final* DOM and CSS state of `#pane-today` is byte-for-byte identical in both cases — same `tab-pane active` class, same active tab button, same `.main` padding (`padding-bottom: calc(49px + var(--safe-bottom) + 28px)`), same single `.footer-spacer` (height 40px), same `paneEnter` animation. `#pane-today` has no pane-specific `min-height`/bottom-padding override in `css/today.css`. Because the structural state is identical and the post-tab-switch state is *correct*, the candidate structural causes were each ruled out: footer-spacer (every pane has exactly one, Today is not double-padded), safe-area duplication (`.tabs` padding-bottom `var(--safe-bottom)` is content-clearance only; `.main` padding-bottom intentionally clears the fixed nav; neither is wrong post-switch), and the double `min-height:100dvh` layering on `html,body` + `.app` (also present post-switch where layout is correct). **Actual root cause:** A first-paint geometry/reflow timing artifact specific to iOS standalone PWAs (`display:standalone` + `viewport-fit=cover` + `apple-mobile-web-app-status-bar-style: black-translucent`). The Today pane and the `position:fixed` `.tabs` are first laid out *behind* the boot shell while the standalone viewport metrics — `100dvh` resolution, `env(safe-area-inset-bottom)`, and the fixed-element containing block (ICB) — are still provisional. Those metrics finalize only when the app is first revealed (`boot-shell` hidden), but the geometry is never recomputed, so `.app` (`min-height:100dvh`) and `.tabs` (`bottom:0`) retain stale dimensions and leave blank space below the nav. A tab switch corrects it only because `switchTab()`→`swap()` toggles pane `display` (`none`→`block`) which forces a full reflow against the now-settled viewport. On first load, `switchTab` is *equivalent* to the existing manual activation block (no previous pane exists, so `swap()` would run directly with no extra reflow) — so routing initial activation through `switchTab` alone cannot help; the missing ingredient is a reflow *after the app is revealed*. **Fix:** In `js/sync.js`, replaced the failed `requestAnimationFrame(() => window.scrollTo(0, 0))` inside `hideBootShell()` with a one-time synchronous reflow on the reveal frame: `requestAnimationFrame(() => { const app = document.querySelector('.app'); if (!app) return; app.style.minHeight = '99.9dvh'; void app.offsetHeight; app.style.minHeight = ''; void app.offsetHeight; })`. Nudging the dvh container's `min-height` and reading `offsetHeight` forces iOS to re-resolve `dvh`, safe-area insets and the fixed-bar containing block against the settled viewport — the same geometry recompute a tab switch produces — at the single chokepoint every reveal path passes through (`hideBootShell` is called from signed-in sync, offline mode, no-session, password-recovery, session-check error, and the 4 s safety-net). No scroll call, no timeout, no animation replay (the `.active` class is never touched, so `paneEnter` does not re-fire; toggling inline `min-height` does not restart any animation). `main.js` initial activation block (lines 511–519) left unchanged — it already mirrors `swap()` and shows Today content immediately under the boot shell with no flash. `history.scrollRestoration = 'manual'` retained (legitimate, harmless). **`sw.js`:** `CACHE_NAME` bumped `elysium-v76` → `elysium-v77`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB change, no HTML change, no CSS change. Today Plan behavior, Care/cycle logic, Chronicle, Sleep, Temple, all domain modals untouched. Smoke tests: `node --check js/sync.js`, `node --check main.js`, `node --check sw.js` all pass. |
| EA-167C | First Launch Bottom Spacing Root-Cause Fix. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`. Source files inspected: `index.html`, `main.js`, `js/ui/tabs.js`, `js/render/today-plan.js`, `js/sync.js`, `css/layout.css`, `css/base.css`, `css/today.css`, `css/temple.css`, `css/modal.css`, `css/auth.css`, `css/settings.css`, `sw.js`. **Diagnosis:** All prior fixes placed `window.scrollTo(0, 0)` before `hideBootShell()`, inside the same unbroken async task. The iOS WebKit compositor does not guarantee a mid-task `scrollTo` survives into the next paint when no frame has been committed between the call and the DOM change that reveals the app. In `switchTab.swap()` the same `scrollTo` reliably works because the 120ms leave-animation delay gives the compositor multiple committed frames before the new pane appears — scroll is settled before the pane is shown. On first load, `hideBootShell()` reveals the app immediately after the `scrollTo` fires (within the same async microtask chain), giving the compositor no frame boundary to commit scroll=0. Scroll anchoring (or compositor layer snapping) resets the position during the paint triggered by `el.hidden = true`. **Fix:** In `js/sync.js`, added one line inside `hideBootShell()`: `requestAnimationFrame(() => window.scrollTo(0, 0))`. A `requestAnimationFrame` callback runs just before the browser paints the next frame. The sequence is now: (1) `el.hidden = true` (DOM change, not yet painted); (2) rAF queued; (3) browser starts next frame — runs rAF → `scrollTo(0, 0)` → layout/style → paint. The first visible frame of the app without the boot shell is painted at scroll=0. No visual flash (the rAF fires before the paint, not after). Covers all `hideBootShell()` call paths: signed-in sync, offline mode, session-check error, and the 4-second safety-net timeout. The existing `window.scrollTo(0, 0)` in `_postSyncCallback` (`main.js`) is retained — it fires before the rAF and is harmless. **`sw.js`:** `CACHE_NAME` bumped `elysium-v75` → `elysium-v76`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no HTML change, no CSS change. Care/cycle logic, Today Plan, Temple, Chronicle, Sleep, all domain modals untouched. |
| EA-167B | First Launch Bottom Spacing Fix. Source files inspected: `CLAUDE.md`, `docs/ticket-log.md`, `index.html`, `main.js`, `js/ui/tabs.js`, `js/render/today-plan.js`, `css/layout.css`, `css/base.css`, `css/today.css`, `sw.js`. **Root cause:** `#pane-today` had `class="tab-pane active"` baked into the HTML, and the today tab button had `class="tab active"`. Because the pane was already `active` in the HTML, CSS applied `animation: paneEnter 240ms both` immediately at first paint (from state: `opacity: 0; transform: translateY(6px)`). More critically, `switchTab()` activates a pane by calling `classList.add('active')` on an element that was previously `display: none` — this `none → block` transition triggers a full browser layout reflow that correctly anchors iOS PWA `position: fixed` elements. On first load, this reflow never happens because the pane was never `display: none`. The EA-167A fix (`history.scrollRestoration = 'manual'` + synchronous `window.scrollTo(0,0)` at line 86) was insufficient because it addressed scroll position only; the iOS fixed-position layout snapping requires the `display: none → block` reflow cycle, not just a scroll reset. The synchronous `scrollTo` also fired too early in the DOMContentLoaded handler (before all event handlers were wired and before iOS had committed its layout pass). **Fix:** `index.html`: changed `#pane-today` from `class="tab-pane active"` to `class="tab-pane"` (no `active`); changed today tab button from `class="tab active"` to `class="tab"`. `main.js`: removed `renderTodayPlan()` and `window.scrollTo(0, 0)` from the early init block (lines 85-86); added an activation block at the very END of DOMContentLoaded (after all event handlers and the `isClosedForToday()` sleep-modal check) — five lines that mirror `switchTab`'s `swap()` function exactly: `querySelectorAll('.tab').forEach(t => t.classList.remove('active'))`, `querySelector('.tab[data-tab="today"]')?.classList.add('active')`, `window.scrollTo(0, 0)`, `getElementById('pane-today')?.classList.add('active')`, `renderTodayPlan()`. This ensures first-launch activation is identical to every subsequent tab switch: the pane goes from `display: none` → `display: block`, triggering the same layout reflow that fixes iOS `position: fixed` positioning. The `paneEnter` animation now fires on first load (same as tab switching), but the boot shell covers the screen until auth resolves (~async), so users never see the opacity-0 start frame. **Interaction with sleep-closed state:** the explicit `if (isClosedForToday()) openSleepModal()` at line 505 still fires before the activation block, opening the sleep modal. The activation block then runs (always), making Today pane visible behind/around the modal so the user sees normal Today content when they close the modal. **`sw.js`:** `CACHE_NAME` bumped `elysium-v73` → `elysium-v74`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. Today Plan behavior (intentions, carryover, OPEN rows), Care/cycle logic, Chronicle, Sleep, Temple all untouched. |
| EA-167A | Today + Temple UI Stabilization Batch. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`. Source files inspected: `index.html`, `main.js`, `sw.js`, `js/render/today-plan.js`, `js/render/temple.js`, `js/ui/tabs.js`, `js/ui/flow.js`, `css/base.css`, `css/today.css`, `css/temple.css`, `css/auth.css`, `css/layout.css`, `js/domains/today-plan.js`, `js/domains/mind.js`, `js/domains/body.js`, `js/domains/water.js`, `js/domains/sleep.js`. Six fixes implemented: **(1) Issue 1 — First-load blank space:** Root cause — `window.scrollTo(0, 0)` is called inside `switchTab()→swap()` on every tab switch but not on initial load; browser scroll restoration on PWA reload places page at a previous scroll position creating visible blank space below last content item. Fix: added `history.scrollRestoration = 'manual'` at top of DOMContentLoaded (prevents browser from auto-restoring scroll position on reload) and `window.scrollTo(0, 0)` immediately after `renderTodayPlan()` in the initial render block of `main.js`. **(2) Issue 2 — Redundant date header:** `index.html`: removed `<div class="today-plan-header"><p class="today-plan-date" id="today-plan-date"></p></div>` block; changed eyebrow text from `TODAY` to `INTENTIONS`. `js/render/today-plan.js`: removed `MONTHS` constant and `renderDateLabel()` function and its call from `renderTodayPlan()`. `css/today.css`: removed `#pane-today .today-plan-header` and `#pane-today .today-plan-date` CSS rules. **(3) Issue 3 — Time-aware OPEN rows:** `js/render/temple.js`: exported new `getPeriodKey()` function reusing the existing `TEMPLE_PERIOD_BUCKETS` array — returns current period key based on hour. `js/render/today-plan.js`: added imports for `getMorningCareState`, `getPeriodKey` from `./temple.js`; `hasMindArrival` from `../domains/mind.js`; `hasArrivedToday` from `../domains/body.js`; `hasHeldToday` from `../domains/water.js`. Rewrote `renderOpenItems()` with three period branches — `first-light`/`morning`: shows morning Care state (`Care · morning open/in motion/kept`) + Chronicle, no Sleep; `midday`/`afternoon`: shows `Focus · return to focus` (or `Focus · held` when mind+body+water all done today) + Chronicle, no Sleep; `golden-hour`/`dusk`/`night` (default): shows night Care + Chronicle + Sleep as before. `main.js`: added `else if (action === 'work') openWorkFlow();` to the `today-plan-open-list` click handler — Focus row opens Work Flow. **(4) Issue 4 — Today card visual polish:** `css/today.css`: `#pane-today .today-plan-block` gains `border: 1px solid var(--separator-warm); border-radius: var(--radius-lg); padding: 14px 16px; background: var(--bg-elevated); margin-bottom: 16px` — both Intentions and OPEN blocks become contained card surfaces. `today-plan-intention:first-child` border-top changed from `1px solid var(--separator-warm)` to `none` (card top provides containment). `today-plan-empty` border-top/bottom removed and padding reduced from `15px 0` to `8px 0`. `today-plan-open-item:first-child` border-top changed to `none`. Eyebrow margin-bottom increased `10px → 12px`. **(5) Issue 5 — Auth error styling:** `css/auth.css`: `.auth-error` updated — font changed to `var(--font-sans) 13px`, letter-spacing `-0.003em`, color changed from `var(--red)` to `var(--ember)` (warm amber-orange, not harsh iOS red), margin-top `8px → 10px`. `main.js`: `showAuthError()` now remaps "invalid login credentials" (case-insensitive) → `'Email or password did not match.'` before setting element text. **(6) Issue 6 — Temple spacing after flow CTA:** `css/temple.css`: `margin-bottom` increased from `14px` to `24px` on all three flow buttons — `.temple-morning-flow`, `.temple-night-flow`, `.temple-work-flow` (work flow had no prior explicit `margin-bottom`). When CTAs are hidden (`display: none`), margins collapse — no extra space when hidden. `sw.js`: `CACHE_NAME` bumped `elysium-v72` → `elysium-v73`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB change. Care task IDs/text/cycle logic untouched. Chronicle note shape, Sleep note shape, Mind/Body/Water state shapes untouched. All domain modals (Sleep, Mind, Body, Water, Light) untouched. Chronicle Read/Write toggle (EA-167) untouched. Constitution §19B compliance: OPEN rows are navigation shortcuts only; Focus row opens Work Flow which requires user action per step; no count/streak/history display added; morning/midday/evening rows are all qualitative state + navigation. |
| EA-167 | Chronicle Reading View. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/design-system.md`, `design/obsidian-temple/implementation-map.md`. Source files inspected: `index.html`, `main.js`, `sw.js`, `js/constants.js`, `js/state.js`, `js/utils.js`, `js/domains/chronicle.js`, `js/render/chronicle.js`, `js/render/today-plan.js`, `js/ui/tabs.js`, `css/chronicle.css`, `css/base.css`. **New file:** `js/render/chronicle-archive.js` — archive rendering module (~110 lines) exporting `getChronicleMode()`, `setChronicleMode(mode)`, `renderArchiveListIfActive()`, `initChronicleArchiveEvents()`; module-level `chronicleMode` and `archiveEventsInitialized` flag; private helpers `formatArchiveDate(dateStr)` (WED 28 MAY 2026 format), `archiveExcerpt(body)` (first non-empty line, ≤90 chars, trimmed with `…`), `renderArchiveList()`, `openArchiveEntry(dateStr)`, `closeArchiveDetail()`. **`js/domains/chronicle.js`:** added `getChronicleEntries()` export — returns all entries with non-empty bodies sorted reverse-chronologically using existing `isYmd` validation; no new state keys, no note shape change. **`js/render/chronicle.js`:** added import of `getChronicleMode`, `renderArchiveListIfActive`, `initChronicleArchiveEvents` from `./chronicle-archive.js`; added 6-line mode guard at top of `renderChronicle()` — calls `initChronicleArchiveEvents()` on each render (no-op after first call), returns early into archive path when mode is `read`. **`index.html`:** added `.chronicle-mode-toggle` (two buttons `#chronicle-mode-write` / `#chronicle-mode-read`, `type="button"`) after `.chronicle-heading`; wrapped all existing Chronicle content (resurfaced, well, river-hd, drift, today) in `<div id="chronicle-write-surface">`; added `<div id="chronicle-archive" hidden>` containing `#chronicle-archive-list` after write surface; added `<div class="chronicle-archive-detail" id="chronicle-archive-detail" hidden>` with `#chronicle-detail-back` button, `#chronicle-detail-meta`, `#chronicle-detail-body`. **`css/chronicle.css`:** appended EA-167 CSS block — `.chronicle-mode-toggle` (flex, justify-content flex-end, gap 18px, margin-bottom 16px); `.chronicle-mode-btn` (mono 10px, uppercase, letter-spacing 0.18em, no background/border, muted-text at opacity 0.5); `.chronicle-mode-btn.is-active` (bronze, opacity 1); `#chronicle-archive[hidden]` (display none); `.chronicle-archive-empty` (mono 10px, muted 0.6 opacity, 28px margin); `.chronicle-archive-row` (full-width display:block button, separator-warm borders, 14px 2px padding, cursor pointer); `.chronicle-archive-row-date` (mono 9px, bronze, letter-spacing 0.20em); `.chronicle-archive-row-excerpt` (Spectral italic 15px/1.55, oracle-text 0.82, `-webkit-line-clamp: 2`); `.chronicle-archive-detail[hidden]` (display none); `.chronicle-detail-back` (mono 10px, muted 0.65 opacity, margin-bottom 22px); `.chronicle-detail-meta` (mono 9px, bronze); `.chronicle-detail-body` (Spectral italic 16px/1.65, pre-wrap, margin-bottom 32px). **`sw.js`:** `CACHE_NAME` bumped `elysium-v71` → `elysium-v72`; added `'./js/render/chronicle-archive.js'` to ASSETS. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. Chronicle note shape (`body`, `updatedAt`, `period`, `cycleDay`) unchanged. Drift, Well, Resurfaced, and writing surface fully preserved inside `#chronicle-write-surface`. Mode is session-only (not persisted). Today's entry included in Read view per §15.4A. Today Plan Chronicle OPEN row continues to call `switchTab('chronicle')` which calls `renderChronicle()` — unaffected. `main.js` and `tabs.js` unchanged. Constitution §15.4A compliance: date + excerpt only (no word count, no entry count); reverse chronological order; read-only entry opening; no search in this ticket; no observations derived from results; no aggregate quantification. §27.2 Six Tests: user reads their own words in their own order (felt-not-read — §27.2.6 clarification explicitly permits Chronicle archive); no counts anywhere (no-counter); mode is session-only ephemeral (reversibility); empty state is one quiet line (silence); entries are not resurfacing — user deliberately opened Read mode (non-actionable per §15.4A(c)); §27.2.6 clarification explicitly names plain Chronicle archive as not failing productivity-app test. |
| EA-166 | Today Plan. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. New Today tab added as the default landing surface, replacing Temple as the active pane on app open. **New files:** `js/domains/today-plan.js` — domain module exporting `normalizeTodayPlanIfNeeded()`, `getTodayPlan()`, `addTodayIntention(text)`, `toggleTodayIntention(id)`, `deleteIntention(id)`, `getCarryoverCandidates()`, `bringForwardIntention(id)`, `letIntentionPass(id)`; rollover logic runs on each `renderTodayPlan()` call — if stored date ≠ today and gap = 1 day, unkept intentions move to `carryover` with new IDs; gap > 1 drops carryover silently; `js/render/today-plan.js` — render module with `renderTodayPlan()` composing `renderDateLabel()`, `renderIntentions()`, `renderCarryover()`, `renderOpenItems()`; OPEN section derives Care/Chronicle/Sleep status from existing domain helpers with no new state reads. **`js/constants.js`:** added `today: { date: null, intentions: [], carryover: [], carryoverDate: null }` to `DEFAULT_DATA`. **`js/state.js`:** added `state.today` migration block at end of `migrateState()` — validates object shape, sanitizes each intention (id/text/kept required, text trimmed/capped 120 chars, addedAt preserved, optional carriedFrom), caps to 3, sanitizes carryover items, validates carryoverDate. **`js/ui/tabs.js`:** added `renderTodayPlan` import and `if (name === 'today') renderTodayPlan();` to `runRenderFor()`. **`main.js`:** imported all 7 today-plan exports plus `renderTodayPlan`; added `renderTodayPlan()` to init block; added Today Plan event handlers via delegation — intentions block (toggle/delete), carryover block (bring-forward/let-pass), add-btn shows add form, input confirm/cancel, OPEN list clicks (care→switchTab('care'), chronicle→switchTab('chronicle'), sleep→openSleepModal()); added post-sync re-render guard for active `pane-today`; updated `temple-goto-today` click to `switchTab('care')`. **`index.html`:** added `<section class="tab-pane active" id="pane-today">` as first pane (default active); HTML structure: header with `#today-plan-date`, TODAY block with `#today-plan-intentions`, `#today-plan-add-wrap`/`#today-plan-add-form`/`#today-plan-input` (hidden until Add tapped), `#today-plan-limit` (hidden until 3 intentions), FROM YESTERDAY section `#today-plan-carryover` (hidden when empty), OPEN block `#today-plan-open-list`, footer "What remains can be kept or left."; removed `active` from `pane-temple`; renamed former `pane-today` (Care) to `pane-care`; updated nav from 3 tabs (Temple|Today|Settings) to 4 tabs (Today|Care|Temple|Settings) with Today as `active`. **`css/today.css`:** global replace of all `#pane-today` selectors → `#pane-care` (116 occurrences; Care styles now scoped correctly); appended Today Plan CSS block scoped to `#pane-today` covering header, date label (mono uppercase muted), block eyebrow (bronze mono 9px), intention rows with circle mark button/text/release button, `is-kept` muted state, empty state (Spectral italic), add button/form/input/actions, limit line (Spectral italic), carryover section/item/forward+pass buttons, OPEN item rows with chevron, footer (Spectral italic 0.65 opacity). **`sw.js`:** `CACHE_NAME` bumped `elysium-v70` → `elysium-v71`; added `'./js/domains/today-plan.js'` and `'./js/render/today-plan.js'` to `ASSETS`. No new state keys beyond `state.today`; `STORAGE_KEY` unchanged; Supabase schema unchanged (JSONB `data` column accepts new key automatically); IndexedDB untouched; Care task IDs/text/cycle/loggedDays/checks untouched; `advanceCycleIfNeeded` untouched; all existing tabs (Temple/Care/Chronicle/Progress/Cycle/Settings) and modals continue to work. Constitution §19B compliance: 1–3 user-entered intentions only; same-day kept/unkept; 1-day carryover with explicit bring-forward/let-pass choice; no carryover counts; no streak display; no completion %; no history view; no recurring tasks; no priority scoring; no AI suggestions; OPEN section is navigation shortcut only, not a performance display. |
| EA-170A | Constitution Amendment for Practical Usefulness. Documentation-only ticket. No app code, state, storage, schema, UI, or service worker changes. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`. Context reviewed: EA-165 Usefulness Breakthrough Review (not yet in log), EA-164 Daily Flow Integration Hardening, EA-149 Care Practical Exception. Five amendments made to `docs/continuity-constitution.md`: (1) **§19B — The Today Practical Exception** added after §19A.7: permits a dedicated Today surface with 1–3 user-entered intentions, same-day kept/unkept state, optional once-only quiet carryover of yesterday's unkept intentions, and dismiss/let-it-pass behavior; inline open items from existing Care/Chronicle/Sleep state allowed; no historical performance view; streaks/percentages/completion scores/weekly-monthly recaps/productivity language/recurring tasks/calendars/priority scoring/automated suggestions/AI plans/carryover counts/day comparisons all explicitly forbidden; Water/Body/Mind/Light/Sleep tracking grids explicitly excluded from exception scope. (2) **§15.4 amended** to permit plain substring Chronicle search and chronological reading view subject to §15.4A restrictions (date+excerpt only, reverse-chronological, read-only entry opening, literal substring match, no observations derived from results, no aggregate writing-practice quantification); §15.5 amended to permit a chronological archive view subject to §15.4A, naming four permitted Chronicle UIs (Drift, Well, writing surface, archive/search). (3) **§22.5A added** after §22.5: distinguishes app-generated nudges (forbidden) from displaying user-authored content (permitted); covers user-entered Today intentions, yesterday carryover, inline Care/Chronicle/Sleep open items; governing principle stated as "The room may hold what the user placed inside it. The room may not decide what the user should do." (4) **§27.2.6 Productivity-App Test qualified**: rewritten to target "performance, output, optimization, or self-improvement trajectory" framing specifically; clarification note added that a small user-authored Today surface, plain Chronicle archive search, and practical navigation do not fail the test; scores/streaks/analytics/dashboards remain covered. (5) **§27.8 — The Practical Usefulness Phase** added after §27.7: establishes that Elysium is entering a practical usefulness phase; states product priority order (Care → Today Plan → Chronicle → Sleep → Temple atmosphere); confirms atmosphere is now the language surrounding practical daily use rather than the whole product; confirms constitution remains binding and §19B/§15.4A/§22.5A define the exact bounds. `CLAUDE.md` section 8 updated with one-line EA-170A summary row. Next implementation ticket: EA-166 Today Plan. |
| EA-164 | Daily Flow Integration Hardening. Required docs inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `design/obsidian-temple/design-system.md`, and `design/obsidian-temple/implementation-map.md`. Required source inspection covered `index.html`, `main.js`, `sw.js`, `js/constants.js`, `js/utils.js`, `js/state.js`, `js/render/temple.js`, `js/render/today.js`, `js/render/chronicle.js`, `js/render/common.js`, `js/domains/care.js`, `js/domains/light.js`, `js/domains/mind.js`, `js/domains/body.js`, `js/domains/water.js`, `js/domains/chronicle.js`, `js/domains/sleep.js`, `js/ui/modals.js`, `js/ui/flow.js`, `css/base.css`, `css/temple.css`, `css/modal.css`, and `css/today.css`; also smoke-inspected `js/render/progress.js`, `js/render/settings.js`, `js/sync.js`, and `js/services/photos.js` for standalone Progress/Settings/sync/photo boundaries. Bug found: the shared flow modal used a single `_flowActive` boolean for 700ms delayed advances; closing a flow and opening another before the old timeout fired could let the stale timeout show the prior flow's next step in the newly opened flow. Related race found in Work Flow: after Body/Water save completion, the same step's Skip button remained visible during the 700ms transition, allowing a user to advance and then have the delayed transition reset the next step. Fixes in `js/ui/flow.js`: added `_flowSession`, `beginFlowSession()`, and `isCurrentFlowSession()`; each flow open and close now advances the session token; Light, Work Body, and Work Water delayed transitions capture the token and abort if it is stale; Work Body/Water renderers reset skip buttons visible for fresh steps and completion handlers hide skip buttons after a save. `sw.js`: `CACHE_NAME` bumped `elysium-v66` -> `elysium-v67`; existing `ASSETS` list already included all Daily Flow HTML/CSS/JS files including `./js/ui/flow.js`. No new features, no new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB/photo change, no Care task ID/text change, and no cycle logic change. Morning, Work, and Night flows remain navigation composition only; no flow-level tracking, analytics, counts, scores, streaks, focus duration, hydration goal, dashboard, or completion history added. Smoke checks: `node --check` passed for flow/render/domain/state/main/SW modules; dynamic ESM import smoke passed for `js/ui/flow.js`, `js/render/temple.js`, `js/ui/modals.js`, and `main.js` with browser globals stubbed. |
| EA-163 | Work Flow: Return to Focus. `index.html`: added `<button class="temple-work-flow" id="temple-work-flow" hidden>` (eyebrow span "MIDDAY", label span "Return to focus") between `#temple-morning-flow` and `#temple-night-flow` in the Temple featured section. Added 4 Work Flow step divs inside `#flow-modal` after `#flow-step-night-complete` — `#flow-step-work-mind` (eyebrow "WORK FLOW", step-label "Mind", static invitation "Name the thread you are returning to.", `<input type="text" class="flow-chronicle-field" id="flow-work-thread" placeholder="One thread.">`, "Hold this thread" button `#flow-work-mind-hold`, skip button `#flow-work-mind-skip`); `#flow-step-work-body` (eyebrow "WORK FLOW", step-label "Body", dynamic invitation `#flow-work-body-step`, `#flow-work-body-continue`, `#flow-work-body-arrive` hidden by default, skip); `#flow-step-work-water` (eyebrow "WORK FLOW", step-label "Water", dynamic instruction `#flow-work-water-step`, `#flow-work-water-continue`, `#flow-work-water-hold` hidden by default, skip); `#flow-step-work-complete` (eyebrow "WORK FLOW", "The thread is ready.", return button `#flow-work-close`). The Mind step reuses `flow-chronicle-field` CSS class on the text input — no new CSS needed for the input itself. `css/temple.css`: added EA-163 block — `#pane-temple .temple-work-flow` (full-width flex column, `align-items: center`, `gap: 4px`, `padding: 14px 20px`, transparent background, `border: 1px solid rgba(160,122,74,0.18)`, `border-radius: var(--radius-xl)`, `transition: border-color 180ms ease, background 180ms ease`); `.temple-work-flow:active` (bronze 0.50 border, 0.07 bg); `.temple-work-flow-eyebrow` (mono 9px, 0.22em letter-spacing, bronze 0.70 opacity); `.temple-work-flow-label` (Spectral italic 16px, oracle-text 0.80); dark-mode overrides mirroring `.temple-night-flow` exactly. `js/render/temple.js`: added `workFlowBtn` visibility block in `renderTemple()` between morning and night flow toggles — `hidden = (period.key !== 'midday' && period.key !== 'afternoon')`; period buckets never overlap so only one flow CTA is visible at a time. `js/ui/flow.js`: added 3 new imports — `arriveMind` from `domains/mind.js`; `arriveBody, getBodySomaticInvitations` from `domains/body.js`; `holdWater, getWaterResetSteps` from `domains/water.js`. Added `WORK_STEPS = ['work-mind','work-body','work-water','work-complete']`; updated `showStep()` to spread all three step arrays so all 12 step IDs are managed. Added `_workBodyStep`, `_workBodyInvitations`, `_workWaterStep`, `_workWaterSteps` module-level state vars. Added `renderWorkBodyStep()` — calls `getBodySomaticInvitations()`, resets step counter, populates `#flow-work-body-step` with first instruction, hides Continue when only 1 instruction, hides Returned until sequence exhausts. Added `renderWorkWaterStep()` — same pattern for water steps via `getWaterResetSteps()`. Added `openWorkFlow()` export — sets `_flowActive = true`, clears `#flow-work-thread`, calls `showStep('work-mind')`, shows modal. Added `registerWorkFlow()` export wiring 9 listeners: `flow-work-mind-hold` → `arriveMind(todayStr)` + `saveState()` + `queueTempleTrace('mind')` + `renderWorkBodyStep()` + `showStep('work-body')`; `flow-work-mind-skip` → `renderWorkBodyStep()` + `showStep('work-body')`; `flow-work-body-continue` → advance somatic step (increment counter, update text, reveal Returned on exhaustion); `flow-work-body-arrive` → `arriveBody(todayStr)` + `saveState()` + `queueTempleTrace('body')` + show "The body has returned." + 700ms setTimeout guarded by `_flowActive` → `renderWorkWaterStep()` + `showStep('work-water')`; `flow-work-body-skip` → `renderWorkWaterStep()` + `showStep('work-water')`; `flow-work-water-continue` → advance water step; `flow-work-water-hold` → `holdWater(todayStr)` + `queueTempleTrace('water')` + show "The pause has been held." + 700ms guarded → `showStep('work-complete')`; `flow-work-water-skip` → `showStep('work-complete')`; `flow-work-close` → `closeFlowModal()`. Mind thread text is **ephemeral** — `arriveMind()` only is called (marks presence for Day Thread and Temple state), the thread text is discarded; `saveMindReflection()` is intentionally not called because the thread input names a focus target not a reflection inscription and surfacing it as the Temple mind line would be a semantic mismatch. `main.js`: added `openWorkFlow, registerWorkFlow` to flow.js import; added `registerWorkFlow()` call and `#temple-work-flow` click → `openWorkFlow` listener between Morning and Night Flow registration blocks. `sw.js`: `CACHE_NAME` bumped `elysium-v65` → `elysium-v66`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. Care task IDs (m1–m3, n1–n8, h1–h3), advanceCycleIfNeeded, cycleDay, loggedDays, checks, Sleep note shape, Chronicle note shape, Mind state shape, Body state shape, Water state shape all untouched. Standalone Mind/Body/Water/Sleep/Light/Chronicle modals all continue to work. Morning Flow (first-light/morning) and Night Flow (dusk/night) both preserved. Constitution Six Tests passed: guided somatic + attention reset, not a dashboard (felt-not-read); zero step numbers, counts, or totals (no-counter); every step skippable, no flow-level state written (reversibility); CTA hidden outside midday/afternoon, no nudge or reminder (silence); no summary or history generated (non-actionable); no session history, focus score, or hydration count (productivity-app). |
| EA-162 | Night Flow: Close the Day. `index.html`: added `<button class="temple-night-flow" id="temple-night-flow" hidden>` (with `.temple-night-flow-eyebrow` "NIGHT" and `.temple-night-flow-label` "Close the day") immediately after `#temple-morning-flow`; added 4 night flow step divs inside `#flow-modal` after `#flow-step-complete` — `#flow-step-night-care` (eyebrow "NIGHT FLOW", step-label `#flow-night-care-label`, protocol line `#flow-night-care-protocol`, protocol note `#flow-night-care-note`, "Keep tonight" + "Skip"), `#flow-step-night-chronicle` (eyebrow "NIGHT FLOW", step-label "Chronicle", `#flow-night-chronicle-field` rows=3 placeholder "What the day left behind.", "Save" + "Skip"), `#flow-step-night-sleep` (eyebrow "NIGHT FLOW", step-label "Sleep", `#flow-night-sleep-invitation`, `#flow-night-sleep-parking` rows=2 placeholder "What can wait until morning.", "Let the day end" + "Skip"), `#flow-step-night-complete` (eyebrow "NIGHT FLOW", "The day has closed.", Return button `#flow-night-close`). `js/ui/flow.js`: added imports — `state` from state.js; `CARE_PROTOCOL_NOTES` from constants.js; `getSleepClosureInvitations`, `getSleepEntry`, `saveSleepClosure` from domains/sleep.js; `keepNightProtocol` from render/today.js; `getCareTurnState`, `getCareCycleLabel` from render/temple.js. Added module-level constants `MORNING_STEPS = ['light','care','chronicle','complete']` and `NIGHT_STEPS = ['night-care','night-chronicle','night-sleep','night-complete']`. Changed `showStep(name)` to spread both arrays so all 8 step IDs are managed correctly. Renamed `closeMorningFlowModal` → `closeFlowModal` (same behavior: `_flowActive=false`, hide modal, `renderTemple()`); all three call sites updated (`#flow-modal-backdrop`, `#flow-close`, `#flow-night-close`). Added night renderers: `renderNightCareStep()` — reads `getCareTurnState()`; if kept, sets label to "The night has been kept.", hides protocol/note/button, shows "Continue"; otherwise sets label to "Care", populates `#flow-night-care-protocol` with `getCareCycleLabel()` and `#flow-night-care-note` with `CARE_PROTOCOL_NOTES[state.cycleDay]`, shows "Keep tonight" + "Skip". `renderNightChronicleStep()` — pre-fills `#flow-night-chronicle-field` with `getChronicleNote(todayStr).body` if present. `renderNightSleepStep()` — clears `#flow-night-sleep-parking` each open; if Sleep already closed shows "The day has already closed." and hides parking+button; otherwise shows `getSleepClosureInvitations()[0]`, parking textarea, "Let the day end". Added `openNightFlow()` export: sets `_flowActive=true`, calls `renderNightCareStep()`, calls `showStep('night-care')`, shows modal. Added `registerNightFlow()` export wiring four button listeners: Night Care "Keep tonight" → `keepNightProtocol()` + advance; Night Care "Skip" → advance; Night Chronicle "Save" → `upsertChronicleNote()` + `saveState()` only when `body.trim()` non-empty + `renderNightSleepStep()` + advance; Night Chronicle "Skip" → `renderNightSleepStep()` + advance; Night Sleep "Let the day end" → reads parking value, `saveSleepClosure(todayStr, noteText)` + `saveState()` + `queueTempleTrace('sleep')` + advance to complete; Night Sleep "Skip" → advance to complete; `#flow-night-close` → `closeFlowModal()`. Chronicle prefill is overwrite-safe: user sees existing text and saves explicitly; `upsertChronicleNote` preserves original `period`/`cycleDay` coordinates on re-save. Empty parking note valid (falsy guard inside `saveSleepClosure`). `js/render/temple.js`: added CTA visibility toggle in `renderTemple()` — `#temple-night-flow` hidden when `period.key !== 'dusk' && period.key !== 'night'`; `#temple-morning-flow` toggle unchanged; periods never overlap so both CTAs are never simultaneously visible. `main.js`: added `openNightFlow` and `registerNightFlow` to flow.js import; added `registerNightFlow()` call and `#temple-night-flow` click → `openNightFlow` listener after Morning Flow registration block. `css/temple.css`: added Night Flow CTA styles — `#pane-temple .temple-night-flow` (full-width, dashed bronze border 0.28 opacity, `rgba(160,122,74,0.03)` background, `--radius-lg`, `display:flex flex-direction:column align-items:center gap:4px`); `.temple-night-flow:active` (bronze 0.50 border, 0.07 background); `.temple-night-flow-eyebrow` (mono 9px, 0.22em letter-spacing, bronze 0.70 opacity); `.temple-night-flow-label` (Spectral italic 16px, oracle-text 0.80 opacity); dark-mode overrides (`rgba(201,165,107,...)`) — all mirroring `.temple-morning-flow` visual family exactly; added `#flow-modal .flow-night-care-note` (Spectral italic 13.5px/1.50, oracle-text 0.55 opacity, centered, `margin: 0 24px 20px`) — quieter than `flow-care-status` (16px/0.80) since it is supplementary protocol information. `sw.js`: `CACHE_NAME` bumped `elysium-v64` → `elysium-v65`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. Care task IDs (m1–m3, n1–n8, h1–h3) untouched. `advanceCycleIfNeeded`, `cycleDay`, `loggedDays`, `checks`, Sleep note shape, Chronicle note shape all untouched. Standalone Care tab, Chronicle screen, Sleep modal, all domain cards continue to work independently. Morning Flow behavior preserved: `showStep` extension only hides/shows more IDs, logic is the same; `closeFlowModal` is a rename with identical behavior. Constitution Six Tests passed: three-step ritual arc is navigated not observed (felt-not-read); no counts, streaks, step numbers, or scores anywhere in flow (no-counter); flow resets fully on each `openNightFlow()` call, modal dismissible at any step with no state change beyond what was explicitly tapped (reversibility); CTA hidden outside dusk/night, section invisible at all other times (silence); "The day has closed." states atmospheric fact with no tap target (non-actionable); no sleep analytics, no daily score, no productivity dashboard (productivity-app). |
| EA-161 | Morning Flow: Open the Day. `js/ui/flow.js` (NEW FILE, ~130 lines): new ES module exporting `openMorningFlow()` and `registerMorningFlow()`; imports `witnessLight`, `getLastWitness`, `getLightOpeningInvitation` from `domains/light.js`; `getChronicleNote`, `upsertChronicleNote` from `domains/chronicle.js`; `keepMorningProtocol` from `render/today.js`; `getMorningCareState`, `renderTemple`, `queueTempleTrace` from `render/temple.js`; `saveState`, `todayStr` from `state.js`. Module-level vars: `_flowActive` (bool, prevents stale 700ms setTimeout from advancing a closed flow), `_selectedTone` (string|null), `_lightEnteredThisFlow` (bool). Private helpers: `showStep(name)` toggles `hidden` on four step divs (`flow-step-light`, `flow-step-care`, `flow-step-chronicle`, `flow-step-complete`); `renderLightStep()` checks `getLastWitness(todayStr)` — if already entered shows "The morning was already named.", hides Enter and tone chooser, sets button text to "Continue"; otherwise shows invitation, Enter, tone chooser, "Skip"; `renderCareStep()` reads `getMorningCareState()` and writes status text, hides "Open the day" if already kept, shows "Continue" instead of "Skip"; `renderChronicleStep()` pre-fills `#flow-chronicle-field` with existing `getChronicleNote(todayStr).body` if present; `closeMorningFlowModal()` sets `_flowActive = false`, hides modal, calls `renderTemple()`. Tone chooser: event delegation on `#flow-tone-chooser`; selected tone stored in `_selectedTone`; same tone tapped again deselects; `FLOW_TONE_CONFIRMATIONS` object maps Soft/Clear/Steady/Guarded to "A soft/clear/steady/guarded morning.". Light step: on "Entered", calls `witnessLight(todayStr)` + `saveState()` + `queueTempleTrace('light')`; invitation replaced with tone phrase or "The threshold is crossed."; Enter gets `is-still`; Skip hidden; 700ms setTimeout advances to Care step (guarded by `_flowActive` check). Care step: "Open the day" calls `keepMorningProtocol()` then advances to Chronicle. Chronicle step: "Save" calls `upsertChronicleNote()` + `saveState()` only when `body.trim()` is non-empty (prevents empty-body overwrite). Completion: "The morning is open." with Return button. `js/render/temple.js`: `getMorningCareState` changed from private `function` to `export function`; `renderTemple()` gained CTA visibility toggle — `document.getElementById('temple-morning-flow').hidden = (period.key !== 'first-light' && period.key !== 'morning')`. `index.html`: added `<button class="temple-morning-flow" id="temple-morning-flow" hidden>` with `.temple-morning-flow-eyebrow` (MORNING) and `.temple-morning-flow-label` (Open the morning) after `.temple-featured` and before `.temple-domains`; added full flow modal (`id="flow-modal"`, `hidden`) with backdrop, handle, and four step divs before `<!-- Edit task modal -->` comment. `css/temple.css`: appended CTA styles (`#pane-temple .temple-morning-flow` — full-width, dashed bronze border, Spectral italic label 16px, mono eyebrow 9.5px, both light/dark mode variants) and flow modal styles (`.flow-modal-sheet`, `.flow-eyebrow`, `.flow-step-label`, `.flow-invitation`, `.flow-care-status`, `.flow-tone-chooser`, `.flow-tone-btn`, `.flow-tone-btn.is-selected`, `.flow-chronicle-field`, `.flow-complete-line`, flow-scoped `btn-primary` and `btn-primary.is-still`, flow-scoped `btn-text`). `main.js`: added `import { openMorningFlow, registerMorningFlow } from './js/ui/flow.js'`; added `registerMorningFlow()` call and `temple-morning-flow` click → `openMorningFlow` listener in DOMContentLoaded. `sw.js`: `ASSETS` list gains `'./js/ui/flow.js'` (between `modals.js` and `toast.js`); `CACHE_NAME` bumped `elysium-v63` → `elysium-v64`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB change. `witnessLight()`, `state.light.entries`, `keepMorningProtocol()`, `state.chronicle.notes`, `upsertChronicleNote()`, Care/cycle/task IDs, existing domain modals all untouched. CTA hidden outside `first-light`/`morning` periods — Work Flow and Night Flow not implemented. Constitution Six Tests passed: flow is atmospheric threshold entry with qualitative per-step status (felt-not-read); no counts, streaks, or scores anywhere in flow (no-counter); `_flowActive` reset each open, modal can be dismissed mid-flow with no state change except what was explicitly tapped (reversibility); CTA absent outside morning periods (silence); no history browsing, no actionable observation generated (non-actionable); "The morning is open." is not a productivity metric (productivity-app). |
| EA-159 | Secondary Ritual Utility Hardening. Full audit of the EA-152 through EA-158 transformation arc. Files inspected: `CLAUDE.md`, `docs/ticket-log.md`, `docs/continuity-constitution.md`, `index.html`, `sw.js`, `js/constants.js`, `js/utils.js`, `js/state.js`, `js/render/temple.js`, `js/render/today.js`, `js/render/chronicle.js`, `js/render/progress.js`, `js/render/common.js`, `js/domains/care.js`, `js/domains/chronicle.js`, `js/domains/light.js`, `js/domains/water.js`, `js/domains/body.js`, `js/domains/mind.js`, `js/domains/sleep.js`, `js/ui/modals.js`, `css/base.css`, `css/temple.css`, `css/today.css`, `css/modal.css`, `css/progress.css`. Validation findings: Sleep Parking Note — `openSleepModal()` correctly clears `#sleep-parking` on each open; `saveSleepModal()` reads and passes note to existing `saveSleepClosure()`; empty/whitespace notes produce no `entry.note` (falsy guard in `saveSleepClosure`); migration correctly preserves non-empty notes and drops empty-string notes; next-night `sleep-recent` resurfacing path confirmed intact. Body Desk Reset — step counter and invitation array correctly reset on open; Continue handler advances through all steps and exhausts sequence; Returned button only revealed after exhaustion; `arriveBody()` + `saveState()` called exactly once on tap; `closeBodyModal()` fires `applyTempleTrace('body')` when `_bodyArrivedThisSession` is true. Mind Focus Hold — `_mindHoldTimer` cleared on both `closeMindModal()` and `openMindModal()`; `is-waiting` CSS transition (0.8s) confirmed in `modal.css`; `resetMindRitualFields()` restores Inscribe button text and invitation on close/reopen; empty reflection correctly short-circuits `saveState()`/`renderTemple()` but still closes; arrival (from `mind-begin`) already persisted independently. Water Reset Pause — step counter reset on open; all 3-step sets exhausted before Held is revealed; `holdWater()` (which calls `saveState()` internally) called once; `_waterHeldThisSession` triggers trace on close. Light Morning Orientation — `_selectedLightTone` reset to null on each `openLightModal()`; `is-selected` cleared from all tone buttons on open; tone toggle/deselect logic correct; confirmation text written after `renderLightModal()` so it overrides the invitation reset; `is-still` added to `light-witness-btn` after `renderLightModal()` removes it (synchronous override). Temple Utility Signals — sub-label copy confirmed in HTML; Daily Rhythm ordering, Day Thread, Closing Summary, Recent Presence Atmosphere untouched. Completion Outcomes — Body "The body has returned." and Water "The pause has been held." show after action; both action buttons hidden; Close and backdrop remain active; completion text is same-session only (no storage). Constitution compliance — no count/streak displays; `getWaterHoldCount()` and `getBodyArrivalCount()` used only for boolean checks internally, never rendered; Day Thread Water/Body traces are qualitative-only; no forbidden language found. State/persistence — STORAGE_KEY unchanged; no new state keys; no DEFAULT_DATA change; no migrateState change; no Supabase schema change; no IndexedDB change. Service worker — `CACHE_NAME = 'elysium-v63'` correct for EA-158. Event listeners — all modal handlers registered once via their respective `register*Modal()` functions; no duplicate listeners. No bugs found. No code, CSS, HTML, or SW changes required. |
| EA-158 | Utility Completion Outcomes. `js/ui/modals.js`: replaced `body-arrive-btn` click handler's `.is-still`/setTimeout pattern with a quiet completion state — on tap, `body-step-text` changes to `'The body has returned.'` and `body-arrive-btn` is hidden; the existing Close button provides manual exit; `is-still` CSS rule for `#body-modal` is preserved but no longer exercised by this flow. Same pattern for `water-hold-btn`: on tap, `water-step-text` changes to `'The pause has been held.'` and `water-hold-btn` is hidden. `main.js`: `light-witness-btn` click handler now always updates `#light-invitation` — with tone selected: existing `LIGHT_TONE_CONFIRMATIONS` phrase; without tone: `'The threshold is crossed.'`; the `if (_selectedLightTone) {...}` block replaced with a single ternary write to `invEl`. Sleep State B ("The day has ended."), Mind 3-state flow, Chronicle, and Care completion flows were inspected and left unchanged — each already has adequate or structurally sufficient completion acknowledgement. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no CSS change, no HTML change. All existing storage functions (`witnessLight()`, `holdWater()`, `arriveBody()`, `saveMindReflection()`, `saveSleepClosure()`) preserved. Care/cycle logic untouched. Constitution Six Tests passed: qualitative room-centric phrases with no numbers (felt-not-read); no counts/streaks (no-counter); completion text is same-session only, reset on next open (reversibility); section is silent before action fires (silence); no tap targets on completion text (non-actionable); atmospheric phrases are not satisfying as performance metrics (productivity-app). `sw.js`: `CACHE_NAME` bumped `elysium-v62` → `elysium-v63`. |
| EA-157 | Temple Utility Signals. `index.html`: updated `.temple-domain-sub` text for five secondary domain cards — Chronicle `remember` → `write what remains`; Light `open` → `name the morning`; Sleep `close` → `park what remains`; Mind `gather` → `hold one thread`; Water `notice` → `hold · breathe · continue`; Body `jaw · shoulders · breath` unchanged (already practical). These sub-labels are Spectral italic 12px in `--muted-text` color; no CSS change needed — the longest new string ("hold · breathe · continue", 26 chars) fits within the existing layout, confirmed by the prior Body sub at 25 chars. No JS changes — dynamic state labels (Quiet/Gathered/Held/Unstirred/etc.) correctly describe current domain state and were not touched. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. Care/cycle logic untouched. No new Temple sections, no new cards, no counts/streaks/analytics introduced. Constitution Six Tests: not triggered (this is utility copy, not a continuity/memory/resurfacing feature). `sw.js`: `CACHE_NAME` bumped `elysium-v61` → `elysium-v62`. |
| EA-156 | Light Morning Orientation. `index.html`: added `<div class="light-tone-chooser" id="light-tone-chooser">` with 4 `<button class="light-tone-btn" data-tone="…">` elements (Soft, Clear, Steady, Guarded) between `#light-invitation` and `.light-horizon-wrap` in the Light modal. `main.js`: added `_selectedLightTone = null` alongside `_lightWitnessedThisSession`; added `LIGHT_TONE_CONFIRMATIONS` object (4 keys mapping tone → "A soft morning." etc.); updated `openLightModal()` to reset `_selectedLightTone` and clear `is-selected` on all tone buttons; added event delegation on `#light-tone-chooser` — click a tone to select (adds `is-selected`), click same tone to deselect; only one tone selected at a time; updated `light-witness-btn` click handler to apply confirmation phrase to `#light-invitation` after `renderLightModal()` if a tone was selected (post-renderLightModal so it overrides the invitation reset). Tone selection is fully ephemeral: nothing is written to state, `witnessLight()` and `state.light.entries` shape are unchanged. `css/temple.css`: added `.light-tone-chooser` (flex row, gap 8px, margin 0 20px 24px) and `.light-tone-btn` (Spectral italic 13px, transparent background, bronze 0.20-opacity border, 8px radius, opacity 0.65, 15ms transitions) and `.light-tone-btn.is-selected` (border-color 0.55, background rgba(201,165,107,0.07), opacity 0.90) rules. `sw.js`: `CACHE_NAME` bumped `elysium-v60` → `elysium-v61`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. `witnessLight()`, `getLastWitness()`, `state.light.entries` witness array, Temple Light state ("Open"/"Entered"), Day Thread "Light entered." trace, horizon marks, Care/cycle logic all untouched. Constitution Six Tests passed: qualitative choice with no numbers (felt-not-read); tones never tallied (no-counter); ephemeral, nothing stored (reversibility); nothing resurfaces from history (silence); "A clear morning." generates no tasks (non-actionable); ritual tone choice, not planning metric (productivity-app). |
| EA-155 | Water Reset Pause. `js/constants.js`: added `WATER_RESET_STEPS` — 7 sets of 3 short sensory reset instructions each, following a hold → notice → act arc; same pattern as `BODY_SOMATIC_INVITATIONS`. `js/domains/water.js`: added `WATER_RESET_STEPS` to import; added exported `getWaterResetSteps()` — deterministic selection by `day-of-year % 7`, no state, no mutation. `js/ui/modals.js`: added `_waterResetStep = 0` and `_waterResetSteps = []` module-level vars alongside `_waterHeldThisSession`; updated import from water.js — removed `getWaterContactInvitation` (element removed), kept `getWaterRitual`; replaced `openWaterModal()` body — on open: calls `getWaterResetSteps()`, resets step counter to 0, populates `#water-step-text` with first step, shows Continue and hides Held when sequence has multiple steps (edge case: single-item set shows Held immediately), keeps `renderWaterSurfaceMarks()` and `getWaterRitual()` calls; added `water-step-continue` click handler in `registerWaterModal()` — increments `_waterResetStep`, updates `#water-step-text` to next instruction, or (on exhaustion) hides Continue and reveals Held; `holdWater()`, `closeWaterModal()`, `_waterHeldThisSession`, and `applyTempleTrace('water')` all unchanged — closing mid-sequence saves nothing. `index.html`: Water modal — replaced `<p class="water-invitation" id="water-invitation">` with `<p class="water-step-text" id="water-step-text">`; added `<button class="btn-primary" id="water-step-continue">Continue</button>` after `water-ritual` and before `water-hold-btn`; added `hidden` attribute to `#water-hold-btn` so it is concealed until sequence completes. `css/temple.css`: replaced `.water-invitation` CSS rule (16px, opacity 0.80) with `.water-step-text` — Spectral italic 17px/1.55, centered, oracle-text 0.88 opacity, `margin: 0 24px 28px`, `min-height: 54px` (mirrors `.body-step-text` pattern). `sw.js`: `CACHE_NAME` bumped `elysium-v59` → `elysium-v60`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no `STORAGE_KEY` change, no Supabase schema change, no IndexedDB change. `holdWater()`, `state.water.holdings` shape, Day Thread "Water was held." trace, Temple Water state ("Held"/"Unstirred") all unchanged. Care/cycle/task IDs untouched. Constitution Six Tests passed: sensory imperatives with no numbers (felt-not-read); zero counts/streaks (no-counter); sequence is ephemeral per modal open (reversibility); close mid-sequence saves nothing (silence); no tappable history surface (non-actionable); a tracker user would find this slower, not more satisfying (productivity-app). |
| EA-154 | Mind Focus Hold. `js/ui/modals.js`: added `_mindHoldTimer = null` module-level var alongside `_mindArrivedThisSession`; `resetMindRitualFields()` now also resets `#mind-end` button — removes `is-waiting` class and restores text to `'Inscribe'`, and resets `.mind-holding-invitation` text to `'Let the first ten minutes be protected.'`; `openMindModal()` cancels any pending `_mindHoldTimer` before reset so a second open never inherits a previous hold; `closeMindModal()` cancels `_mindHoldTimer` before hiding the modal so closing mid-hold leaves no pending callback; `mind-begin` click handler now adds `is-waiting` to `#mind-end` immediately on entering State B, starts a 60-second `setTimeout` — on fire: removes `is-waiting` from `#mind-end` (CSS transition fades it in at 0.8s ease) and sets `.mind-holding-invitation` text to `'The thread is ready.'`; the previous `setTimeout focus` on `mind-end` removed since the button is not visible during the hold. `css/modal.css`: added `transition: opacity 0.8s ease` to `#mind-modal .mind-end-btn`; added `#mind-modal .mind-end-btn.is-waiting` rule (`opacity: 0; pointer-events: none; cursor: default`) immediately after. `sw.js`: `CACHE_NAME` bumped `elysium-v58` → `elysium-v59`. No HTML changes. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. `saveMindReflection()`, `getMindReflectionEntries()`, `arriveMind()`, `mind.arrivals`, `mind.sessions`, Chronicle Well, Temple mind-line, recent threads, Care/cycle logic all untouched. |
| EA-153 | Body Desk Reset Sequence. `index.html`: Body modal — removed `<p class="body-sub">The fact of being here.</p>` and `<div class="body-somatic-list" id="body-somatic-list"></div>`; added `<p class="body-step-text" id="body-step-text"></p>` between `.body-title` and `.body-ritual`; added `<button class="btn-primary" id="body-step-continue">Continue</button>` before the arrive button; added `hidden` attribute to `#body-arrive-btn` so it is concealed until the sequence completes. `js/ui/modals.js`: removed local `renderBodySomaticInvitations()` function (rendered all somatic items at once into the now-removed `#body-somatic-list`); added `_bodyResetStep = 0` and `_bodyResetInvitations = []` module-level vars alongside `_bodyArrivedThisSession`; replaced `openBodyModal()` — on open: loads `getBodySomaticInvitations()` into `_bodyResetInvitations`, resets `_bodyResetStep` to 0, populates `#body-step-text` with `_bodyResetInvitations[0]`, shows Continue and hides Returned when multiple steps exist (edge case: single-item or empty set shows Returned immediately), preserves `renderBodyHorizonMarks()` and `getBodyRitual()` calls; added `body-step-continue` click handler in `registerBodyModal()` — increments `_bodyResetStep`, updates `#body-step-text` to the next instruction, or (on exhaustion) hides Continue and reveals Returned; `arriveBody(todayStr)` handler, `closeBodyModal()`, `_bodyArrivedThisSession`, `applyTempleTrace('body')`, and `renderBodyHorizonMarks()` all unchanged. `css/temple.css`: removed `.body-sub` rule; removed entire EA-130 block (`.body-somatic-list`, `.body-somatic-item`, `.body-somatic-item:last-child`, `.body-somatic-index`, `.body-somatic-text`); added `#body-modal .body-step-text` — Spectral italic 17px/1.55, centered, oracle-text at 0.88 opacity, `margin: 0 24px 28px`, `min-height: 54px` (prevents layout shift as instructions change). `sw.js`: `CACHE_NAME` bumped `elysium-v57` → `elysium-v58`. No new state keys, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change. `BODY_SOMATIC_INVITATIONS`, `getBodySomaticInvitations()`, `getBodyRitual()`, `arriveBody()`, `state.body.arrivals` shape, `renderBodyHorizonMarks()`, `closeBodyModal()`, Care/cycle logic, Day Thread, Temple rhythm, and EA-146 count compliance all untouched. |
| EA-152 | Sleep Parking Note. `index.html`: added `<textarea class="sleep-parking" id="sleep-parking" rows="2" placeholder="What can wait until morning.">` to Sleep State A, between `#sleep-recent` and `.sleep-handoff`. `js/ui/modals.js`: `openSleepModal()` now clears `#sleep-parking` each time State A is displayed; `saveSleepModal()` reads `parkingEl.value` and passes it to the existing `saveSleepClosure(todayStr, noteText)` instead of the previous hardcoded empty string — empty text is valid and produces no note. `css/modal.css`: added EA-152 block — `.sleep-parking` styled as transparent-background, no-border, Spectral italic 14px/1.55 field with `min-height: 42px`, `resize: none`, `outline: none`, `margin: 0 0 18px`; `::placeholder` at 0.45 opacity muted-text. No new state keys — `sleep.entries[date].note` was already preserved by `migrateState()` (state.js lines 156–158); `saveSleepClosure()` already accepted note text; `getSleepNoteEntries()` and next-night `sleep-recent` resurfacing already worked. No DEFAULT_DATA change, no Supabase schema change, no IndexedDB change, no Care/cycle logic change. `sw.js`: `CACHE_NAME` bumped `elysium-v56` → `elysium-v57`. |
| EA-149 | Care Practical Exception & Progress Boundary. Documentation-only ticket. `docs/continuity-constitution.md`: inserted §19A · The Care Practical Exception into Article IV, immediately after §19 and before Article V. The section formally recognizes that Care's practical completion history (calendar, `loggedDays`, cycle position display, constellation lines, per-day record modal, "correct record" facility, passive stage label) is constitutionally permitted because Care involves active skincare ingredients with a 3-day protocol whose safety depends on knowing whether last night's care was kept — a practical need that atmospheric design alone cannot satisfy. §19A.4 enumerates what remains forbidden even for Care (streaks, percentages, counts, summaries, comparatives, pattern observations, achievement badges). §19A.5 explicitly excludes all other domains (Water, Body, Mind, Sleep, Light, Chronicle, Temple, Day Thread, Closing Summary, Recent Presence Atmosphere) from exception coverage. §19A.6 establishes a burden-of-proof rule for future Care expansion appeals. §19A.7 declares that if the Progress/Stars calendar ever surfaces non-Care data, the exception is violated. Constitutional analysis confirmed the current Stars implementation (progress.js) fits entirely within the exception: the 42-day calendar and constellation are Care-only, the day-record modal is read-only and non-browsable, the stage label is qualitative and life-stage-framed. No app code changed. No state, migration, storage, schema, IndexedDB, service worker, or UI changes. EA-146 count compliance confirmed intact — §19A.5 explicitly reinforces the Water/Body count prohibition. `CLAUDE.md`: EA-149 summary row added to section 8. |
| EA-148 | Chronicle Prompt Intelligence. `js/constants.js`: replaced flat `CHRONICLE_PROMPTS` array (28 timeless strings) with a 5-pool object keyed by period — `morning` (7 strings: orientation/threshold energy), `midday` (7 strings: interruption/mid-motion energy), `evening` (8 strings: gathering/residue energy), `night` (8 strings: closure/release energy), `fallback` (7 timeless strings). `js/render/chronicle.js`: refactored `getChroniclePrompt()` — now maps `getPeriodKey()` to a pool key (`first-light`/`morning` → `morning`; `midday`/`afternoon` → `midday`; `golden-hour`/`dusk` → `evening`; `night` → `night`; else → `fallback`), then selects deterministically from that pool using the same `dayOfYear % pool.length` formula as before — same date + same period = same prompt, no state write, no Math.random(). No HTML, CSS, state, or migration changes. `getChroniclePromptEyebrow()` (period-aware eyebrow, already in place since EA-125) untouched. Chronicle save/autosave/resurfacing/Well/Drift all untouched. Domain files (chronicle.js, care.js, etc.) untouched. Care/cycle logic untouched. Constitution Six Tests passed: prompts are invitations not observations (felt-not-read); zero counts (no-counter); no state written (reversibility); one quiet prompt rendered (silence); reflective fragments only (non-actionable); no tracking or scoring (productivity-app). Prompt selection is period/time-of-day-aware only — no behavioral pattern analysis. `sw.js`: `CACHE_NAME` bumped `elysium-v55` → `elysium-v56`. |
| EA-147 | Architecture Consolidation. `js/utils.js`: added exported `getDayOfYear(date)` using `Math.floor` and `new Date(year, 0, 0)` — the single shared implementation replacing 3 local copies. `js/domains/sleep.js`: removed local `getDayOfYear`; added `getDayOfYear` to utils import. `js/domains/body.js`: removed local `getDayOfYear`; added `getDayOfYear` to utils import. `js/domains/water.js`: removed local `getDayOfYear`; added `getDayOfYear` to utils import; simplified `getWaterContactInvitation()` from 3-line inline calc to single `getDayOfYear(today)` call. `js/domains/light.js`: added `getDayOfYear` import from utils; simplified `getLightOpeningInvitation()` from 3-line inline calc to single `getDayOfYear(now)` call. `js/render/temple.js`: added `ymd` and `getDayOfYear` to utils import; simplified `ymdFromOffset()` — last line now `return ymd(d)` instead of manual template literal; simplified `getDailyLine()` — inline day-of-year calc replaced with `getDayOfYear(now)`. `js/render/chronicle.js`: added `ymd` to utils import; simplified `ymdOffset()` — last line now `return ymd(dt)` instead of manual template literal; simplified `getWellEntry()` anniversary anchor — `anchorStr` construction replaced with `ymd(anchor)`. (Note: the `Math.round` day-of-year calc inside `getWellEntry` offset-rotation is deliberately left as-is — it uses distinct rounding semantics.) `js/ui/modals.js`: removed no-op `export function openMindModalIfActive() {}` stub; replaced manual `yesterdayStr` template literal in `openSleepModal()` with `ymd(yd)` (already imported); replaced manual `todayYmd` template literal in `mindThreadDateLabel()` with `todayStr` (already imported). `main.js`: removed `openMindModalIfActive` from modals.js import; removed `openMindModalIfActive()` call from DOMContentLoaded setup block. `js/state.js`: loosened `water.holdings` migration filter — replaced strict `keys.length !== 1 || keys[0] !== 'at'` check with `Object.keys(e).some(k => k !== 'at' && k !== 'period')` plus period validation against `_VALID_PERIODS`; existing `{ at }` entries pass unchanged; future `{ at, period }` entries now survive migration instead of being silently dropped. `css/base.css`: removed unused `--yellow` (#ffcc00 light / #ffd60a dark) and `--green-soft` (both modes) tokens — confirmed unreferenced across all CSS files. All other iOS-era tokens (`--green`, `--red`, `--pink`, `--pink-soft`, `--orange-soft`, `--indigo`, `--indigo-soft`) retained as they have active CSS references. Deferred: `temple.js` atmosphere extraction (countRecentPresenceDays + TEMPLE_RECENT_PRESENCE_THRESHOLD cross any boundary cleanly; net import complexity neutral); `modals.js` domain split (544 lines, low-priority, defer to future EA); Chronicle Well extraction (return value contract change risk). No new state keys, no DEFAULT_DATA change, no Supabase schema change, no IndexedDB change, no Care/cycle logic change, no ritual copy change, no visible UI behavior change. `sw.js`: `CACHE_NAME` bumped `elysium-v54` → `elysium-v55`. |
| EA-146 | Constitution Compliance: Remove Count Displays. `index.html`: removed the Water/Body modal rhythm status elements so repeated same-day presence no longer has a dedicated rendered line. `js/ui/modals.js`: removed the Water/Body rhythm render helpers, their imports, and their open/save refresh calls; Water holding and Body arrival actions still save normally and still refresh marks/Temple. `js/render/temple.js`: Day Thread now derives Water/Body traces from boolean presence only and renders "Water was held." and "Body returned." regardless of same-day holdings/arrivals volume; `getWaterHoldCount()` and `getBodyArrivalCount()` remain inside domain helpers for internal boolean detection. `css/temple.css`: removed dead rhythm-line styling. `CLAUDE.md` and this log: added EA-146 handoff notes and clarified the old EA-138/EA-141 summaries as superseded where they described removed repeated-contact UI. `sw.js`: `CACHE_NAME` bumped `elysium-v53` → `elysium-v54`. No state shape change, no DEFAULT_DATA change, no migrateState change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB/photo change, no Care/skincare/cycle logic change, and existing Water/Body arrays remain intact. |
| EA-144 | Care Guidance Surface. `js/constants.js`: added `CARE_PROTOCOL_NOTES` export — object keyed 0/1/2 with one compact Spectral-italic guidance line per cycle day: cycleDay 0 (Niacinamide) "Niacinamide controls oil and fades marks. Pea-sized, pressed in. Moisturize after.", cycleDay 1 (Salicylic) "Salicylic acid exfoliates inside pores. T-zone or affected areas only — not all over. Skip if skin feels dry or irritated.", cycleDay 2 (Rest) "Rest night — no active. Skin recovers from the week's treatment. A little more moisturizer is fine." `js/render/common.js`: added `CARE_PROTOCOL_NOTES` to constants import; added `document.getElementById('care-protocol-note')` write at end of `renderTodayCycle()` — sets `textContent` to `CARE_PROTOCOL_NOTES[cycleDay]` so the note updates on every render cycle with no extra function. `index.html`: added `<p class="care-protocol-note" id="care-protocol-note"></p>` inside `.care-night-section`, between `.care-section-hd` and `.care-night-keeper` — one element, always present, text filled by JS. `css/today.css`: added EA-144 block — `.care-protocol-note` (Spectral italic 13.5px/1.55, muted-text, opacity 0.82, margin 0 0 14px, scoped to `#pane-today`). No new state keys, no DEFAULT_DATA change, no migrateState change, no Supabase schema change, no IndexedDB change. Task IDs (m1–m3, n1–n8, h1–h3) untouched. Cycle logic, cycle auto-advance, loggedDays, checks all untouched. Morning/night completion flow untouched. Temple Daily Rhythm, Day Thread, Daily Closing Summary, Mind Reflection threads all unaffected. Existing task-info modal (per-step detail) preserved as-is. `sw.js`: `CACHE_NAME` bumped `elysium-v52` → `elysium-v53`. |
| EA-142 | Recent Presence Atmosphere. (Originally submitted as "Weekly Continuity Surface" — reframed after constitutional analysis: the original observation-based approach failed the felt-not-read, no-counter, and productivity-app tests of §27.2, plus violated §9.1, §9.5, §9.7, §20.4, §21.4, §22.1, §22.2.) `js/render/temple.js`: added three EA-142 constants — `TEMPLE_RECENT_BOOST_MAX = 0.0100` (max additional gradient alpha lift from recent activity), `TEMPLE_RECENT_PRESENCE_THRESHOLD = 3` (minimum recent presence days to activate daily-line variant), `RECENT_PRESENCE_LINES` array (4 atmospheric phrases: "The room has been kept.", "Something has remained.", "The room has not gone cold.", "Traces remain."); added `countRecentPresenceDays()` — calls existing `collectTemplePresenceDays(todayDayNum)` and filters for days `> todayDayNum - 7 && < todayDayNum` (the 7 completed days before today, excluding today-in-progress); modified `getTempleGradientAlpha()` to add `recentLift = (countRecentPresenceDays() / 7) * TEMPLE_RECENT_BOOST_MAX` on top of the existing long-term warmth lift — 0 recent days → 0 boost, 3/7 → ~0.0043, 7/7 → 0.010; the boost decays naturally as each presence day falls outside the 7-day window (no separate decay mechanism needed); modified `getDailyLine()` to insert new branch after "Light was seen." and before `isQuietStretch()` — fires when `countRecentPresenceDays() >= 3` and no today-specific signal has already fired, returns a day-of-year-rotating phrase from `RECENT_PRESENCE_LINES`; the phrase is environmental/room-centric, contains no counts, no "this week" label, and no behavioral observation about the user. No HTML changes, no CSS changes, no new state keys, no `DEFAULT_DATA` or `migrateState` changes, no Supabase schema change, no IndexedDB change. Care/cycle logic untouched. All prior EA-137/138/139/140/141 behaviors preserved: rhythm ordering, ritual utilities, Mind reflection threads, Closing Summary, Day Thread all unaffected. All Six Tests passed: felt via gradient opacity and non-informational phrase (felt-not-read); no counts or ratios (no-counter); fades to 0 when last 7 days go quiet (reversibility); cold start produces no phrase and no boost (silence); no tap targets (non-actionable); room-centric atmospheric phrase is not satisfying as performance feedback (productivity-app). `sw.js`: `CACHE_NAME` bumped `elysium-v51` → `elysium-v52`. |
| EA-141 | Day Thread. `index.html`: added `<div class="temple-day-thread" id="temple-day-thread" hidden>` with inner `.temple-day-thread-sep` and `#temple-day-thread-lines` container, placed between `.temple-domains` (closing `</div>`) and `#temple-closing`. `js/render/temple.js`: added private `deriveTodayTraces()` — derives up to 8 completed ritual traces from today's state in domain-time order: `getLastWitness(todayStr)` → "Light entered."; `getMorningCareState() === 'kept'` → "Morning care kept."; Water/Body presence traces (made qualitative by EA-146); `hasMindArrival || session?.date === todayStr` → "A thread was gathered." or "A thread was left." (if `getMostRecentReflectionSession()?.date === todayStr`); `state.chronicle.notes[todayStr].body` → "A line was left."; `getCareTurnState().key === 'kept'` → "Night care kept."; `getSleepEntry(todayStr)` → "The day closed."; returns traces as plain string array; added private `renderDayThread()` — hides element when `deriveTodayTraces()` returns empty array, otherwise populates `#temple-day-thread-lines` with `<p class="temple-day-thread-line">` elements; added `renderDayThread()` call before `renderTempleClosing()` in `renderTemple()`; modified `renderTempleClosing()` to remove the four "kept" lines ("Light was entered.", "The thread was gathered.", "Body returned.", "Water was held.") — these are now covered by Day Thread, leaving Closing Summary focused exclusively on open evening items (Care, Chronicle, Sleep). `css/temple.css`: added EA-141 block — `.temple-day-thread` (margin-bottom 14px), `.temple-day-thread-sep` (1px warm gradient, margin 4px 0 12px, opacity 0.60), `.temple-day-thread-lines` (flex column, gap 6px), `.temple-day-thread-line` (Spectral italic 14px/1.45, muted-text, 0.72 opacity, no margin). No new state keys, no DEFAULT_DATA change, no migrateState change, no Care/cycle logic change, no Supabase schema, no IndexedDB change. EA-137 rhythm ordering, EA-138 utilities, EA-139 Mind reflection, EA-140 Closing Summary (open-items-only behavior preserved, kept-items cleanly moved to Day Thread) all intact. `sw.js`: `CACHE_NAME` bumped `elysium-v50` → `elysium-v51`. |
| EA-140 | Daily Closing Summary. `index.html`: added `<div class="temple-closing" id="temple-closing" hidden>` with inner `.temple-closing-sep` and `#temple-closing-lines` container, placed between `.temple-domains` and `.temple-secondary-nav`. `js/render/temple.js`: added private `renderTempleClosing(periodKey)` — shows only during `dusk` or `night` when Sleep is not yet closed; collects up to 7 phrase-lines from existing domain state (Care: "Care still waits." / "Care is in motion." / silent if kept; Chronicle: "Chronicle is still open." if unwritten; Sleep: "Sleep has not closed." always when section is visible; Light/Mind/Body/Water: kept-trace phrases when present); each phrase is a `<p>` with `.temple-closing-line--open` (oracle-text 0.80) or `.temple-closing-line--kept` (muted-text 0.65); hides section and returns immediately if period is not dusk/night or Sleep is already closed; section re-renders on every `renderTemple()` call so transitions (sleep close, period change) are picked up immediately. `renderTemple()`: calls `renderTempleClosing(period.key)` at its final line. `css/temple.css`: added EA-140 block — `.temple-closing` (margin-bottom 16px), `.temple-closing-sep` (1px warm gradient separator, margin 4px 0 14px, opacity 0.70), `.temple-closing-lines` (flex column, gap 7px), `.temple-closing-line` (Spectral italic 14px/1.45, no margin), `.temple-closing-line--open` (oracle-text 0.80), `.temple-closing-line--kept` (muted-text 0.65). No new state keys, no DEFAULT_DATA change, no migrateState change, no Care/cycle logic change, no Supabase/IndexedDB change, no new imports. EA-137 rhythm ordering fully preserved. EA-139 Mind reflection saving/threads unaffected. All Six Tests of the Continuity Constitution passed: room-state qualitative phrases; no counts/comparatives; section hides at dawn and when Sleep closes; silence when sleep is closed; read-only for EA-140; not satisfying to a productivity-app user. `sw.js`: `CACHE_NAME` bumped `elysium-v49` → `elysium-v50`. |
| EA-139 | Mind Reflection Readability. `js/domains/mind.js`: added `uid` to utils import; added exported `saveMindReflection(dateStr, body)` — pushes a completed session (`durationMinutes: 1`, `completed: true`) to `state.mind.sessions` when reflection is non-empty; uses existing `uid()`, `isYmd()`, `getPeriodKey()` — no new imports beyond `uid`. `js/ui/modals.js`: added `saveMindReflection` and `getMindReflectionEntries` to mind.js import; added private `mindThreadDateLabel(dateStr)` (computes relative label: "Today", "Yesterday", "Two days ago", "Three days ago", "Earlier this week", "Last week", "Some time ago"); added private `renderMindThreads()` (reads up to 3 most-recent entries from `getMindReflectionEntries()`, sorted newest-first, builds `.mind-threads-label` + `.mind-thread` elements into `#mind-threads`, hides element when empty); added `renderMindThreads()` call to `openMindModal()` so threads refresh each open; modified `mind-complete` handler to read `#mind-reflection` value, call `saveMindReflection()`, `saveState()`, and `renderTemple()` before closing — so the Temple mind-line updates immediately and future Chronicle Well picks up the inscription. `index.html`: added `<div class="mind-threads" id="mind-threads" hidden></div>` after the `#mind-begin` button in State A. `css/modal.css`: changed `#mind-modal .mind-modal-sheet` from `overflow: hidden` → `overflow-y: auto` so threads section is scrollable when content exceeds sheet height; added EA-139 block — `.mind-threads` (border-top separator, 24px top margin, 16px top padding), `.mind-threads-label` (mono 9px, 0.55 opacity, 14px bottom margin), `.mind-thread` (separator-warm bottom border, 14px gap, last-child no border), `.mind-thread-meta` (mono 9.5px, muted, 0.55 opacity), `.mind-thread-body` (Spectral italic 13px/1.6, oracle-text 0.72 opacity, -webkit-line-clamp 2). Historical `state.mind.sessions` preserved; existing `getMindReflectionEntries()` used in Chronicle Well unchanged. No new state keys, no DEFAULT_DATA change, no migrateState change, no Care logic, no Supabase schema, no IndexedDB change. `sw.js`: `CACHE_NAME` bumped `elysium-v48` → `elysium-v49`. |
| EA-138 | Ritual Utility Refinement Batch. `js/domains/sleep.js`: `getSleepClosureInvitations()` now accepts an optional `Date` argument for deterministic same-date/different-date verification while preserving the existing 4-invitation day-of-year rotation and no state writes. `js/domains/body.js` and `js/domains/water.js`: added derived same-day count helpers (`getBodyArrivalCount`, `getWaterHoldCount`) from existing `arrivals`/`holdings` arrays; no new storage shape. `index.html`: clarified Body's Temple subtext to `jaw · shoulders · breath`; added modal-only repeated-contact rhythm lines, later removed by EA-146. `js/ui/modals.js`: rendered those rhythm lines from existing same-day arrays until EA-146 removed the visible repetition display. `js/render/temple.js`: Body state copy now reads `Return`/`Returned`; Water kept state now reads `Held`. `css/temple.css`: added quiet mono styling for the modal rhythm lines, later removed by EA-146. Cleanup inspection confirmed EA-131 already removed `SLEEP_RITUALS`, `getSleepRitual()`, `MIND_RITUALS`, `getMindRitual()`, the dead Mind timer/session functions, and the duplicate Water ritual string. No new state, migration, Care logic, skincare logic, Supabase schema, or IndexedDB changes. `sw.js`: `CACHE_NAME` bumped `elysium-v47` → `elysium-v48`. |
| EA-137 | Temple Daily Rhythm. `index.html`: added `id="temple-domain-label"` to the `.temple-domain-label` span in the domain section head. `js/render/temple.js`: added `getMorningTasks` to care.js import; added `TEMPLE_RHYTHM_HEADINGS` constant (7 period-keyed heading strings: "The morning approaches" / "This morning" / "The day is moving" / "What the afternoon holds" / "As the day turns" / "What remains tonight" / "For the night"); added `RHYTHM_CARD_IDS` constant mapping domain names to card element IDs; added `getMorningCareState()` (derives open/motion/kept from `getMorningTasks()` + `getTodayChecks()`, no state write); added `getRhythmMap(periodKey)` (returns per-domain `now`/`kept`/`later` values derived from existing domain state — `now` helper returns `kept` if done else `now`, `later` helper returns `kept` if done else `later`; period-to-domain mapping: first-light → light:now; morning → light+mind:now; midday/afternoon → water+body+mind:now; golden-hour → chronicle+water+body:now; dusk → chronicle+sleep:now; night/default → sleep+chronicle:now); updated `renderTemple()` to (a) render period-aware hero copy for first-light/morning (Morning kept./Morning in motion./Morning awaits.) and night-protocol copy for all other periods, (b) write `TEMPLE_RHYTHM_HEADINGS` text to `#temple-domain-label`, (c) apply `data-rhythm` attribute to each domain card. `css/temple.css`: added EA-137 block — `[data-rhythm="now"]` gets `order: 0` and `border-color: rgba(160, 122, 74, 0.38)` (light mode), `[data-rhythm="kept"]` gets `order: 1`, `[data-rhythm="later"]` gets `order: 2`; dark-mode override for `now` border-color `rgba(201, 165, 107, 0.30)`. No new state, no migration, Care cycle logic untouched. `sw.js`: `CACHE_NAME` bumped `elysium-v46` → `elysium-v47`. |
| EA-136 | Sleep Closure Rotation. `js/constants.js`: expanded `SLEEP_CLOSURE_INVITATIONS` from 5 → 16 entries, adding: "Return the day to itself.", "Leave tomorrow outside the door.", "Let the last thought pass without holding it.", "Put the weight down for the night.", "Let silence take the edge off the room.", "Close what does not need your hands.", "Let the body know it is no longer required.", "Allow the unfinished to remain unfinished.", "The day does not need to be solved now.", "Let one concern become distant.", "Let the room darken before the mind does." `js/domains/sleep.js`: added private `getDayOfYear()` (mirrors body.js pattern); rewrote `getSleepClosureInvitations()` to select 4 items deterministically — `offset = dayOfYear % pool.length`, returns 4 consecutive items wrapping around. Same date = same 4 items; next day = offset shifts by 1. No new state, no migration, no HTML/CSS/modals.js changes. `sw.js`: `CACHE_NAME` bumped `elysium-v45` → `elysium-v46`. |
| EA-135 | Temple Morning Orientation Line. `js/render/temple.js`: added `TEMPLE_PERIOD_ORIENTATION` constant — a 7-key object mapping each period key (`first-light`, `morning`, `midday`, `afternoon`, `golden-hour`, `dusk`, `night`) to a quiet ambient phrase. Modified `getDailyLine()` to accept `periodKey` parameter; replaced `return ''` at the end of the function with `return TEMPLE_PERIOD_ORIENTATION[periodKey] ?? ''`. Updated the sole call site in `renderTemple()` from `getDailyLine()` to `getDailyLine(period.key)` — `period.key` is already computed immediately above. All existing earned-state checks (sleep closed, night passed, care kept/motion, chronicle note, mind arrival, light witness, quiet stretch) remain unchanged and return first; the fallback only fires when every earned condition misses. No new DOM elements, no new CSS, no new state keys, no migration changes, no storage writes. `sw.js`: `CACHE_NAME` bumped `elysium-v44` → `elysium-v45`. |
| EA-133 | Light Opening Ritual. `js/constants.js`: added `LIGHT_OPENING_INVITATIONS` (7 restrained threshold strings: "Let the room become visible.", "Stand where the morning can reach you.", "Open one window, even slightly.", "Let the first light touch the day.", "Name the day before entering it.", "Begin without rushing.", "Notice what arrived before you did."). `js/domains/light.js`: added import of `LIGHT_OPENING_INVITATIONS`; added exported `getLightOpeningInvitation()` — deterministic day-of-year selection, no state, no mutation. `index.html`: Temple Light card `.temple-domain-sub` changed "witness" → "open"; `.temple-domain-state` initial value changed "Unseen" → "Open"; Light modal rebuilt — removed `.light-sky`, `.light-sun`, `.light-cursor`, `.light-cursor-dot`, `.light-cursor-time` elements, removed dynamic `id="light-eyebrow"`; added static eyebrow `LIGHT / OPENING`, `<p class="light-invitation" id="light-invitation">`, `.light-horizon-wrap` with `.light-threshold-line` and `#light-marks`, button text changed "I'm here" → "Entered". `css/temple.css`: replaced EA-47 Light modal CSS section entirely — removed sky/sun/cursor rules; added `.light-modal-sheet` background (top-radial warm glow, airy, lighter than Body's earthier bottom-glow), `.light-modal-eyebrow` (mono 9.5px, centered, bronze, 22px top margin), `.light-invitation` (Spectral italic 16px, centered, oracle-text 0.80 opacity), `.light-horizon-wrap` (36px tall, overflow hidden), `.light-threshold-line` (1px bronze gradient line at 50%), `.light-marks` (inset-0 positioning parent), `.light-witness-mark` and `.light-witness-mark.is-linger`. `js/render/temple.js`: removed `getLightPeriodLabel` from import; changed temple-light-state from period label/"Unseen" to "Entered"/"Open". `main.js`: added `getLightOpeningInvitation` to light.js import; removed `LIGHT_PERIODS` array and `getCurrentLightPeriod()` function; simplified `renderLightModal()` (populates invitation, renders yesterday-linger + today marks in `#light-marks`, resets `is-still`); simplified click handler to Body/Water pattern (no setTimeout chain, no closeBtn visibility manipulation). `sw.js`: `CACHE_NAME` bumped `elysium-v43` → `elysium-v44`. No state shape changes, no new state keys, no mergeDefaults or migrateState changes — existing `state.light.entries[date].witnesses[]` unchanged. |
| EA-131 | Ritual Domain Review & Convergence. Dead-code pass following EA-122/EA-123/EA-127/EA-128 modal rebuilds. `js/constants.js`: removed `SLEEP_RITUALS` (28 strings, orphaned when EA-127 rebuilt Sleep modal without the ritual inscription field) and `MIND_RITUALS` (28 strings, orphaned when EA-128 rebuilt Mind modal without the ritual inscription field); removed the duplicate WATER_RITUALS entry `'Let your eyes go soft. Nothing needs to be sharp right now.'` which was verbatim identical to BODY_RITUALS entry 19. `js/domains/sleep.js`: removed `getSleepRitual()` function and `SLEEP_RITUALS` from import — function was exported but never called after EA-127. `js/domains/mind.js`: removed `getMindRitual()` function and `MIND_RITUALS` import; removed `getTodaySession()`, `getSessionHeldMs()`, `formatHeldMs()`, `beginMindSession()`, `endMindSession()` — all dead since EA-128 replaced the timer-based session model; removed now-unused `uid` import. Retained: `getMostRecentSession()` and `getMostRecentReflectionSession()` (used in temple.js), `getMindReflectionEntries()` (used in chronicle.js Well for historical session data), `getMindArrival()`, `hasMindArrival()`, `arriveMind()`. No HTML, CSS, state-shape, or interaction changes. `sw.js`: `CACHE_NAME` bumped `elysium-v42` → `elysium-v43`. |
| EA-130 | Body Return Ritual. `js/constants.js`: added `BODY_SOMATIC_INVITATIONS` — array of 7 sets (each 3–5 short imperative somatic phrases: jaw, shoulders, feet, breath, tension-noticing, return). `js/domains/body.js`: added import of `BODY_SOMATIC_INVITATIONS`; added exported `getBodySomaticInvitations()` — selects set by `day-of-year % 7`, no state, no mutation. `index.html`: added `<div class="body-somatic-list" id="body-somatic-list"></div>` between `.body-sub` and `.body-ritual` in the Body modal. `css/temple.css`: added `.body-somatic-list` (flex column, separator-warm border-top, margin 0 0 18px), `.body-somatic-item` (grid 28px/1fr, 10px gap, separator-warm border-bottom, padding 10px 0), `.body-somatic-item:last-child` (no border), `.body-somatic-index` (mono 9px, bronze, 0.72 opacity), `.body-somatic-text` (sans 15px, line-height 1.4, oracle-text). `js/ui/modals.js`: added `getBodySomaticInvitations` to import; added `renderBodySomaticInvitations()` function (replaceChildren, builds index+text rows); `openBodyModal()` now calls `renderBodySomaticInvitations()` before `renderBodyHorizonMarks()`. No state shape changes, no migration, no new CSS file, no new JS module. `sw.js`: `CACHE_NAME` bumped `elysium-v41` → `elysium-v42`. |
| EA-129 | Water Contact Ritual. `js/constants.js`: added `WATER_CONTACT_INVITATIONS` array (5 short sensory imperatives: "Notice the temperature before drinking.", "Hold the vessel for one breath.", "Drink slowly enough to feel it.", "Let the room come back into focus.", "Feel the weight of what you are holding."). `js/domains/water.js`: added import of `WATER_CONTACT_INVITATIONS`; added exported `getWaterContactInvitation()` — selects by `day-of-year % 5`, no state, no mutation, one invitation per calendar day. `index.html`: replaced static `<p class="water-sub">A pause at the surface.</p>` with `<p class="water-invitation" id="water-invitation"></p>` in the Water modal. `css/temple.css`: replaced `.water-sub` rules with `.water-invitation` block (font-family: display, italic, 16px, line-height 1.55, center-aligned, oracle-text at 0.80 opacity, margin 0 24px 28px); increased `water-surface-wrap` bottom margin 28px → 32px. `js/ui/modals.js`: imported `getWaterContactInvitation`; `openWaterModal()` now populates `#water-invitation` at open time alongside the existing `#water-ritual`. No state shape changes, no migration changes, no new CSS file, no new JS module. `sw.js`: `CACHE_NAME` bumped `elysium-v40` → `elysium-v41`. |
| EA-128 | Mind Gathering Ritual. `index.html`: replaced Mind's duration-first modal opening with three ritual states: Gathering ("Name the one thing."), Holding ("Close the unnecessary doors." / "Let the first ten minutes be protected."), and Inscribing ("Write the thread before entering."); Temple Mind subtitle changed from `hold` to `gather`. `js/constants.js`: added `mind.arrivals` to `DEFAULT_DATA` while preserving legacy `mind.sessions`. `js/domains/mind.js`: added `arriveMind()`, `getMindArrival()`, and `hasMindArrival()` for a simple per-day `{ at, period }` arrival mark. `js/state.js`: migration now validates `mind.arrivals`, supports array-shaped draft marks defensively, and collapses to one mark per date without changing legacy session/reflection migration. `js/sync.js`: Supabase merge now carries a single local-preferred `mind.arrivals` mark per date alongside legacy sessions. `js/ui/modals.js`: Mind no longer creates or completes timed sessions from the modal; typed gathering/inscribing content is cleared on open/close and never persisted; completing the ritual records only the arrival mark and applies the existing Temple trace. `js/render/temple.js`: Mind arrivals feed Temple warmth/presence derivation, daily line, and today's Mind state label (`Gathered`) without adding counts or analytics. `css/modal.css`: added scoped ritual-stage and ephemeral-field styling; no timer/progress surface is used by the new modal. `sw.js`: `CACHE_NAME` bumped `elysium-v39` -> `elysium-v40`. No Care tracking logic, task IDs/text, cycle logic, milestone thresholds, Supabase schema, IndexedDB, productivity score, streak, analytics, goal, performance language, or timer-first UX added. Smoke tests: `node --check` for touched JS modules and a module smoke test for default merge, migration sanitization, invalid arrival removal, collapse-to-one behavior, and arrival creation shape. |
| EA-1 | Product and technical blueprint. Analysis only — no code changes. Confirmed evolutionary refactor direction. |
| EA-2 | Identity pass. App renamed to Elysium in manifest, title, auth screen, footer, and service worker cache name. |
| EA-3 | Elysium design tokens added to `styles.css` (both light and dark) without any visual redesign. |
| EA-4 | Restrained copy pass. "Routine" language lightly shifted to "ritual" across visible UI text and notifications. |
| EA-5 | Temple tab added as read-only home summary. Made default active tab. Renders ring, status, cycle name, streak, and quick-action rows. |
| EA-6 | Chronicle tab added between Cycle and Progress. One note per day stored in `chronicle.notes` via existing state/save/sync flow. |
| EA-8 | Care module language introduced. Four copy-only changes: Today tab hero label, Temple "Care cycle" quick action, Settings "Care ritual" section heading, and morning push notification title. |
| EA-9 | Renamed project identity across GitHub and Vercel to elysium-app; local origin remote updated and external tooling verified. |
| EA-10 | Temple visual polish. Six `#pane-temple`-scoped CSS rules: larger hero card (padding, radius, shadow, gap), 100px ring, `--divine-accent` ring stroke, `--bronze` eyebrow label and chevrons, `--oracle-text` quick-action names. No HTML or JS changes. |
| EA-11 | Chronicle visual polish. CSS-only, Chronicle-scoped: upgraded card (16px radius, `--ritual-surface`, `--shadow-md`, separator border), focus-within ring via `--divine-accent`, textarea breathing room (padding, line-height, `--oracle-text`), placeholder via `--muted-text`, status margin, full-width `--divine-accent` save button. No HTML or JS changes. |
| EA-13 | Obsidian Temple foundation tokens. CSS-only, additive-only: 21 new custom properties added to `:root` and dark-mode block — obsidian depth layers, alabaster surface, bronze-soft fill, ink text material, warm separator, temple gradient, bronze glow, card shadow, radius scale (sm–2xl), spacing scale (xs–xl). No existing variables changed, no selectors changed, no HTML or JS changes. |
| EA-14 | Stacked Medallion layout for Temple tab. CSS-only, appended `#pane-temple`-scoped EA-14 block: hero card converted to column/centered layout, `--alabaster` surface in light mode / `--temple-gradient` in dark, `--shadow-card` + `--shadow-bronze`, `--separator-warm` card border and ring track, 120px ring, `--bronze` ring stroke, centered hero text, quick-action card radius/shadow/separator upgraded. No HTML or JS changes. |
| EA-15 | Bottom tab bar active state bronze. CSS-only, one-property change: `.tab.active` color swapped from `var(--tint)` (iOS system blue) to `var(--bronze)` (warm metallic). No layout, icon, animation, HTML, or JS changes. |
| EA-16 | Today/Care Obsidian Temple polish. CSS-only, `#pane-today`-scoped block appended: hero card `--alabaster` surface (light mode) + `--separator-warm` border + `--shadow-card`; dark-mode hero card override restores `--bg-elevated`; ring track `--separator-warm`; hero label `--bronze`; task list cards `--radius-lg` + `--shadow-card`. No HTML or JS changes. |
| EA-17 | Chronicle Obsidian Temple polish. CSS-only, `#pane-chronicle`-scoped block appended: chronicle card `--alabaster` surface (light mode) + `--separator-warm` border + `--shadow-card`; dark-mode card override restores `--bg-elevated`; focus ring changed to `--bronze`; save button `--radius-lg`. No HTML or JS changes. |
| EA-18 | Progress / Stars Obsidian Temple polish. CSS-only, `#pane-progress`-scoped block appended: stat cards and calendar card `--alabaster` (light) + `--separator-warm` border + `--shadow-card`; dark-mode overrides restore `--bg-elevated`; today calendar ring `--bronze` (logged-today white ring preserved); milestone list `--radius-lg` + `--shadow-card` + `--separator-warm` rows; active milestone dot `--bronze` + `--bronze-soft` glow; photo items `--radius-lg`. No HTML or JS changes. |
| EA-19 | Settings Obsidian Temple polish. CSS-only, `#pane-settings`-scoped block appended: setting and install cards `--alabaster` (light) + `--separator-warm` border + `--shadow-card` + `--radius-lg`; dark-mode override restores `--bg-elevated`; row separators `--separator-warm`; action rows `--oracle-text` (removes iOS blue); danger rows explicitly re-declared `--red`; chevrons `--bronze`; danger chevrons `--red`; Reset text button `--bronze`. No HTML or JS changes. |
| EA-20 | Global atmosphere pass. CSS-only. Token-level (light mode only): `--bg` `#f2f2f7` → `#ede8df` (warm stone canvas); `--separator` → bronze-warm `rgba(95,70,28,.13)`; `--bg-input` → warm `rgba(100,75,30,.10)`. Global override block appended: header + tab bar warm glass + `--separator-warm` borders (light mode); `.task-list` + `.cycle-card:not(.active)` + `.info-card` → `--alabaster` with dark-mode restore to `--bg-elevated`; `.btn-row` → `--oracle-text`; `.text-btn` + `.task-info-btn` → `--bronze`; `.field-input:focus` → bronze ring; `.btn-primary` → `--obsidian` (light mode); `.app` ambient warm radial glow (light mode). No dark-mode token changes. No HTML or JS changes. |
| EA-21 | Obsidian Temple typography identity pass. Added Google Fonts loading for DM Sans, Spectral, and JetBrains Mono in `index.html`; added `--font-sans`, `--font-display`, and `--font-mono` variables; applied DM Sans to base UI, JetBrains Mono to meta labels/version, Spectral italic bronze treatment to ring numbers/stat values, Spectral italic Chronicle textarea, and lighter 500-weight headings. Updated `CLAUDE.md` only for handoff. No app.js, service worker, state, storage, sync, schema, layout, or feature changes. |
| EA-22 | Temple home concept-card rebuild. Replaced Temple-only hero/quick-action markup with a featured Care ritual card, medallion/seal progress ring, ritual-card quick-action buttons, mono state labels, and restrained bronze hairlines/glow. Preserved all `renderTemple()` IDs and quick-action button IDs. Added only `#pane-temple` scoped CSS plus this handoff log. No app.js, state, storage, sync, schema, service worker, dependency, or other-tab changes. |
| EA-23 | Bronze state pass. CSS-only, single appended block: Today `#pane-today .ring-fg` stroke → `--bronze`; `.task-item.done .task-check` fill+border → `--bronze`; `.task-check svg` color → `var(--bg)` for mode-adaptive checkmark contrast; `.task-item.done .task-text` strikethrough → `rgba(201,165,107,0.3)`; `.cycle-card.active` border → `--bronze`, fill → `--bronze-soft`; `.cycle-active-badge` background → `--bronze`, label color → `var(--bg)`; `.cal-day.logged` background → `--bronze`, day numeral color → `var(--bg)` (EA-18 today-and-logged white inset ring preserved); `.streak-pill` background → `--bronze-soft`, color → `--bronze`; `.task-info-btn` color → `--bronze`. No HTML, no app.js, no service worker, no manifest, no global token changes, no EA-22 Temple block changes. |
| EA-24 | Dark mode token correction. CSS-only. Dark `@media :root` block: 18 token corrections — `--bg` → `#0E0E12` (obsidian floor, not pure black); `--bg-elevated`/`--bg-grouped` → `#1A1A21` (removes iOS cool cast); `--bg-input` → warm `rgba(236,230,217,0.10)`; `--label` → `#ECE6D9` (warm ink, not cold white); `--label-secondary`/`--label-tertiary` → warm `rgba(236,230,217,...)` basis; `--separator` → `rgba(236,230,217,0.08)` (7.5× reduction, warm hairline); `--bronze` → `#C9A56B` (champagne gold, not orange-amber); `--obsidian`/`--obsidian-bg` → `#0E0E12`; `--obsidian-elevated` → `#131318`; `--marble` → `#1A1A21`; `--bronze-soft` → champagne RGB basis; `--ink`/`--ink-secondary` → concept warm values; `--separator-warm` → `rgba(236,230,217,0.10)` (warm-white hairlines replace orange-tinted borders); `--shadow-bronze` → champagne basis with `-4px` spread. Header/tab bar dark glass updated from `rgba(0,0,0,...)` to `rgba(14,14,18,0.88)`. Dark ambient temple glow added to `.app`. No light mode changes, no HTML, no app.js, no service worker, no manifest. |
| EA-25 | Sign-in / sign-up Obsidian Temple redesign + Aperture logo. `index.html`: replaced inner markup of `#auth-screen` with new `.auth-shell` structure — inline Aperture SVG mark (circle + golden-ratio horizon hairline + bronze dot) inside a bronzed medallion, "OBSIDIAN TEMPLE" mono eyebrow, `Elys` DM Sans 500 + italic `ium` Spectral bronze wordmark, italic tagline, underline hairline fields (`.auth-field` / `.auth-field-input`), bronze pill CTA (`.auth-cta`, mono uppercase "Continue" / "Begin" / "Send reset link" / "Go to sign in" / "Update password"), quiet mono links, mono reassurance footer. All five `auth-view` sub-views preserved; every existing element ID preserved verbatim. `styles.css`: appended single EA-25 block — full auth shell styling. Old `.auth-card`/`.auth-glyph`/`.auth-title`/`.auth-switch`/`.btn-link`/`.field`/`.field-input` rules retained (inert) for easy rollback. No app.js, service worker, manifest, favicon, app-icon, Supabase, storage, IndexedDB, skincare task, cycle, or milestone changes. |
| EA-26 | Mobile polish — boot shell, scroll reset, auth keyboard fix, scrollbar hiding, tab bar stabilisation, sign-out tab reset. `index.html`: added `#boot-shell` (Aperture mark + Elys*ium* wordmark, first child of body) as first-paint startup cover; updated `theme-color` metas to `#0E0E12`/`#ede8df`. `app.js`: added `hideBootShell()` (idempotent, null-guarded); called at all 6 exit paths of `applyAuthSession`; 4s safety-net setTimeout fallback; `window.scrollTo(0,0)` in `switchTab()`; `switchTab('temple')` in sign-out handler. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-27 | Modal Obsidian Temple alignment. CSS-only, single block appended, all selectors scoped to `.modal` context. Corrected: handle background, title weight/tracking, field-label mono bronze, field-input underline-only, field-input:focus bronze underline, select arrow bronze, btn-primary dark mode, btn-secondary rust, btn-text muted. No HTML, app.js, state, cycle, milestone, storage, sync, or schema changes. |
| EA-29A | JS modularization with browser-native ES modules. Monolithic `app.js` (~1842 lines) split into 18 ES modules + `main.js` entry point under `js/` tree. Key patterns: callback injection breaks circular deps; `initState()` wraps module-level init; `Object.assign(state, newState)` preserves live ESM binding. `index.html`: `<script src="app.js">` → `<script type="module" src="main.js">`. `sw.js`: CACHE_NAME `elysium-v1` → `elysium-v2`, all module paths added. `app.js` deleted. No CSS, state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, DEFAULT_DATA, or behavior changes. |
| EA-28 | Temple ritual-domain entry screen. `index.html`: replaced `#pane-temple` inner content with featured Care card, 6 domain cards (Chronicle active + 5 dormant), secondary mono nav row. `styles.css`: replaced EA-22 block with EA-28 scoped block. `app.js`: `renderTemple()` updated circumference, cycle blurbs, chronicle note state; added `#temple-goto-chronicle` event listener. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-29B | CSS modularization. Monolithic `styles.css` (2,796 lines) split into 11 feature-based files under `css/`. `index.html` loads all 11 in documented cascade order. `sw.js` ASSETS updated, CACHE_NAME bumped `elysium-v2` → `elysium-v3`. `styles.css` deleted. No visual changes, no new tokens, no JS logic changes. |
| EA-30 | Progress tab Stars/Constellation foundation. `index.html`: added "The *stars*" page heading, `.section-hd` pattern, `star-field-card` class, meta span IDs. `css/progress.css`: star dot states (logged=bronze, partial=ring, empty), constellation SVG line styles, milestone roman numeral. `js/render/progress.js`: added `ROMAN`, `isPartial`, `renderConstellationLines()` (proximity-threshold SVG), roman milestone indices, stage meta. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-34 | Stars/Progress deeper refinement. `js/render/progress.js`: constellation algorithm changed from chain to all-pairs nearest-neighbor; maxDist `cellUnit * 2.5` → `cellUnit * 1.9`. `css/progress.css`: milestone badges → transparent mono bronze; logged star glow strengthened; partial state closer to prototype; constellation line opacity `0.35 → 0.42`. No HTML, state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, or photo logic changed. |
| EA-33 | Today/Care task surface refinement. CSS-only, EA-33 block in `css/today.css`: task-list strips card wrapper; task-item separator-led rows; task-text 15px; task-check 20px; section-icon quiet dim glyph; section-title h2 Spectral italic 17px; cycle-tag/comfort-tag → transparent mono bronze. No HTML, JS, state, task IDs, cycle logic, milestone, storage, sync, or IndexedDB changes. |
| EA-32 | Chronicle prompt card and writing atmosphere. `index.html`: added `.chronicle-heading`, `.chronicle-prompt` dashed card, cleaned `.chronicle-card`. `css/chronicle.css`: heading, prompt, status, save button styles. `js/render/chronicle.js`: added `PROMPTS` array (7 questions), `renderChronicle()` updates question by day of week. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, DEFAULT_DATA, or save/sync logic changed. |
| EA-31 | Cycle tab Astrolabe foundation. `index.html`: replaced 3-card stack with `.astrolabe-wrap` (4 concentric rings + center disc + legend). `css/cycle.css`: astrolabe ring variants, center disc, node positioning/animation, legend. `js/render/cycle.js`: 3 `.al-node` buttons JS-positioned at -90°/30°/150° on 102px radius; click handler unchanged. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-35 | Ritual philosophy pass. Temple domain card subtitles → interaction-primitive verbs (`remember`, `witness`, `arrive`, `hold`, `notice`, `close`); `.temple-domain-archetype` mono archetype names added. Chronicle → Drift: rebuilt to reading-first with `#chronicle-drift` JS-rendered past entries above writing surface; `renderDrift()` with year-ago surfacing and HTML escaping. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-37 | Global header restraint pass. `index.html`: demoted header to compact daily-status rail; removed visible `Today` h1 and flame icon; preserved `#date-label`, `#streak-pill`, `#streak-num`. `css/layout.css`: reduced padding/glass, `.streak-pill` → quiet mono meta. `css/components.css`: removed unused `.greeting` selector. No JS, state, storage, sync, Supabase schema, IndexedDB, task IDs, cycle logic, milestone logic, service worker, manifest, or dependencies changed. |
| EA-39 | Progress Photos section heading alignment. `index.html`: replaced old `.section-header` + circular add affordance with `.section-hd` pattern; preserved `#add-photo-btn` and `#weekly-photo-input`. `css/progress.css`: `.photo-add-action` text-button styling. `js/services/photos.js`: copy-only empty-state update. No photo logic, IndexedDB, state, storage, sync, Supabase schema, progress/calendar/constellation logic, service worker, manifest, or dependencies changed. |
| EA-41 | Settings page heading alignment. `index.html`: added Settings page heading with mono eyebrow `ACCOUNT & RITUAL` and Spectral italic `Settings`. `css/settings.css`: heading styles matching Obsidian Temple pattern. No JS, state, settings behavior, auth behavior, storage, sync, Supabase schema, IndexedDB, service worker, manifest, or dependency changes. |
| EA-43 | Sleep bedtime domain, modal-first. Added `sleep: { entries: {} }` state with migration; `js/domains/sleep.js`; Sleep Temple domain card activated; `#sleep-modal` for bedtime entry + 7-night history; union merge on sign-in; scoped CSS; service worker `elysium-v4`. No new tab, no dependencies, no Supabase schema change, no IndexedDB change, no Care task IDs, no 3-day cycle change, no milestone threshold change. |
| EA-44 | Light Witness domain, modal-first. State `light: { entries: { "YYYY-MM-DD": { witnessedAt: "HH:MM" } } }`; `js/domains/light.js`; Temple Light card active with `#temple-light-state`; `#light-modal` with 7-day dot history and `I'm here` action; `mergedLightEntries` union merge in `js/sync.js`. No new tab, no dependencies, no Supabase schema change, no IndexedDB change, no Care task IDs, no 3-day cycle change, no milestone threshold change. |
| EA-46 | Today/Care emoji section icon removal. Removed emoji glyphs (`☀`, `☾`, `◐`) from section headings; removed unused `#pane-today .section-icon` override from `css/today.css`. All task/cycle/state behavior preserved. |
| EA-47 | Today/Care ritual detail transformation. `index.html` `#pane-today`: removed hero card; added `.care-heading`, `.care-meta-row` (72px mini ring + kv pairs), `.care-section-hd` pattern, `.care-cycle-strip`. `css/today.css`: EA-47 block. `js/render/common.js`: `renderTodayCycle()` updated — `care-tonight` + `care-cycle-strip` rebuild. No state keys, task IDs, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, or DEFAULT_DATA changed. |
| EA-48 | Sleep + Light modal ritual philosophy redesign. Sleep modal → two-state closure ritual (State A: writing surface with Hypnos eyebrow, italic heading, textarea, cue section; State B: goodnight screen with crescent glyph). Light modal → atmospheric sky gradient header, sun disc + time-cursor, elapsed-time figure, `I'm here` / `Witnessed` action. `js/domains/sleep.js`: `saveSleepClosure()`. `js/ui/modals.js`: rewrote `openSleepModal`/`saveSleepModal`. `main.js`: rewrote `renderLightModal()`. CSS in `css/modal.css` and `css/temple.css`. No state schema change. |
| EA-51 | Quieted the global shell by removing the header `#streak-pill` markup, its `css/layout.css` styles, the `main.js` click shortcut, `renderHeader()` streak-number write, and stale `#streak-num` updates from Today and past-day Progress paths. `getStreak()` and the Stars/Progress `#stat-streak` KPI remain unchanged. No state, storage, sync, Supabase schema, IndexedDB, Care task, cycle, milestone, service worker, manifest, or dependency changes. |
| EA-52 | Stars reframe. `index.html`: removed the four Progress KPI stat cards (`Streak`, `Logged`, `This week`, `21-day goal`), changed the eyebrow to `STARS`, and renamed the milestone section to `Ritual stage`. `js/render/progress.js`: removed stat DOM writes and rendered one quiet stage line from the existing milestone threshold result (`Stage I–IV of IV — foundation/acne control/marks and texture/maintenance`). `css/progress.css`: removed KPI/stat and milestone badge-row styles, added scoped quiet stage-line typography. `renderConstellationLines()`, weekly photo behavior, past-day modal behavior, loggedDays/checks, state/storage/sync, cycle logic, Care tasks, and milestone thresholds unchanged. Ticket creation skipped for later batch Jira creation. |
| EA-53 | Care smart-feedback rewrite. `js/render/temple.js`: replaced chain-count, missed-day, and productivity feedback with cycle-turn copy (`Night I/II/III`, protocol in motion, kept turn + tomorrow's protocol), and made the Temple featured Care card lead with the current cycle label while keeping the percent arc. `js/render/common.js`: Today/Care cycle meta and night tag now render `Night I/II/III · protocol`. `js/render/today.js`: completion toast now uses the same kept-turn copy. `index.html`: initial Temple/Today fallback labels changed from generic completion wording to cycle-aware text. No state keys, task IDs, task text, task completion logic, cycle values, milestone thresholds, storage, sync, Supabase schema, IndexedDB, service worker, manifest, dependencies, or unrelated screens changed. Ticket creation skipped for later batch Jira creation. |
| EA-54 | Sleep closure enforcement. `js/domains/sleep.js`: added `isClosedForToday()` and `reopenSleepClosure()` using the existing `sleep.entries[YYYY-MM-DD]` entry as the only closed-day source of truth. `js/state.js`: migration now preserves optional Sleep `note`. `js/ui/tabs.js`: tab switching is guarded while closed and redirects to Sleep State-B. `js/ui/modals.js`: State-B is non-dismissable while closed and reopen deletes today’s Sleep entry, saves locally, refreshes Temple, and hides the modal. `main.js`: startup/post-sync closed checks, closed-day handler registration, `Reopen the day` wiring, and immediate Supabase flush after reopen. `index.html`/`css/modal.css`: replaced State-B `Close` with restrained `Reopen the day`. No new closure key, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change. Ticket creation skipped for later batch Jira creation. |
| EA-55 | Light Witness honesty pass. `index.html`: removed the fake duration figure, duration label, and placeholder-only sun footer from `#light-modal`; eyebrow simplified to `THE FIRST HOUR`; witness button fallback copy simplified to `I'm here`. `main.js`: removed the elapsed-duration helper and all elapsed-minute writes; witnessed button now displays `Witnessed · HH:MM` using existing `witnessedAt`, while current-time cursor positioning remains. `css/temple.css`: removed unused Light metric/footer selectors and tightened eyebrow spacing. `witnessLight()`, `light.entries[date].witnessedAt`, Temple Light card rendering, storage/sync behavior, geolocation/weather/API absence, Care tasks, cycle logic, milestone thresholds, Supabase schema, IndexedDB, service worker, manifest, and dependencies unchanged. Ticket creation skipped for later batch Jira creation. |
| EA-56 | Chronicle subtractive pass. `index.html`: removed the visible Chronicle save button, fixed the prompt to `What asked something of you today?`, softened the textarea placeholder, and made the status line initially hidden. `js/render/chronicle.js`: replaced rotating prompts with one fixed prompt constant, added dirty-guarded autosave on 800ms input idle and blur, removed Chronicle toast feedback, kept empty textarea clearing through existing `upsertChronicleNote(todayStr, body)` delete semantics, hid empty Drift copy, expanded Drift from 3 to 12 rendered entries, and added progressive quieting classes. `main.js`: wired textarea input/blur listeners and flushes pending Chronicle edits before page-hide Supabase flush only when the dirty flag is set. `css/chronicle.css`: removed obsolete Chronicle save-button styles and added quieting rules for older Drift entries. No `chronicle.notes` shape change, no multiple notes per date, no Supabase schema/sync-path change, no IndexedDB change, no Care task/cycle/milestone changes, no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-57 | Care De-Checklist. `index.html` `#pane-today`: moved the three-night cycle strip above task groups, placed Night before Morning, changed Night to a protocol-labeled primary section, demoted Morning, and folded existing Habit tasks into a `Supporting rituals` details surface while preserving `#list-habit` and `data-section="habit"`. `css/today.css`: added an EA-57 scoped block for the primary Night surface, quieter Morning rows, and folded support styling. `js/render/common.js`: `renderTodayCycle()` now updates `#night-protocol-label` (`Tonight · Niacinamide`, `Tonight · Salicylic`, `Rest night`) and keeps `#cycle-tag` to `Night I/II/III`. No state keys, task IDs/text, task completion logic, edit/info behavior, comfort mode behavior, cycle logic, milestone thresholds, storage/sync, Supabase schema, IndexedDB, dependencies, or unrelated screens changed. Ticket creation skipped for later batch Jira creation. |
| EA-59 | Tab bar consolidation — Temple as home. `index.html`: removed Cycle, Chronicle, and Progress `<button class="tab">` elements from the bottom nav; 3 remaining buttons (Temple, Today, Settings) space evenly with existing `flex: 1 / space-around`. `js/ui/tabs.js`: made tab-button lookup null-safe — when `switchTab(name)` is called for a pane with no tab button, Temple's button stays active as the parent surface rather than throwing. All panes (`#pane-cycle`, `#pane-chronicle`, `#pane-progress`) remain in DOM; all render functions, Temple domain cards, and secondary nav links unchanged. No state, storage, sync, Supabase schema, IndexedDB, Care tasks, cycle logic, milestone thresholds, service worker, manifest, or dependency changes. Ticket creation skipped for later batch Jira creation. |
| EA-58 | Reminders into domains — naming and framing pass. `index.html`: Settings section heading `Reminders` → `Practice cues`; row labels `Morning reminder` → `Morning cue`, `Night reminder` → `Night cue`, `Evening check-in` → `Evening cue`; hint paragraph `Reminders fire while…` → `Practice cues fire while…`; Sleep modal TONIGHT row `Bed` → `Night cue` (existing `nightTime` value already displayed there). `js/services/notifications.js`: toasts `Reminders on/off` → `Practice cues on/off`; `fireNotification` confirmation title/body updated; scheduled evening cue title `Daily check-in` → `Evening cue`. No `state.reminders` shape change, no notification scheduling logic change, no permission logic change, no CSS change, no modal behavior change, no Supabase schema change, no IndexedDB change, no Care task/cycle/milestone changes. Ticket creation skipped for later batch Jira creation. |
| EA-60 | Mind domain — Session primitive. `js/constants.js`: added `mind: { sessions: [] }` to `DEFAULT_DATA`. `js/state.js`: added `mind.sessions` migration guard — validates `id` (string), `date` (isYmd), `durationMinutes` (positive number), `completed` (boolean); clamps `reflection` to 500 chars. `js/domains/mind.js`: new module — `getMostRecentSession()`, `getTodaySession()`, `beginMindSession(durationMinutes)`, `endMindSession(sessionId, reflection)`. `js/render/temple.js`: imported `getMostRecentSession`; added Mind card state write to `renderTemple()` — shows `Held · Xm` after any completed session, `—` otherwise. `js/sync.js`: union merge for `mind.sessions` by `id` (local wins on conflict). `index.html`: Mind card `disabled` removed, `id="temple-goto-mind"` added, `id="temple-mind-state"` added; `#mind-modal` added with three states: State A (duration selector 25/40/90, "Begin" primary CTA), State B (quiet "Hold." Spectral italic, duration mono label, `.mind-end-btn` quiet text action — backdrop locked during hold), State C (optional reflection textarea, "Complete" CTA). `css/modal.css`: EA-60 block appended — eyebrow, title, sub, duration row/buttons/unit, State B centered holding layout, `.mind-end-btn` quiet mono style, State C reflection textarea matching Sleep note pattern. `main.js`: imported `beginMindSession`/`endMindSession`; wired `openMindModal`/`closeMindModal` (backdrop-locked during `_mindHolding`), duration button selection, Begin/End/Complete flow with `saveState()` + `renderTemple()` on complete. `sw.js`: `js/domains/mind.js` added to ASSETS; CACHE_NAME bumped `elysium-v4` → `elysium-v5`. No new tab, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change. Ticket creation skipped for later batch Jira creation. |
| EA-62 | Doc sync + dead cleanup. `AGENTS.md`: replaced 249-line stale monolith-era snapshot (referencing deleted `app.js`/`styles.css`, pre-EA-29A constraints, incomplete ticket log) with a 5-line pointer to `CLAUDE.md` as the authoritative handoff document. `js/domains/care.js`: removed `getStreak()` and `getMissedDays()` — confirmed zero imports anywhere in the module tree or `main.js`. `js/state.js`: in `migrateState`, added a second `.filter()` pass on `mind.sessions` that drops any session where `completed === false` and `sess.date` is not today (calculated inline via `ymd(new Date())` since module-level `todayStr` is not yet set at migration time); completed sessions and today's in-progress session are always preserved; session shape unchanged. `CLAUDE.md` + `docs/ticket-log.md`: EA-62 entries added. No UI changes, no state shape changes, no Supabase schema changes, no IndexedDB changes, no task/cycle/milestone changes, no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-63 | Decommission Care Percentage — Cycle-State Care. `index.html`: replaced the Today numeric label/suffix and Temple mini-arc markup with three-night cycle-state indicators. `css/today.css` and `css/temple.css`: added scoped mark/state styling for untouched, in motion, kept, and resting states. `js/render/temple.js`: replaced numeric Care card arc logic with cycle-turn state derivation from existing night task checks; Temple now renders `Night I/II/III · protocol` plus protocol state copy. `js/render/common.js`: Today cycle strip and compact indicator now reflect current turn state without numbers. `js/render/today.js`: task toggles still use existing morning/night completion and `loggedDays` rules, then refresh Today and Temple Care state. `CLAUDE.md` + `docs/ticket-log.md`: EA-63 entries added. No state shape changes, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no task IDs/text change, no 3-day cycle change, no milestone threshold change, no dependencies, and no Sleep/Light/Mind behavior change. Ticket creation skipped for later batch Jira creation. |
| EA-64 | Past-Day Modal Kept-Turn Rewrite. `js/render/progress.js`: removed the Stars modal checkbox/task rendering path and historical toggle handler; selected non-future dates now render a read-only `Day record` with `MONDAY · 18 MAY` style date meta, cycle context (`Night I/II · protocol` or `Rest night`), and status derived only from existing `loggedDays`/`checks` (`kept`, `partial`, `not kept`, `quiet` for today with no checks). Chronicle notes for the date surface as a small italic excerpt when present. `css/modal.css`: replaced past-day task-list section styling with scoped record/status/excerpt styling using JetBrains Mono, Spectral italic, warm separators, and restrained bronze. `index.html`: updated the modal title fallback to `Day record`. `CLAUDE.md` + `docs/ticket-log.md`: EA-64 entries added. No state shape changes, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no task IDs/text change, no 3-day cycle change, no loggedDays/checks persistence rule change, no Today/Care task completion change, and no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-65 | Mind Hold-the-Time. `js/domains/mind.js`: added timestamp-derived held-duration helpers, most-recent-reflection lookup, and preservation of an existing `endedAt` when saving reflection after the hold ends. `js/ui/modals.js`/`main.js`: moved Mind modal wiring into the modal module; State B now starts a quiet elapsed timer, updates a bronze hairline progress variable, disables `End session` with `Hold a little longer` for the first 60 seconds, resumes today's incomplete hold if present, and stamps `endedAt` when the user manually ends the hold rather than after writing the reflection. `index.html`/`css/modal.css`: added a restrained recent-reflection line in State A, a held elapsed line + bottom hairline in State B, and softened State C copy to `Held.` / `Save line`. `js/render/temple.js`: Temple Mind card now renders actual held duration from `startedAt`/`endedAt` instead of selected `durationMinutes`. `CLAUDE.md` + `docs/ticket-log.md`: EA-65 entries added. No state shape expansion, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no Care task IDs/text, no 3-day cycle change, no milestone threshold change, no analytics/totals/streaks/charts, and no dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-68 | Light Witness Deepening — Temporal Sky. `js/domains/light.js`: added `LIGHT_PERIOD_LABELS` (8 named periods by hour range) and exported `getLightPeriodLabel(h)`. `js/render/temple.js`: imported `getLightPeriodLabel`; Temple Light state now shows the qualitative period name at time of witnessing (e.g., `Morning`, `Golden hour`) instead of raw `formatTime12()` timestamp. `main.js`: added `LIGHT_PERIODS` array (8 periods with `top`/`mid`/`bot` hex colors from Obsidian Temple palette) and `getCurrentLightPeriod()`; rewrote `renderLightModal()` — sets `--light-sky-top/mid/bot` CSS custom props on `#light-sky`, writes `APOLLO · [PERIOD]` to `#light-eyebrow`, button always enabled; removed `if (getLightEntry(todayStr)) return;` early-return guard from click handler; after witness tap, button briefly reads `Witnessed ·` for 1500ms then calls `renderLightModal()` to reset. `index.html`: added `id="light-eyebrow"` to `.light-modal-eyebrow` div; fallback text changed to `APOLLO · MORNING`. `css/temple.css`: converted `.light-sky` background to `linear-gradient` using `var(--light-sky-top/mid/bot)` with dark-mode hex fallbacks; light-mode `@media` block now sets the same custom properties rather than a separate hardcoded gradient; removed `#light-modal .btn-primary:disabled` rule (dead code — button is never disabled). No state shape change, no `witnessedAt` field change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no sync path change, no Care/Sleep/Mind/Chronicle behavior change, no new module, no new dependencies. Ticket creation skipped for later batch Jira creation. |
| EA-70 | Edit/Utility Modal Obsidian Pass. `css/components.css`: replaced `.task-info-label` (was `font-weight: 600`, `color: var(--label-tertiary)` — iOS tokens) with mono-eyebrow treatment (`font-family: var(--font-mono)`, `9px`, `0.22em`, `var(--bronze)`, `font-weight: 400`); replaced `.task-info-text` (was `color: var(--label-secondary)`) with Spectral italic body (`font-family: var(--font-display)`, `font-style: italic`, `15px`, `color: var(--muted-text)`, `margin: 0`). `index.html`: edit-modal — field label "Task" → "Name", field label "Cycle day" → "Cycle", select option "Daily habit" → "Daily", cycle options "Day 1/2/3" → "I/II/III · Niacinamide/Salicylic/Rest", "All cycle days" → "All turns", delete button "Delete" → "Remove"; confirm-modal — title "Are you sure?" → "Confirm". `js/ui/modals.js`: `openEditModal` title string `'Edit task'` → `'Edit step'`; `openAddModal` title string `'New task'` → `'New step'`. No behavior changes, no state changes, no select `value` attributes changed, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no task IDs/text change, no Care/cycle/Sleep/Light/Mind/Chronicle/Stars/Temple logic change. Ticket creation skipped for later batch Jira creation. |
| EA-69 | Stars Stop Counting. `js/render/progress.js`: removed `const ROMAN` array (only used for the now-deleted stage fraction); cleared `#star-field-meta` write (was `${logged} RECORDED`); cleared `#milestone-stage-meta` write (was `CURRENT`); simplified `stageLine.innerHTML` from a three-span flex row (`Stage I of IV — foundation`) to a single `stage-line-title` span containing only the Spectral italic season name. `index.html`: "Ritual stage" section label changed to "Season". `css/progress.css`: removed dead `#pane-progress .stage-line-meta` and `#pane-progress .stage-line-separator` rules (no longer rendered). `CLAUDE.md` + `docs/ticket-log.md`: EA-69 entries added. No constellation algorithm change, no `renderConstellationLines()` change, no `openPastDayModal`/`renderPastDayBody` change, no `loggedDays`/`checks`/`milestoneStage`/`startDate` change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no Care/Chronicle/Sleep/Light/Mind behavior change, no new stats/counts/labels added. Ticket creation skipped for later batch Jira creation. |
| EA-71 | Chronicle Silent-Night Atmosphere. `index.html`: removed the `.chronicle-prompt` block (`.chronicle-prompt-label` "A quiet record of the day" and `.chronicle-prompt-q` "What asked something of you today?" — 4 lines); changed `rows="7"` to `rows="2"` on `#chronicle-textarea`. `css/chronicle.css`: added `field-sizing: content` and `min-height: calc(2 * 1.6 * 15px + 32px)` to `.chronicle-textarea` for natural auto-grow from 2-line minimum; removed dead `.chronicle-prompt`, `.chronicle-prompt-label`, `.chronicle-prompt-q`, and associated dark-mode block (was ~30 lines). `js/render/chronicle.js`: no change — `if (promptEl)` guard already made the element optional. No `chronicle.notes` shape change, no one-note-per-day change, no autosave/debounce change, no drift rendering change, no Supabase schema change, no IndexedDB change, no STORAGE_KEY change, no state/sync behavior change, no other domain changes. Ticket creation skipped for later batch Jira creation. |
| EA-72 | Past-Cycle Chronicle Surfacing. `js/domains/chronicle.js`: added `getLastChronicleNoteForCycleDay()` to derive the newest prior same-protocol Chronicle note from existing `chronicle.notes` and `getCycleDayForDate()`. `index.html`: added hidden `#care-turn-memory` markup inside the Care night protocol surface after `#list-night`. `js/render/common.js`: `renderTodayCycle()` now fills or hides the memory line with `Last Niacinamide night`, `Last Salicylic night`, or `Last rest night` plus a short normalized Chronicle excerpt. `css/today.css`: added scoped whisper styling with a quiet hairline, mono label, and Spectral italic muted body. `sw.js`: bumped `CACHE_NAME` to `elysium-v6` for shell freshness. No new state, no `chronicle.notes` shape change, no one-note-per-day change, no Chronicle autosave/drift behavior change, no Care task IDs/text change, no 3-day cycle logic change, no `loggedDays`/`checks` behavior change, no Supabase/storage/sync/IndexedDB change, and no Day Record behavior change. |
| EA-73 | Past Day Read-Only by Default. `js/render/progress.js`: replaced the always-visible `.past-day-mark-btn` in `renderPastDayBody` with a two-phase affordance — Phase 1 renders a `.past-day-correct-link` quiet text button ("correct record"); clicking it hides the link and reveals a `.past-day-correction-panel` containing a `.past-day-correction-note` qualifying line ("Use only if the day was kept in life but missed in the app.") and the existing `.past-day-mark-btn` ("Mark as kept" / "Clear record"). Mark/clear state logic unchanged. `css/modal.css`: replaced EA-67 `.past-day-actions` comment/rule with an EA-73 block — `.past-day-correct-link` (mono 9px, lowercase, `--muted-text`, opacity 0.55, no border/background), `.past-day-correction-panel[hidden]` display:none, `.past-day-correction-panel` flex column gap:12px, `.past-day-correction-note` (Spectral italic 13px, `--muted-text`, opacity 0.75). `.past-day-mark-btn` styling unchanged. No state shape change, no `loggedDays`/`checks` behavior change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no sync path change, no Care/cycle/Chronicle/Sleep/Light/Mind logic change, no Today/Care editability change, no future-date protection change. |
| EA-74 | Toast tone pass. `js/ui/modals.js`: reworded `showToast('Task name is required')` → `showToast('Name your step')` (aligns with EA-70 "step" modal language); removed `showToast('Task added')`, `showToast('Task saved')`, `showToast('Task deleted')` — all three are redundant: the modal closes and the task list re-renders as the confirmation signal; `deleteTask` already goes through `showConfirm()`. `js/services/photos.js`: removed `showToast('Photo saved')` — the grid re-render after the async compress/save is the confirmation, and `'Saving photo…'` already covers in-progress UX; removed `showToast('Photo deleted')` — user already confirmed via `confirm()` and the grid re-renders. `js/render/settings.js`: removed `showToast('Data exported')` — the browser file download dialog is the confirmation signal. `js/state.js`: reworded `` showToast(`Ritual updated for ${MILESTONES[newStage].weeks}`) `` → `showToast('A new season begins')` — aligns with EA-69 "Season" framing; this fires at most 3 times in the app's lifetime (days 14/28/56). `js/render/cycle.js`: reworded `` showToast(`Switched to ${CYCLE_NAMES[i]} night`) `` → `showToast(CYCLE_NAMES[i])` — follows the sparse `'Witnessed'` pattern; astrolabe already visually confirms the active node. No CSS changes, no state shape changes, no auth/sync/notification changes, no toast utility behavior changes, no task ID/text changes, no 3-day cycle logic changes, no Supabase schema changes, no IndexedDB changes, no service worker or manifest changes. |
| EA-75 | Light Witness Deepening II — persistent horizon marks + brief stillness pause. `js/domains/light.js`: `witnessLight(dateStr)` changed from setting `{ witnessedAt: "HH:MM" }` to pushing to `witnesses: []` array (deduplicates by minute — same HH:MM string not pushed twice); added exported `getLastWitness(dateStr)` that returns `witnesses.at(-1)` or null. `js/state.js`: `migrateState()` — added per-entry loop for `light.entries`: detects old `witnessedAt` shape and wraps to `witnesses: [witnessedAt]`; ensures all entries have a valid `witnesses` array; filters witnesses to valid `HH:MM` strings; removes entries with non-YMD keys. `js/sync.js`: replaced simple object spread for `mergedLightEntries` with a per-date union merge — iterates all dates from cloud and local, union-merges the `witnesses` arrays via `Set` deduplication and `.sort()`. `js/render/temple.js`: import swapped from `getLightEntry` to `getLastWitness`; Temple Light card now reads `getLastWitness(todayStr)` to derive the period hour for `getLightPeriodLabel()`. `main.js`: `renderLightModal()` now injects `.light-witness-mark` elements — clears old marks, then for each time in `state.light.entries[todayStr].witnesses` creates an absolute-positioned div at the correct `left` percentage on `#light-sky`; click handler updated: commits immediately, sets button to `·` and disables it, hides close button via `visibility: hidden`, after 3000ms calls `renderLightModal()` (which renders sky marks), restores close button, sets button to `Witnessed ·`, then after 1500ms calls `renderLightModal()` again to reset to `I'm here`. `css/temple.css`: added `.light-witness-mark` rule — 4×4px bronze circle at `bottom: 43px` (horizon height), 55% opacity, no pointer-events. `sw.js`: bumped `CACHE_NAME` `elysium-v6` → `elysium-v7` to invalidate cached JS/CSS for offline users. `index.html`: no changes. No Supabase schema change, no IndexedDB change, no STORAGE_KEY change, no Care/Sleep/Mind/Chronicle/Cycle/milestone behavior changes, no count or analytics language added. |
| EA-77 | Cycle Tab Quieting. `index.html`: removed both info-card sections from `#pane-cycle` — the "Sweat management" block (4 `info-row` items: dab sweat, rinse, no touching, beard care) and the "Active rules" block (4 `info-row` items: salicylic frequency, no layering, pea-sized, no picking); replaced both with a single `<p class="cycle-note">` — "One active per night. Pea-sized. Nothing layered." — placed between the astrolabe legend and `footer-spacer`. `css/cycle.css`: appended `.cycle-note` rule — Spectral italic 14px, `--muted-text`, center-aligned, `4px 0 28px` padding. No JS changes, no state changes, no constants changes, no Care/cycle logic changes, no Today/Care tab changes, no EA-76 Night protocol changes, no task IDs or default task text changes, no Supabase schema changes, no STORAGE_KEY changes, no sync behavior changes. |
| EA-78 | Regression Fix Pass — 6 targeted fixes. `css/base.css`: added `--bronze-dim: rgba(160,114,42,0.55)` to light `:root` block and `rgba(201,165,107,0.55)` to dark block, placed after existing `--bronze-soft` in each section. `css/today.css`: removed the duplicate EA-76 rule `#pane-today .care-night-section.has-care-memory .task-item:last-child { border-bottom: 1px solid var(--separator-warm); }` that was overriding the earlier correct `border-bottom: none` rule, fixing the double-separator visual when care memory is shown. `js/services/notifications.js`: morning push title changed from `'Morning care ritual'` → `'Morning cue'`; night notification body changed from `() => \`Cycle day ${state.cycleDay + 1}: ${CYCLE_DESC[state.cycleDay]}\`` → `() => \`Tonight — ${CYCLE_NAMES[state.cycleDay]}\`` (same as title); `CYCLE_DESC` removed from import since it is no longer used in this file. `js/render/settings.js`: export download filename changed from `skincare-data-${todayStr}.json` → `elysium-data-${todayStr}.json`. `js/ui/modals.js`: `deleteTask` confirm prompt changed from `showConfirm('Delete this task?', 'Delete')` → `showConfirm('Remove this step?', 'Remove')` — consistent with EA-70 "step" language. `js/render/chronicle.js`: removed the dead `CHRONICLE_PROMPT` constant (string literal left over from EA-71 HTML removal), the `promptEl` lookup (`getElementById('chronicle-prompt-q')`), and the `if (promptEl) promptEl.textContent = CHRONICLE_PROMPT` guard — all three are no-ops since the element was removed from HTML in EA-71. Chronicle autosave, drift, state, and sync behavior untouched. No state shape changes, no Supabase schema changes, no STORAGE_KEY changes, no IndexedDB changes, no Care/Sleep/Mind/Light/cycle logic changes. |
| EA-79 | Dead Code Cleanup. `js/domains/light.js`: removed `getLightEntry(dateStr)` — grep confirmed zero imports across all JS/HTML; `getLastWitness` is the active accessor. `js/domains/sleep.js`: removed `getRecentSleepHistory(days)` and `saveSleepBedtime(dateStr, bedtime)` — grep confirmed zero imports across all JS/HTML; also removed the now-orphaned `ymd` import from the same file. `js/constants.js`: removed `CYCLE_FULL_NAMES` array — grep confirmed zero imports across all JS/HTML. `css/today.css`: removed five dead EA-16/EA-23 scoped selectors whose target elements no longer exist in `#pane-today`: `#pane-today .hero-card` (both light rule and dark @media variant), `#pane-today .ring-bg`, `#pane-today .hero-label`, `#pane-today .ring-fg`; also removed `#pane-today .section-title h2` (EA-33 remnant); grep on `index.html` confirmed none of these class names appear in `#pane-today` markup. `css/components.css`: removed the entire `.info-card` block (two rule sets + dark @media) and the `.info-row` / `.info-row:last-child` block — grep confirmed zero usages in any HTML or JS file; `.task-info-row` is distinct and untouched. `index.html`: fallback static text inside `#modal-title` changed from `Edit task` → `Edit step` — this text is overwritten by `openEditModal`/`openAddModal` at runtime, but the EA-70 "step" language should be consistent even in the dormant initial state. No behavior changes, no state shape changes, no Supabase schema changes, no STORAGE_KEY changes, no sync path changes, no Care/cycle/milestone/Chronicle/Sleep/Mind/Light logic changes. |
| EA-80 | Settings/System Surface Polish. `index.html` (`#pane-settings` only, 9 copy edits): row label "Signed in as" → "Signed in"; section heading "Care ritual" → "Care"; comfort mode sub-text → "Warm months — face wash and sunscreen only"; `#notif-hint` → "Cues fire while the app is running. On iPhone, install to Home Screen for background delivery."; section heading "Install" → "Home Screen"; `#install-text` → "iPhone: Share → Add to Home Screen. Mac: File → Add to Dock. Chrome: install icon in the address bar."; section heading "Data" → "Records"; export button label "Export data" → "Export"; import label "Import data" → "Import". `css/settings.css` (5 CSS changes): `.setting-name` `font-size` 16px → 14.5px (prototype spec); `.setting-sub` `font-size` 13px → 11.5px (prototype spec); `.toggle input:checked + .toggle-slider` `background` `var(--green)` → `var(--bronze)` — removes the only iOS system green in the app; `#pane-settings .btn-row.danger` and `.btn-row.danger .chevron` `color` `var(--red)` → `#d97a5e` (desaturated rust per prototype spec, flagged since EA-19); added `#pane-settings .section-h` override — JetBrains Mono 10px 0.22em uppercase `var(--bronze-dim)`, converting six 20px iOS-style section headings into quiet mono eyebrow labels. Scoped to `#pane-settings` — no other tab affected. No JS changes, no state shape changes, no Supabase schema changes, no STORAGE_KEY changes, no sync path changes, no notification behavior changes, no Care/cycle/Sleep/Mind/Light logic changes, no new settings or controls. |
| EA-81 | Temple Card State Deepening. `js/render/temple.js` only — display-only changes to four domain-card state strings in `renderTemple()`. Chronicle: `hasNote ? 'Written'` → `hasNote ? 'A line'` — more qualitative, Chronicle-vocabulary, implies something was captured rather than a file-system action; `'Quiet'` unchanged. Light (unwitnessed): `'—'` → `'Unseen'` — names the absence of the act, mirrors domain verb WITNESS; witnessed state (qualitative period label from `getLightPeriodLabel()`) unchanged. Sleep: replaced `getLastSleepEntry()` (all-time) with `getSleepEntry(todayStr)` (today only) — fixes a correctness bug where yesterday's bedtime would appear on today's card while today was still open; state is now `'Closed'` when today has an entry, `'—'` when today is open; the old formatted bedtime metric and `'Quiet'` label removed. Mind (no sessions): `'—'` → `'Unheld'` — names the absence of the act, mirrors domain verb HOLD; `'Held · Xm'` state unchanged. Import changes: removed `formatTime12` from `'../utils.js'` (no longer used in this file after Sleep change); replaced `getLastSleepEntry` with `getSleepEntry` in `'../domains/sleep.js'` import. No state shape changes, no STORAGE_KEY change, no Supabase schema changes, no IndexedDB changes, no sync path changes, no CSS changes, no HTML changes, no Care/cycle/milestone/Chronicle/Mind/Light behavior changes, no new metrics or counts. |
| EA-82 | Dormant Domain Restraint. `index.html`: removed `<span class="temple-dormant-label">IN STUDY</span>` from inside `.temple-dormant-head` — the separator div is kept for its `border-top` line, the text claim is gone; changed Body `.temple-domain-state` from `—` to `Still`; changed Water `.temple-domain-state` from `—` to `Still`. `css/temple.css`: updated `#pane-temple .temple-domain-card:disabled` `opacity` from `0.38` to `0.45` — shifts read from "disabled button" to "quieter card"; removed the now-dead `#pane-temple .temple-dormant-label` CSS block entirely. No JS changes, no state shape changes, no domain activation, no behavior changes, no new CSS rules, no sync/storage/schema changes. |
| EA-83 | Light Witness Stillness. `main.js`: removed immediate `showToast('Witnessed')` from the witness click handler; replaced `btn.disabled = true` with `btn.classList.add('is-still')` so blocking feels atmospheric rather than hard-disabled; replaced `btn.disabled = false` in `renderLightModal()` with `btn.classList.remove('is-still')`; removed redundant `b.disabled = false` from the post-stillness callback. `css/temple.css`: added `#light-modal .btn-primary.is-still` modifier (`opacity: 0.4; pointer-events: none`) scoped to the light modal. Behavior unchanged: witness commits immediately, 3-second stillness with `·` button, horizon mark appears after pause, button briefly shows `Witnessed ·`, returns to `I'm here`. No state shape, sync, migration, Temple label, witness array, or domain changes. |
| EA-85 | Temple Temporal Breath. `js/render/temple.js`: added module-scope `TEMPLE_PERIOD_BUCKETS` constant (8 entries, `{ start, end, key }`) mapping hours to period keys (night/first-light/morning/midday/afternoon/golden-hour/dusk/night); in `renderTemple()`, added 4 lines before the existing `lineEl` update — compute `new Date().getHours()`, find matching bucket, set `paneEl.dataset.period` on `#pane-temple`. `css/base.css`: added `--temple-grad-alpha: 0.10` to both light-mode `:root` and dark-mode `:root` as a standalone ambient alpha token. `css/temple.css`: updated both `#pane-temple .temple-featured` background gradient (light) and its dark-mode override to use `var(--temple-grad-alpha, 0.10)` as the rgba alpha for the first gradient stop instead of hardcoded `0.10`/`0.11`; added 7 period attribute selector rules (`#pane-temple[data-period="..."]`) each overriding only `--temple-grad-alpha` — night 0.06 / first-light 0.09 / morning 0.12 / midday 0.08 / afternoon 0.10 / golden-hour 0.14 / dusk 0.11. No new DOM elements, no new state, no storage changes, no sync/schema changes, no labels added, no animation loops, no new imports. `renderTemple()` is already called on every tab switch so no timer is needed. The shift is sub-perceptual (±4% opacity on a warm gradient); the effect is felt over days of use, not noticed on a single visit. |
| EA-86 | Inter-Screen Continuity Pass. `css/layout.css`: replaced the previous single-line `.tab-pane.active { display: block; animation: fadeIn 0.25s ease; }` with three rules — `.tab-pane.active` now uses `animation: paneEnter 240ms cubic-bezier(0.25, 0.6, 0.25, 1) both`, `.tab-pane.is-leaving` (display: block, `animation: paneLeave 120ms ease-out both`, `pointer-events: none`); added two new keyframes — `paneEnter` (`opacity: 0 → 1`, `transform: translateY(6px) → translateY(0)`) and `paneLeave` (`opacity: 1 → 0`, no transform); preserved the existing `fadeIn` keyframe (still used by `.auth-screen` in `css/auth.css`); added `@media (prefers-reduced-motion: reduce) { .tab-pane.active, .tab-pane.is-leaving { animation: none; } }` so no fade and no translate fires when reduced motion is on. `js/ui/tabs.js`: refactored `switchTab(name)` — added module-scope `leavingTimer` and private `runRenderFor(name)` helper (centralises the existing `renderTemple`/`renderChronicle`/`renderProgress` + `renderWeeklyPhotos`/`renderCycleList`/`updateSettingsView` calls). New flow: (1) `isClosedForToday()` short-circuit unchanged; (2) resolve `newPane` via `getElementById('pane-' + name)`, return `false` if missing (defensive); (3) resolve `currentPane` via `.tab-pane.active:not(.is-leaving)`; (4) immediate tab-bar swap (Temple fallback preserved); (5) same-pane retap re-renders without animating; (6) cancel any pending `leavingTimer` and force-clean residual `.is-leaving`/`.active` classes if a rapid switch happens mid-fade; (7) `prefers-reduced-motion` check via `matchMedia` — when set, swap is immediate (no `.is-leaving` class added, no setTimeout); (8) otherwise add `.is-leaving` to outgoing pane, `setTimeout(swap, 120)`; the `swap()` closure removes `.active`/`.is-leaving` from outgoing, calls `window.scrollTo(0, 0)`, adds `.active` to incoming (triggers `paneEnter` keyframe), then calls `runRenderFor(name)`. No DOM restructuring, no router introduced, no new dependencies. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync path change, no IndexedDB change, no Care/Sleep/Mind/Light/Chronicle behavior change, no modal animation change, no tab-bar animation change, no header/auth/boot-shell animation change. Total perceptual transition ≈ 360ms (120ms leave + 240ms enter); reduced-motion = instant. |
| EA-84 | Temple Daily Opening State. `index.html`: inserted `<div class="temple-greeting"><p class="temple-greeting-line" id="temple-daily-line"></p></div>` immediately before `.temple-featured` — a minimal block that holds a single Spectral italic line above the Care card; no heading, no eyebrow, no fallback copy. `js/render/temple.js`: added private `getDailyLine()` helper that derives one quiet sentence from existing state in priority order — (1) Sleep closed today → "The day has been closed.", (2) Care turn kept → "The night has been kept.", (3) Care turn in motion → "The night moves quietly.", (4) Chronicle note exists today → "A line was left.", (5) Mind session completed today → "A time was held.", (6) Light witnessed today → "Light was seen.", (7) otherwise empty string; added two-line call in `renderTemple()` to set `#temple-daily-line` textContent. `css/temple.css`: added `#pane-temple .temple-greeting` (center, `--space-md` bottom margin) and `.temple-greeting-line` (Spectral italic 15px `--muted-text`) before the small-viewport media block. No state shape changes, no STORAGE_KEY change, no Supabase schema changes, no sync path changes, no IndexedDB changes, no domain behavior changes. All data reads reuse imports already present in `temple.js`. | EA-76 | Care De-Checklist II. `index.html`: changed the Today/Care Night surface from visible task-list-first markup to a protocol-first section with `#care-night-action` ("Keep tonight"), quiet `#care-night-state`, and `#care-night-steps` details reveal; moved the Night add control inside the Steps reveal while preserving `#list-night` and `data-section="night"`. `js/render/today.js`: added `keepNightProtocol()` that marks all current `getNightTasks()` IDs true in the existing `checks[todayStr]` object, then reuses a shared `reconcileDayCompletion()` helper so `loggedDays` still updates only when Morning and Night are complete; individual task toggles keep their existing behavior and still announce the day-complete toast path. `js/render/common.js`: added Night protocol control rendering — disables the action once kept, writes `Night kept.`, sets Steps meta (`View protocol` / `In motion` / `Kept`), and auto-opens Steps only for partial Night completion. `main.js`: wired `#care-night-action` to `keepNightProtocol()`. `css/today.css`: added scoped EA-76 styles for the restrained protocol action, Spectral state line, and quiet Steps reveal. `sw.js`: bumped `CACHE_NAME` `elysium-v7` → `elysium-v8` for changed shell assets. `CLAUDE.md` + `docs/ticket-log.md`: EA-76 entries added. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync behavior change, no Chronicle bridge change, no task add/edit/delete data model change, no task IDs/text change, no 3-night cycle value or advancement change, no scores/percentages/streaks/analytics added. |
| EA-87 | Chronicle From the Well. `index.html` `#pane-chronicle`: inserted a new `#chronicle-well` block (hidden by default) between `.chronicle-heading` and `#chronicle-river-hd`, containing `.chronicle-well-divider` (mono "From the well" centred between hairlines) and a `.chronicle-well-entry` with `#chronicle-well-eyebrow` (mono micro-label) and `#chronicle-well-body` (Spectral italic). `js/render/chronicle.js`: removed `getYearAgoEntry()` and the `.chronicle-drift-ago` rendering path inside `renderDrift()`; added `ymdOffset(baseStr, dayOffset)`, `daysApart(laterStr, earlierStr)`, `isLivingNote(note)` helpers and `getWellEntry()` returning `{ dateStr, note, label } | null` in priority order — (1) exact same-date one year ago via `setFullYear(y-1, m, d)` → `A year ago today`; (2) `±3`-day window around that anchor iterated `[1,-1,2,-2,3,-3]` so closer days win → `A year ago this week`; (3) ascending past entries with `daysApart(todayStr, dateStr) >= 60 && daysApart(newestNonToday, dateStr) >= 14` → `From an earlier turn`; (4) otherwise `null` (silence); added `renderWell()` that writes textContent into the eyebrow and body nodes (textContent preserves `\n` under `white-space: pre-wrap`) and toggles `hidden`; refactored `renderDrift(el, excludeDateStr)` to filter the well's `dateStr` out of the past-entries list and removed the year-ago `<div class="chronicle-drift-ago">…</div>` wrapper; `renderChronicle()` now captures the well's returned dateStr and passes it to `renderDrift`. `css/chronicle.css`: removed the now-dead `.chronicle-drift-ago`, `.chronicle-drift-ago-label`, and `.chronicle-drift-ago .chronicle-drift-entry:first-child` rules (light + dark `@media`); appended an EA-87 block with `.chronicle-well` (margin), `.chronicle-well[hidden]` (display:none), `.chronicle-well-divider` (flex row with `::before`/`::after` hairlines + mono `var(--bronze-dim)` span padding 0 14px), `.chronicle-well-entry` (padding 4px 2px 18px), `.chronicle-well-eyebrow` (mono 9px 0.20em uppercase `var(--bronze)` opacity 0.75), and `.chronicle-well-body` (Spectral italic 16px line-height 1.55 `var(--oracle-text)` `white-space: pre-wrap` `word-break: break-word`). `sw.js`: bumped `CACHE_NAME` `elysium-v8` → `elysium-v9` for changed shell assets. `CLAUDE.md` + `docs/ticket-log.md`: EA-87 entries added. No `chronicle.notes` shape change, no one-note-per-day change, no autosave/debounce/blur/page-hide change, no Drift quieting tier change, no Chronicle status meta change, no `#chronicle-river-hd` markup or copy change, no Care turn-memory whisper change, no Day Record Chronicle excerpt change, no `state.chronicle` migration, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no sync path change, no Sleep/Light/Mind/Care/Cycle/milestone behavior change, no new dependencies, no new state, no prompts, no AI, no mood/tags/search/stats/streaks/multiple-entries-per-day. |
| EA-88 | Care rhythm bars. `index.html`: added `.care-rhythm` wrapper (`.care-rhythm-bars` grid + `.care-rhythm-l` label) inside `.care-cycle-strip` after the `#care-cycle-strip` 3-card grid. `css/today.css`: EA-88 block appended — `.care-rhythm` top margin; `.care-rhythm-bars` 21-column 28px grid with 2px gaps; 6 `.care-bar` modifier rules covering all state combinations (past+on, past+rest, now+on, now+rest, future+on, future+rest); `.care-rhythm-l` JetBrains Mono 8.5px `--muted-text` label. `js/render/common.js`: added `renderCareRhythm(cycleDay)` function — loops 21 positions, derives each bar's cycle type from `((cycleDay + offset) % 3 + 3) % 3` using only `state.cycleDay`; clears and rebuilds `#care-rhythm-bars` on every `renderTodayCycle()` call. Bars are positional only: never reads `state.loggedDays`, `state.checks`, or `state.startDate`. No dates, no counts, no labels per bar, no tooltips, no tap interaction, no new state, no sync/storage/schema change, no Care task IDs/text change, no Night protocol change, no cycle logic change, no milestone threshold change, no service worker bump, no new dependencies. |
| EA-90 | Temple State-Carry on Return. `js/render/temple.js`: added three module-scope variables — `_pendingTrace` (null or domain key), `_pendingTraceAt` (timestamp ms), `TRACE_TTL_MS = 90000`; added `TRACE_CARD_IDS` lookup (`light→'temple-goto-light'`, `sleep→'temple-goto-sleep'`, `mind→'temple-goto-mind'`, `care→'temple-goto-today'`); exported `applyTempleTrace(domain)` — finds card by id, removes `.just-touched`, forces reflow via `offsetWidth`, adds `.just-touched`, registers `{ once: true }` `animationend` listener that removes the class (self-cleaning, no timer needed); exported `queueTempleTrace(domain)` — sets `_pendingTrace` and `_pendingTraceAt`; added 10-line pending-trace check at the very top of `renderTemple()` — if pending and within TTL, calls `applyTempleTrace(domain)` and clears both vars; if past TTL, clears silently. `js/ui/modals.js`: added `applyTempleTrace` to the existing `import { renderTemple }` line from `../render/temple.js`; added `applyTempleTrace('sleep')` at the end of `saveSleepModal()` after the state-b flip; added `applyTempleTrace('mind')` inside the `mind-complete` click listener after `renderTemple()` and before `mind-modal.hidden = true`. `main.js`: added `applyTempleTrace` to the existing `import { renderTemple }` line from `./js/render/temple.js`; added `let _lightWitnessedThisSession = false` module-scope flag inside DOMContentLoaded; modified `openLightModal()` to set `_lightWitnessedThisSession = false` before `renderLightModal()` (flag resets on every modal open, not inside renderLightModal which fires again after stillness pause); added `_lightWitnessedThisSession = true` inside the `light-witness-btn` click handler immediately after `witnessLight(todayStr)`; modified `closeLightModal()` — if flag is true, calls `applyTempleTrace('light')` and resets flag, otherwise no-op. `js/render/today.js`: added `queueTempleTrace` to the existing import from `./temple.js`; added `queueTempleTrace('care')` at the end of `keepNightProtocol()` after `renderAllLists()` — this fires AFTER the `renderAllLists()` → `renderTemple()` call chain, so the queue is set for the NEXT Temple visit (the user is on the Today tab when Care is kept). `css/temple.css`: EA-90 block appended after the small-viewport media query — `#pane-temple { --bronze-trace-glow: rgba(160, 122, 74, 0.28) }` light-mode token; dark-mode override `rgba(201, 165, 107, 0.32)`; `@keyframes templeTrace` — 0% baseline shadow, 20% baseline + `0 0 14px -3px var(--bronze-trace-glow)` glow peak, 100% baseline shadow (warm rise then cool); `.temple-featured.just-touched` and `.temple-domain-card.just-touched` rule (`animation: templeTrace 2.4s cubic-bezier(0.25, 0.1, 0.25, 1) forwards`); `prefers-reduced-motion: reduce` override (`animation: none`). Trigger behavior: (1) Light — trace fires on `closeLightModal()` call only when `_lightWitnessedThisSession` is true, so opening and dismissing without witnessing produces no trace; (2) Sleep — trace fires on every successful `saveSleepModal()` call (guard `saveSleepClosure()` returns false prevents spurious fire if save fails); (3) Mind — trace fires on `mind-complete` click after a valid session end; (4) Care — trace is queued after `keepNightProtocol()` changes at least one check (`!changed` early-return prevents spurious queue). Chronicle autosave does not trigger a trace. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync path change, no IndexedDB change, no domain logic change, no modal open/close behavior change, no Sleep lock change, no Light witness array change, no Mind session shape change, no Care checks/loggedDays logic change, no new HTML, no new files, no new dependencies. |
| EA-89 | Astrolabe Commit Gesture. `js/render/cycle.js`: removed `showToast` import (was the only usage — `showToast(CYCLE_NAMES[i])` on tap); added module-scope `let _armTimer = null` and `let _armedIndex = null`; added private `commitCycle(i)` — clears arm state, sets `state.cycleDay = i` and `state.lastCycleDate = todayStr`, calls `saveState()` / `renderCycleList()` / `renderTodayCycle()` / `renderAllLists()`, no toast; added private `armNode(i, nodeEl)` — clears any existing timer, removes `.is-armed` from all `.al-node` elements and `.is-preview` from `.al-center`, adds `.is-armed` to `nodeEl`, updates `#al-numeral` and `#al-lbl` DOM text to preview value (no state write), adds `.is-preview` to `.al-center` (restarts CSS animation), sets `_armedIndex = i`, starts 1500ms `_armTimer` → `commitCycle(i)`; added exported `cancelCycleArm()` — clears timer, resets arm index, removes `.is-armed`/`.is-preview` classes, restores center disc text to `state.cycleDay`; refactored `renderCycleList()` click handler to: no-op if `isActive`; `commitCycle(i)` if `i === _armedIndex` (second tap commits immediately); otherwise `armNode(i, btn)`; added arm-state clear (`clearTimeout` + class cleanup + disc restore) at the top of `renderCycleList()` before node rebuild. `js/ui/tabs.js`: added `cancelCycleArm` to the existing `cycle.js` import; added `if (currentPane?.id === 'pane-cycle') cancelCycleArm()` immediately after `const currentPane` resolution in `switchTab()` — fires on any tab leave, dissolving arm state and restoring center disc before the pane begins its leave animation; harmless no-op when no arm is in progress. `css/cycle.css`: EA-89 block appended — `.al-node.is-armed` (border `--bronze`, background `--bronze-soft`, color `--bronze`, `transform: none` to override `:hover` scale); `.al-node.is-armed .al-node-name` (color `--bronze`); `@keyframes alBreath` (0%: opacity 0.6 / no shadow → 50%: opacity 0.6 / `0 0 0 1.5px var(--bronze)` ring → 100%: opacity 1 / no shadow); `.al-center.is-preview` (animation: `alBreath 1.5s ease-in-out 1 forwards`); `@media (prefers-reduced-motion: reduce)` override (animation: none, opacity: 0.6 static). No state shape change, no STORAGE_KEY change, no Supabase schema change, no IndexedDB change, no sync path change, no cycle advancement logic change (`advanceCycleIfNeeded` untouched), no Care task IDs/text change, no Night protocol change, no milestone threshold change, no new module, no new dependencies. |
| EA-91 | Morning De-Checklist. `index.html`: changed the Today/Care Morning surface from visible task rows to a quiet secondary protocol surface with `#care-morning-action` (`Open the day`), `#care-morning-state`, and `#care-morning-steps` details reveal; moved the Morning add control inside the Steps reveal while preserving `#list-morning`, `#comfort-tag`, and `data-section="morning"`. `js/render/today.js`: added `keepMorningProtocol()` that marks only current visible `getMorningTasks()` IDs true in existing `checks[todayStr]`, then reuses `reconcileDayCompletion()` so `loggedDays` still updates only through the existing Morning + Night completion rule; individual Morning toggles remain available behind Steps and can still correct partial/kept records. Morning task empty/info/edit aria copy now uses "step" language, without changing any task text. `js/render/common.js`: added Morning protocol control rendering — disables the action once kept, writes `Morning kept.`, sets Steps meta (`View morning` / `In motion` / `Kept`), and auto-opens Steps only for partial Morning completion. `main.js`: wired `#care-morning-action` to `keepMorningProtocol()`. `css/today.css`: added scoped EA-91 styles for the quieter Morning action, Spectral state line, and Steps reveal, intentionally less prominent than Night. `sw.js`: bumped `CACHE_NAME` `elysium-v9` → `elysium-v10` for changed shell assets. `CLAUDE.md` + `docs/ticket-log.md`: EA-91 entries added. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync behavior change, no `tasks.morning` shape change, no task IDs/text change, no Comfort mode behavior change, no checks/loggedDays shape change, no Night protocol behavior change, no new state, no scores/percentages/streaks/progress counts added. |
| EA-92 | Supporting Rituals Fold / Retire. `index.html`: reframed the Today/Care supporting `tasks.habit` reveal from "Supporting rituals" / "Quiet layer" to "Additional" / "Kept aside", changed the inner note to "Kept aside from the turn.", and changed the edit modal `habit` option label from "Daily" to "Additional" while preserving `value="habit"`, `data-section="habit"`, and `#list-habit`. `js/render/today.js`: extended the existing step-language branch so `section === 'habit'` uses "No steps. Tap ＋ to add one.", "Step info", and "Edit step"; task rendering, toggling, and save flow remain unchanged. `css/today.css`: reduced the supporting reveal's spacing, softened the top hairline to `--separator`, muted summary/meta/body typography, and kept the plus/minus affordance visible through `--bronze-dim`. `sw.js`: bumped `CACHE_NAME` `elysium-v10` → `elysium-v11` for changed shell assets. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync path change, no `tasks.habit` removal, no task ID/text change, no checks/loggedDays behavior change, no Night or Morning protocol change, no add/edit/delete behavior change, no new state, no scores/counts/streaks/analytics/goals/domains. |
| EA-93 | Stars Grid Recede. `css/progress.css`: appended a scoped EA-93 block that hides the Stars weekday row, keeps the existing 42 `.cal-day` tap targets and 7-column `.cal-grid`, removes visible base cell borders/backgrounds/date numerals, and renders visual marks with pseudo-elements — logged days as small glowing bronze dots, partial days as smaller dim bronze dots, empty past days as barely visible warm pinpricks, future days visually absent, and today as a quiet bronze ring; constellation SVG is assigned behind star dots with `z-index`. `js/render/progress.js`: removed an unused local `logged` variable; preserved date generation, click binding, `openPastDayModal()`, correction/backfill behavior, and future-date guard; changed Day Record display labels only (`partial` → `in motion`, missed `not kept` → `quiet`, `kept`/`quiet` unchanged). `index.html`: renamed the photo section heading from "Weekly photos" to "Skin memory" while preserving `#add-photo-btn`, `#weekly-photo-input`, and `#weekly-photos-grid`. `sw.js`: bumped `CACHE_NAME` `elysium-v11` → `elysium-v12` for changed shell assets. `CLAUDE.md` + `docs/ticket-log.md`: EA-93 entries added. No state shape change, no STORAGE_KEY change, no Supabase schema change, no sync path change, no IndexedDB/photo storage change, no `loggedDays`/`checks` shape change, no future-date behavior change, no Day Record correction behavior change, no photo add/delete behavior change, and no counts/streaks/scores/goals/analytics added. |
| EA-95 | Sleep First-Light Reciprocity. `js/render/temple.js`: extended `getDailyLine()` with a first-light branch positioned between the today-sleep check and the Care turn check — computes `yesterdayStr` via local-date subtraction, then returns `'The night has passed.'` when: yesterday has a sleep entry AND today's sleep entry is absent AND `new Date().getHours() < 11`. The line surfaces on the first Temple render of the morning after a closed night, yields automatically to Care/Chronicle/Mind/Light once any day ritual begins, and fades to silence after 11am. No new imports (uses already-imported `getSleepEntry`). No HTML change, no CSS change, no new state key, no STORAGE_KEY change, no Supabase schema change, no sync path change, no IndexedDB change, no closure lock change, no reopen behavior change, no Sleep duration/score/alarm/streak/notification, no new domain. |
| EA-94 | Settings Workshop Reframe. `index.html` `#pane-settings`: changed the visible page heading from "Settings" to "The workshop", added the Spectral-style stanza "The quiet machinery beneath the ritual.", and replaced seven small grouped-list sections with three larger surfaces: `Threshold` (signed-in row, sign out, Comfort mode), `Cues` (morning/night/evening time inputs plus notification toggle/status), and a native `Archive` disclosure whose summary names "Install, beginning, export, import, reset." The Archive contains install guidance, ritual start date reset, `Export`, `Import`, hidden `#import-file`, and `Reset everything`. All existing JS-bound IDs and core classes are preserved (`#account-email`, `#sign-out-btn`, `#comfort-toggle`, cue time IDs, `#notif-toggle`, `#notif-status`, `#notif-hint`, `#install-text`, `#start-date-label`, `#reset-start`, `#export-btn`, `#import-file`, `#reset-all-btn`). `css/settings.css`: appended a scoped EA-94 block for `.settings-stanza`, `.workshop-block`, `.workshop-block-head`, `.workshop-block-copy`, `.workshop-note`, `.workshop-summary`, and `.workshop-summary-mark`; inner `.setting-card` / `.install-card` surfaces inside Workshop blocks are reset to transparent/no-shadow so the larger Workshop panels carry the visual surface; row separators, time inputs, toggles, chevrons, and active states are softened with warm scoped styling. No JS changes, no state shape change, no STORAGE_KEY change, no Supabase schema change, no sync/storage path change, no auth behavior change, no notification scheduling or permission behavior change, no cue time behavior change, no export/import/reset behavior change, no new settings, no internal ID/class renames, no `Task name`, `Progress`, or touch-target audit changes. |
| EA-96 | Identity Surface Reset. `README.md`: replaced the stale skincare-tracker README with the current Elysium identity: a quiet ritual operating system, with Care described as one domain rather than the whole product; removed old `skincare-app`, `app.js`, and `styles.css` setup/deploy language; documented the current vanilla JS PWA shape, modular `css/` and `js/` tree, local-first storage, Supabase sync, IndexedDB photos, and service-worker offline shell. `manifest.json`: description changed to `A quiet ritual operating system for a rhythm of living.`, `background_color`/`theme_color` changed to `#0E0E12`, and categories changed to `["lifestyle", "utilities"]` while preserving name, short name, start URL, scope, display, orientation, and icons. `sw.js`: top comment changed from Skin to Elysium and `CACHE_NAME` bumped `elysium-v12` → `elysium-v13` for manifest/shell freshness. `CLAUDE.md`: Section 1 now leads with current ritual-OS identity while noting skincare-tracker origin and protected Care/storage compatibility; EA-96 summary row added. `package-lock.json`: package name changed `skincare-app` → `elysium-app` with no scripts/dependencies added. `test-checklist.html`: title and H1 changed from Skincare App to Elysium. No `STORAGE_KEY` change, no IndexedDB name change, no Supabase config/schema change, no sync behavior change, no state shape change, no task IDs/text change, no app file renames, no runtime implementation logic change, and historical ticket entries were not rewritten. |
| EA-97 | Dead Code / Architecture Cleanup. `js/constants.js`: removed unused `CYCLE_DESC` after `rg`/static export scan confirmed no references. `js/domains/care.js`: removed unused `today`/`ymd` imports plus dead `getMorningTasksForDate()` and `getAllTodayTasks()` exports; active Care accessors (`getMorningTasks`, `getNightTasks`, `getTodayChecks`, `getDaysSinceStart`, `getCycleDayForDate`) remain unchanged. `js/domains/sleep.js`: removed dead `getLastSleepEntry()` export; current Sleep paths use `getSleepEntry(todayStr)` and closure/reopen helpers. `js/render/cycle.js`: removed unused `CYCLE_NAMES` import; Astrolabe node labels remain local `NODE_NAMES`. `css/components.css`: removed orphaned global hero/ring/old section-icon/old section-header CSS and stale user-select references to removed cycle/section classes. `css/cycle.css`: removed the unused pre-Astrolabe cycle-card CSS. `css/temple.css`: removed unused EA-10/EA-14 Temple hero/quick-action CSS targeting classes no longer present after the domain-card Temple rebuild. `css/auth.css`: removed old EA-25 rollback selectors for `.auth-card`, `.auth-glyph`, `.auth-title`, `.auth-switch`, `.btn-link`, and unused auth-view button overrides while keeping active auth layout/shared text styles. `css/chronicle.css`: removed unused `.chronicle-drift-empty`. `css/settings.css`: removed unused `.install-card` rules after EA-94 archive markup no longer uses that class. `css/modal.css`: cleaned obsolete internal comments only. `CLAUDE.md` + `docs/ticket-log.md`: EA-97 entries added. No visible copy changes, no HTML changes, no state shape change, no `STORAGE_KEY` change, no Supabase schema or sync path change, no IndexedDB change, no service worker change, no Care task IDs/text change, no `checks`/`loggedDays` rename, no Sleep/Light/Mind/Chronicle/Care/Cycle behavior change, and Body/Water remain dormant. |
| EA-98 | Milestone Reframe. `js/constants.js`: retained dormant milestone metadata while removing the old upgrade task metadata. `js/state.js`: removed the active progression and task-injection runtime while keeping passive season derivation plus all migration validation for the legacy `milestoneStage` field. `main.js`, `js/sync.js`, and `js/render/settings.js`: boot, cloud-sync, import, and start-date reset paths no longer trigger automatic milestone mutation; reset still sets `state.milestoneStage = 0` for compatibility. `js/render/progress.js`: the Stars season line now resolves from local season titles with a quiet `ritual` fallback. `sw.js`: cache bumped `elysium-v13` to `elysium-v14`. Existing saved tasks, including any previously injected steps, remain ordinary task data. No task migration, no bulk deletion, no `STORAGE_KEY` change, no Supabase schema change, no sync merge behavior change, no `loggedDays`/`checks` change, no IndexedDB change, no Body/Water activation, no ritual identity change, no visual styling change, and no new features. |
| EA-99 | Cycle / Sleep / Mind Philosophical Fixes. `index.html`: Cycle `.cycle-sub` changed to `Three nights.`, the `.al-legend` block was removed, Sleep's full `.sleep-tonight` reminder block was removed, `#mind-held-elapsed` was removed, and `#mind-end` now renders hidden with text `End session` instead of disabled with countdown copy. `js/render/cycle.js`: arm commit timeout changed from 1500ms to 2500ms. `css/cycle.css`: removed unused `.al-legend`, `.al-legend-item`, `.al-dot`, `.al-dot-active`, and `.al-dot-pending` rules; `alBreath` animation duration changed from 1.5s to 2.5s. `js/render/temple.js`: removed Mind held-duration imports and changed the Temple Mind card state from `Held · Xm` to `Held`. `js/ui/modals.js`: removed the Sleep reminder time lookups/setters, removed the `isValidReminderTime` and `formatHeldMs` imports from this module, removed Mind elapsed-label updates, and changed the end button visibility logic to `if (endBtn) endBtn.hidden = elapsedMs < MIND_MIN_HELD_MS;`. `css/modal.css`: removed Sleep Tonight rules and `.mind-holding-dur`. No new state, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no Body/Water activation, no Body/Water UI changes, no Care task IDs/text changes, no Night/Morning protocol changes, no Sleep closure/reopen behavior change, no Mind session shape change, no Light/Chronicle/Stars behavior change, and no new features. |
| EA-100 | Mechanical Cleanup. `js/constants.js`: removed the entire `MILESTONES` export (6 lines — 4-entry array with clinical skincare week descriptions and day thresholds); confirmed zero references across all 18 modules and `main.js` after EA-98 removed its only consumer in `state.js`. `css/components.css`: removed the dead `background: var(--green); border-color: var(--green)` block for `.task-item.done .task-check` that was silently overridden by the EA-23 `--bronze` rule below it in cascade; merged the redundant `.task-check svg { color: white }` line into the EA-23 canonical block (which already declared `color: var(--bg)`) — net result is one `.task-check svg` rule with correct contrast token and no dead duplicate. `js/ui/modals.js`: fixed `saveSleepModal()` — removed the `hh`/`mm` time construction and `formatTime12()` call that wrote a bedtime string to `#temple-sleep-state`; `renderTemple()` unconditionally overwrites that element with `'Closed'` on every Temple render, making the time write a dead inconsistency; changed the direct DOM write to `'Closed'` to match; removed now-unused `formatTime12` from the `utils.js` import line. `index.html`: comfort mode sub-copy changed from `"Warm months — face wash and sunscreen only"` (skincare product language) to `"Reduces morning Care to essentials"` (Care-neutral framing). `sw.js`: `CACHE_NAME` bumped `elysium-v14` → `elysium-v15` — required because this ticket touches `js/ui/modals.js`, `css/components.css`, and `index.html`, all of which are in the ASSETS list. No state shape change, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no Care task IDs/text change, no 3-night cycle logic change, no milestone threshold change, no new features, no behavior changes. |
| EA-102 | Sedimentary Phase I — Presence & Residue. Four small continuity gestures across Mind / Sleep / Temple / Chronicle, derived entirely from existing state. Light continuity remains the EA-101 yesterday linger (untouched).<br>**Mind — Temple inscription.** `js/render/temple.js`: extended the existing `getMostRecentSession` import line to also import `getMostRecentReflectionSession` from `'../domains/mind.js'` (helper already shipped in EA-65). `renderTemple()` gains a block at the end that reads `getMostRecentReflectionSession()` and, when present and non-empty, writes the trimmed `reflection` string into `#temple-mind-line` and unhides it; otherwise clears textContent and hides the span. `index.html`: added `<span class="temple-mind-line" id="temple-mind-line" hidden></span>` inside the Mind domain card's `.temple-domain-body`, after `.temple-domain-archetype`. `css/temple.css`: appended an EA-102 block before the EA-85 temporal-breath section — `.temple-mind-line` uses `display: -webkit-box; -webkit-line-clamp: 1; -webkit-box-orient: vertical; overflow: hidden; text-overflow: ellipsis;` for single-line visual truncation (full text remains in state), Spectral italic 12px, `var(--muted-text)` at 0.78 opacity, 2px top margin. State pill (`Held` / `Unheld`) remains beside the sub-line.<br>**Sleep — previous-night whisper.** `index.html`: added `<p class="sleep-recent" id="sleep-recent" hidden></p>` inside `#sleep-state-a` between `.sleep-sub` and `.sleep-note` textarea. `js/ui/modals.js` (`openSleepModal()`, inside the existing `else` branch that prepares State A): added inline `yesterdayStr` computation using the same date arithmetic pattern as `getDailyLine()` and EA-101 (`new Date()`, `setDate(getDate() - 1)`, `${y}-${MM}-${DD}` template); reads `getSleepEntry(yesterdayStr)` and the entry's `note` field; when a non-empty trimmed note exists, writes it into `#sleep-recent` and unhides; otherwise clears and hides. Renders once at modal open; never re-renders during the session. Constitution §17.3 honored: one short faint italic line, no `Yesterday:` framing label, no multiple notes, no history controls. `css/modal.css`: appended an EA-102 `.sleep-recent` rule before the existing `#sleep-modal .sleep-note` rule — same line-clamp pattern, Spectral italic 13px, `var(--muted-text)` at 0.55 opacity, 14px bottom margin.<br>**Temple — quiet-stretch acknowledgment.** `js/render/temple.js` (`getDailyLine()`): refactored the inline yesterday-string computation into a private `ymdFromOffset(daysBack)` helper (used by both the existing first-light branch and the new helpers); added private `hasSignalOn(dateStr)` (boolean — true if `chronicle.notes[d].body`, `sleep.entries[d]`, `light.entries[d].witnesses.length`, or any completed `mind.sessions` entry for that date exists); private `hasAnyHistory()` (boolean — true if any of the four state surfaces holds at least one non-today entry); private `isQuietStretch(lookbackDays = 7)` (true when `hasSignalOn` is false for every day from 1 to `lookbackDays` back). New final branch positioned after the Light witness check and before the empty return — `if (isQuietStretch() && hasAnyHistory()) return 'The room has waited.';`. Trigger conditions: today has no signal (every upstream branch fell through) AND the past 7 days hold no signal AND the user has at least one prior ritual record. The line vanishes the moment any signal today fires. Vocabulary is finite, hand-curated, contains no number, no "X days," no comparative. No counter ever reaches the DOM — the lookback is internal control flow only.<br>**Chronicle — Drift fade by age.** `js/render/chronicle.js`: replaced `quietClass(index)` (position-based) with `quietClass(dateStr)` (age-based, keyed by `daysApart(todayStr, dateStr)` — the already-defined helper hoisted as a function declaration). Tiers: `≤14d` → `''` (vivid), `15–60d` → `' is-quiet'` (0.72 opacity), `61–180d` → `' is-quieter'` (0.52 opacity), `181d+` → `' is-faint'` (0.34 opacity). Updated the single call site inside `renderDrift()` from `quietClass(shown)` to `quietClass(dateStr)`. Existing CSS opacity tiers (`.chronicle-drift-entry.is-quiet/is-quieter/is-faint`) untouched. `DRIFT_MAX = 12` cap untouched. Constitution §8.1 honored: "Recent vivid; old gauzy; ancient ghost."<br>**Service worker.** `sw.js`: `CACHE_NAME` bumped `elysium-v16` → `elysium-v17` because `index.html`, `js/render/temple.js`, `js/render/chronicle.js`, `js/ui/modals.js`, `css/temple.css`, and `css/modal.css` are all in the ASSETS list.<br>**Constitution Six Tests (each gesture).** Felt-not-read: Mind/Sleep text is constitution-authorized (§18.2, §17.3); Temple line is qualitative single sentence; Drift fade is pure opacity (atmosphere). No-counter: no count, ratio, streak, percentage, or comparison surfaces anywhere; the 7-day lookback in Temple never touches the DOM. Reversibility: Mind line is replaced by next reflection (not cumulative); Sleep whisper is yesterday-only; Temple line vanishes on first daily signal; Drift opacity recovers to vivid for today's entries. Silence: every gesture has an empty-data hidden state; no placeholder copy. Non-actionable: no new tap targets; entries remain non-actionable. Productivity-app test: nothing here resembles a measure of performance.<br>**No-touch list.** No new top-level state key. No new tab/screen/modal. No `STORAGE_KEY` change. No Supabase schema or sync path change. No IndexedDB change. No Body/Water activation. No Care state reshape. No `chronicle.notes` / `sleep.entries` / `light.entries` / `mind.sessions` shape change. No migration. No notification change. No AI. No new dependency. EA-101 yesterday-witness linger is untouched. Sleep closure lock, Mind 60s floor, Cycle astrolabe, Stars constellation, and Care Night/Morning protocols are untouched. |
| EA-101 | First Continuity Primitive — Light · Yesterday's Witness Linger. First true continuity gesture in Elysium. Yesterday's witnessed light positions render as faint residue on today's sky inside the Light modal — atmospheric spatial memory, not history. `main.js` (`renderLightModal()`, the witness-mark render block following the existing `skyEl.querySelectorAll('.light-witness-mark').forEach(el => el.remove())` clear): added inline `yesterdayStr` computation using the same date arithmetic pattern as `getDailyLine()` in `js/render/temple.js` (`new Date()`, `setDate(getDate() - 1)`, `${getFullYear()}-${padStart(month)}-${padStart(date)}`); added a first render loop reading `state.light?.entries?.[yesterdayStr]?.witnesses || []` and appending `<div class="light-witness-mark is-linger" style="left: ${pct}%">` for each valid `HH:MM` entry; the existing today loop runs unchanged after, so DOM order paints today's 4px 0.55 marks on top of yesterday's 3px 0.18 linger marks. `css/temple.css`: added one rule beside the existing `#light-modal .light-witness-mark` block — `#light-modal .light-witness-mark.is-linger { width: 3px; height: 3px; opacity: 0.18; }`. All other linger properties (position, bottom offset, border-radius, background `var(--bronze)`, `transform: translateX(-50%)`, `pointer-events: none`) inherit from the base rule. No new helper functions, no new state keys, no migration (existing per-date `light.entries` already carries the shape needed), no `STORAGE_KEY` change, no Supabase schema or sync path change, no IndexedDB change, `sw.js`: `CACHE_NAME` bumped `elysium-v15` → `elysium-v16` (both modified files — `main.js` and `css/temple.css` — are in the ASSETS list, matching the EA-100 precedent and Hard Constraint #7), no Temple atmosphere change, no Sleep/Mind/Care/Chronicle changes, no animation, no transition, no hover state, no ARIA label, no setting/toggle, no eyebrow change, no cursor change, no horizon/sun change. Yesterday is read at render time only — no caching, no setInterval, no clock-driven fade; the linger marks look identical at 4am and 8pm and silently vanish at rollover to the day after. Empty yesterday → blank linger (no fake continuity). Acceptable graceful-fail: a user who never notices the trace loses nothing; a user who notices receives a quiet, accurate atmospheric memory of having been here yesterday. |
| EA-103 | Resonance Infrastructure Phase — five invisible infrastructure changes.<br>**(1) Coordinate imprinting.** `js/utils.js`: added `getPeriodKey(hour?)` — pure, 7-branch, maps hour to the same period-key vocabulary as `TEMPLE_PERIOD_BUCKETS` (`'night'|'first-light'|'morning'|'midday'|'afternoon'|'golden-hour'|'dusk'`). `js/domains/chronicle.js`: added `getPeriodKey` to import; `upsertChronicleNote()` now writes `period` and `cycleDay` into the note entry, preserving existing values on resave (`??` nullish coalescing). `js/domains/sleep.js`: added `getPeriodKey` to import; `saveSleepClosure()` now includes `period: getPeriodKey(now.getHours())` in every closure entry. `js/domains/mind.js`: added `getPeriodKey` to import; `endMindSession()` imprints `period` and `cycleDay` on the session at completion, using `if (x === undefined)` to prevent double-write. `cycleDay` is Chronicle + Mind only (Sleep closure is not meaningfully cycle-aware).<br>**(2) Migration validation.** `js/state.js`: `migrateState()` gains `const _VALID_PERIODS = new Set(...)` (7 string values) defined once at the chronicle block and reused by sleep and mind sections below it. Chronicle notes: per-entry loop strips invalid `period` (not in set) and invalid `cycleDay` (not in [0,1,2]) without touching `body`/`updatedAt`. Sleep entries: `.map()` now conditionally copies valid `period` alongside existing `bedtime`/`note`. Mind sessions: `.map()` refactored from object literal to block-body; conditionally copies valid `period` and `cycleDay`. All validation is strip-on-invalid, no backfill. No DEFAULT_DATA change, no STORAGE_KEY change, no Supabase schema change.<br>**(3) Well label age-graduation.** `js/render/chronicle.js`: deep-time fallback label `'From an earlier turn'` replaced with two age-tiered qualitative labels — `'From an earlier season'` for entries 60–364 days old, `'From the beginning'` for entries ≥365 days old. Both use the existing `daysApart(todayStr, dateStr)` helper. Constitution §15.6 honored.<br>**(4) Anti-deduction safeguards.** `js/render/chronicle.js`: anniversary ±3 window iteration order now rotates by day-of-year (`dayOfYear % 6` seed from `startOfYear` math). `baseOffsets = [1,-1,2,-2,3,-3]`; `rotStart = dayOfYear % baseOffsets.length`; spread-slice rotation applied. Exact anniversary (offset 0) is checked separately before the window and is unaffected by rotation. Result: same determinism within a given calendar day, different entry selected day-to-day when multiple window candidates exist. Removes nearest-first predictability over multi-day windows.<br>**(5) Silence hardening.** `js/render/temple.js`: `renderTemple()` now captures `getDailyLine()` return into `const line`; sets `lineEl.textContent = line` as before; adds `const greetingEl = lineEl.parentElement; if (greetingEl) greetingEl.hidden = !line;` — the `.temple-greeting` wrapper is hidden when there is nothing to say, preventing a `margin-bottom` whitespace gap on silent days. `index.html`: `.temple-greeting` div gains `hidden` attribute for correct pre-JS paint.<br>**(5) Session repetition floor.** `js/render/chronicle.js`: module-level `const _wellExcluded = new Set()` (never persisted, resets on page reload). `getWellEntry(excluded)` accepts a Set and skips entries whose dateStr is in it — applied at all three tiers (exact anniversary, window, deep-time). `renderWell()` passes `_wellExcluded` and calls `_wellExcluded.add(candidate.dateStr)` after a successful render. If all candidates are excluded → silence (Well hidden). Does not conflict with §11.1 determinism: within a day the Set is deterministic; across sessions it resets.<br>**SW cache.** `sw.js`: bumped `elysium-v17` → `elysium-v18` (five modified files in ASSETS: `js/utils.js`, `js/domains/chronicle.js`, `js/domains/sleep.js`, `js/domains/mind.js`, `js/state.js`, `js/render/chronicle.js`, `js/render/temple.js`, `index.html`).<br>**Resonance compatibility.** Coordinate fields are stored silently and available to future cross-domain resurfacing logic (e.g., surfacing a Chronicle note written on a Salicylic night when tonight is also Salicylic). No current surface reads these fields. **Migration notes.** Additive-only: old entries that lack `period`/`cycleDay` continue to work; new entries gain the fields going forward. Cloud sync unchanged — JSONB column transparently carries new optional fields. **Constitution Six Tests (all five gestures).** Felt-not-read: coordinates are invisible metadata; Well labels are qualitative vocabulary; silence fix is purely presentational. No-counter: no count, ratio, or streak introduced. Reversibility: session exclusion resets on reload; coordinate fields do not accumulate unboundedly (one per entry). Silence: all surfaces handle absence by hiding. Non-actionable: no new tappable elements. Productivity-app test: none of the changes are satisfying to a productivity user. |
| EA-104 | Cross-Domain Well v1 — Chronicle Well deep-time branch now pools from Chronicle notes, Sleep closure notes, and Mind reflections into a single source-ambiguous rotation. Anniversary exact and ±3-day window branches stay Chronicle-only. Constitution §13.4 (cross-domain ambiguity), §15.3 (one entry), §17.6 + §18.5 (deep-time thresholds), and §15.6 (finite vocabulary) honored throughout.<br>**`js/domains/sleep.js`.** Added `getSleepNoteEntries()` returning `[{ dateStr, body }]` over entries with non-empty trimmed `note` and a valid `isYmd(dateStr)` guard. No state-shape change.<br>**`js/domains/mind.js`.** Added `getMindReflectionEntries()` returning `[{ dateStr, body }]`. Iterates `state.mind.sessions`, ignores incomplete sessions, requires non-empty trimmed `reflection`, and collapses to the single most-recent reflection per date by max `startedAt`. No state-shape change.<br>**`js/render/chronicle.js`.** Imported the two new helpers. Module-level `const SOURCE_RANK = { chronicle: 0, mind: 1, sleep: 2 }` introduced as the static, content-blind tie-break precedence for same-date entries in the deep-time pool. `getWellEntry(excluded)` rewritten: (a) anniversary exact and window branches return `{ source: 'chronicle', dateStr, body, label }` and check `excluded.has('chronicle:'+candStr)` instead of the bare dateStr; (b) deep-time branch builds a union pool from Chronicle (`Object.entries(notes)` with `isLivingNote`), Sleep (`getSleepNoteEntries()`), and Mind (`getMindReflectionEntries()`), tagging each entry with its source — also filters out `dateStr === todayStr` and `dateStr > todayStr` at pool-build time; (c) computes `newestNonTodayInUnion` from the union of all pool dates before exclusion filtering; (d) eligible entries require `!excluded.has(source+':'+dateStr)` AND `daysApart(todayStr, dateStr) ≥ 60` AND `daysApart(newestNonTodayInUnion, dateStr) ≥ 14`; (e) sort ascending by `dateStr` with `SOURCE_RANK` tie-break (Chronicle wins same date); (f) returns first match with `From the beginning` (≥365d) or `From an earlier season` (60–364d) label, normalized to `{ source, dateStr, body, label }`. `renderWell()` updated: reads `candidate.body` instead of `candidate.note.body`; `_wellExcluded.add(candidate.source+':'+candidate.dateStr)` keys the repetition floor by source-prefixed dateStr; returns the dateStr to `renderChronicle` only when `candidate.source === 'chronicle'` (else `null`) so `renderDrift` continues to receive a Chronicle dateStr to exclude — Sleep/Mind sources do not need a Drift exclusion because Drift only reads `chronicle.notes`.<br>**`sw.js`.** `CACHE_NAME` bumped `elysium-v18` → `elysium-v19` (three modified files in ASSETS: `js/render/chronicle.js`, `js/domains/sleep.js`, `js/domains/mind.js`).<br>**Source-ambiguity guarantees.** No source labels in the rendered surface. No icons or chips. No "from Sleep" / "from Mind" copy. The eyebrow remains exactly one of the existing four phrases (`A year ago today`, `A year ago this week`, `From an earlier season`, `From the beginning`). A Sleep note from two years ago and a Chronicle note from two years ago surface identically. Voice-leak through the user's own writing is acceptable per §13.4 ("ambiguity at a glance").<br>**Backwards compatibility.** Existing users with only Chronicle data see no behavioral change in the anniversary branches. The deep-time branch becomes very slightly more selective only when Sleep notes or Mind reflections older than 60 days exist AND are at least 14 days older than the union's newest non-today entry. No migration needed; the helpers read fields that EA-103 already imprints (Sleep `note`, Mind `reflection`).<br>**Constitution Six Tests.** Felt-not-read: surface unchanged, single Spectral italic line, qualitative label. No-counter: no counts, ratios, streaks, or percentages introduced. Reversibility: session repetition floor resets on reload; deletion of an entry removes it from the pool. Silence: union pool empty → `getWellEntry` returns `null` → `wellEl.hidden = true`. Non-actionable: no new tap targets; Well remains read-only. Productivity-app test: a tracker user gains nothing — no log, no totals, no filter, no source view.<br>**No-touch list.** No new top-level state key. No new tab/screen/modal. No new DOM elements. No new CSS. No `STORAGE_KEY` change. No `DEFAULT_DATA` change. No Supabase schema or sync path change. No IndexedDB change. No Body/Water activation. No Care/Light/Cycle/Stars/Settings changes. No `chronicle.notes` / `sleep.entries` / `mind.sessions` shape change. No migration. No notification change. No AI. No new dependency. EA-103 coordinate imprinting and repetition floor primitive untouched. Drift behavior unchanged. Sleep modal whisper (EA-102) untouched. Light yesterday linger (EA-101) untouched. Anniversary exact and window branches still Chronicle-only by construction. |
| EA-105 | Well Resonance Hardening.<br>**Selection stability.** `js/render/chronicle.js` replaces the EA-103/104 advance-on-rerender repetition floor with a per-session Well cache: `renderWell()` computes a signature from source/date/body/coordinate availability, computes one candidate or silence once for that signature, and reuses it across autosave, blur, and tab rerenders. This prevents the Well from walking eligible memories during a session while still recomputing after sync/import materially changes the pool.<br>**Rarity gates.** Exact Chronicle anniversary remains ungated. Anniversary-window candidates now pass a deterministic `well-window` gate before surfacing. Deep-time candidates are split into `60-364d` seasonal and `365d+` beginning bands; seasonal opens only on a 1-of-7 deterministic phase, beginning only on a 1-of-17 deterministic phase. Silence is therefore an intentional common outcome even when eligible memories exist.<br>**Predictability hardening.** Deep-time selection no longer sorts oldest-first. Eligible candidates are collapsed by date, sorted only for stable ordering, then selected by a deterministic date-phase hash for the open band. Same-date cross-source candidates collapse to one entry using the existing static source precedence, preventing sequential same-day resurfacing.<br>**Cross-domain gates.** `getSleepNoteEntries()` now returns the existing invisible `period` coordinate. `getMindReflectionEntries()` now validates YMD dates and returns existing `period` and `cycleDay` coordinates. Sleep and Mind deep-time candidates require age >= 120d and coordinate resonance against the current period, or for Mind, current period or current Care cycle. Legacy cross-domain entries without coordinates require age >= 180d plus an additional 1-of-7 deterministic legacy gate. Chronicle candidates retain the native 60d deep-time floor.<br>**Cache.** `sw.js` bumped `elysium-v19` -> `elysium-v20`. No UI, HTML, CSS, visible copy, source labels, counters, metadata, archive/history/search, AI, summaries, state shape, `DEFAULT_DATA`, `STORAGE_KEY`, Supabase schema/sync, IndexedDB, Body/Water, Care, Light, Sleep modal, Mind modal, Drift cap, or Chronicle autosave behavior changed. Constitution tests: felt-not-read, no-counter, reversibility, silence, non-actionable, and productivity-app all pass. |
| EA-109 | Body Domain Activation — Atlas's Standing. Body activated as a real ritual domain; the Water card remains dormant. **State.** `js/constants.js` DEFAULT_DATA gains `body: { arrivals: {} }`. Shape: `arrivals['YYYY-MM-DD']` is an array of `{ at: 'HH:MM', period: <periodKey> }` entries, deduped by minute. No `cycleDay` imprint (Body does not enter the Chronicle Well). **Migration.** `js/state.js` `migrateState()`: validates and strips invalid `body.arrivals` entries using the existing `_VALID_PERIODS` Set already defined in the chronicle block. Invalid date keys and invalid per-entry shapes are removed. **Domain module (new).** `js/domains/body.js`: `ensureBodyState()`, `arriveBody(dateStr)` (pushes `{at, period}`, deduped by minute, using `getPeriodKey()` from `utils.js`), `getBodyArrivals(dateStr)`, `hasArrivedToday(dateStr = todayStr)`. **Sync.** `js/sync.js` `syncFromSupabase()`: `mergedBodyArrivals` union-merges per date, deduped by `at` string, mirrors the Light witnesses merge. `state.body` is reassigned after merge. **HTML.** `index.html`: (a) Body domain card: `disabled` attribute removed, `id="temple-goto-body"` added, state span `id="temple-body-state"` added; all other attributes/glyph/copy unchanged. (b) `#body-modal` added before `#confirm-modal`: `modal-backdrop` id `body-modal-backdrop`, `body-modal-sheet` with `modal-handle`, `body-eyebrow`, `body-title` (`<em>Standing</em>`), `body-sub`, `body-horizon-wrap` holding `body-horizon` + `body-marks`, `btn-primary` id `body-arrive-btn` ("Returned"), `btn-text` id `body-modal-close` ("Close"). **CSS.** `css/temple.css` EA-109 block appended: `.body-modal-sheet` override: `background-image: radial-gradient(ellipse at 50% 120%, rgba(160,122,74,0.10), transparent 60%)` + `background-color: var(--bg-grouped)` (dark: 0.08 alpha); eyebrow, title, sub typography; horizon-wrap 16px relative container; 1px horizon hairline; arrival-mark 4px bronze squares x-positioned by minute-of-day; `btn-primary`/`.is-still`/`btn-text` layout matching Light modal pattern. **Temple render.** `js/render/temple.js`: `hasArrivedToday` imported from `domains/body.js`; `TRACE_CARD_IDS` gains `body: 'temple-goto-body'`; `collectTemplePresenceDays()` iterates `state.body.arrivals` to include Body presence days in the EA-107 warmth coefficient; `renderTemple()` writes `'Stood'` or `'—'` to `#temple-body-state`. **Modals.** `js/ui/modals.js`: `arriveBody` and `getBodyArrivals` imported from `domains/body.js`; `_bodyArrivedThisSession` module-level flag; private `renderBodyHorizonMarks()` clears and repaints marks from today's arrivals; `openBodyModal()` resets session flag, rerenders marks, resets CTA, opens modal; `closeBodyModal()` hides modal and calls `applyTempleTrace('body')` if session flag set; `registerBodyModal()` wires backdrop + close + arrive-btn (3-second `.is-still` EA-83 pattern, no toast, rerenders horizon after tap). **Main.** `main.js`: `openBodyModal`/`registerBodyModal` added to modals import; `registerBodyModal()` called after `registerMindModal()`; `#temple-goto-body` click → `openBodyModal`. **Service worker.** `sw.js`: `CACHE_NAME` `elysium-v22` → `elysium-v23`; `'./js/domains/body.js'` added to ASSETS. **Explicit non-changes.** No note/reflection text in Body modal. No yesterday-linger marks (deferred). No `getDailyLine()` Body branch (deferred). No Chronicle Well contribution (Body has no text). No Stars/`loggedDays` contribution. No streak, count, total, average, duration, region, quality, or graph. No HealthKit/wearable integration. No suggestions. Constitution §27.2 Six Tests: felt-not-read (horizon mark is the only feedback — no information read), no-counter (multiple arrivals per day exist in data, none surfaced as number), reversibility (warmth coefficient decays with absence; marks only show today's arrivals), silence (`—` when not arrived; new user sees no scolding), non-actionable (modal is Body's entire UI, no history/browse surface), productivity-app (no workout, no log, no duration, no count — fails a productivity user completely). |
| EA-108 | Phase III Calibration Audit. `js/render/temple.js`: `TEMPLE_WARMTH_MAX_ALPHA_LIFT` raised from `0.0100` to `0.0240` — at realistic steady-state warmth (~0.84 after near-daily use for one year), the original constant produced a peak alpha lift of ~0.008, which is below perceptual threshold even side-by-side and fails to meet Constitution §26.3 ("perceptible to a returning user if they were to compare side-by-side"). At `0.024`, steady-state lift is ~0.020, producing a ~14–17% change in the featured-card gradient alpha — barely side-by-side perceptible after ~12 months, definitively sub-articulable day-to-day. `css/temple.css`: added a 3-line comment before the EA-85 period attribute-selector block noting that these CSS values are overridden by the JS inline style at runtime and that period feel must be re-calibrated via `TEMPLE_PERIOD_ALPHA` in temple.js. `sw.js`: `CACHE_NAME` bumped `elysium-v21` → `elysium-v22`. No HTML, no new CSS rules, no state shape, no `DEFAULT_DATA`, no `STORAGE_KEY`, no Supabase schema, no sync path, no IndexedDB, no domain behavior, no copy, no new features. |
| EA-107 | Lifetime Warmth Coefficient Infrastructure.<br>**Runtime derivation.** `js/render/temple.js` now derives a private coefficient at Temple render time from existing date-keyed presence only: unique dates from `loggedDays`, truthy `checks`, Chronicle notes with body text, Light witness entries, Sleep closure entries, Mind session dates, and weekly photo dates. Each date contributes at most once; future dates are ignored. Nothing is stored, synced, migrated, cached in state, or exposed in UI.<br>**Bilateral sedimentation.** Presence-days raise the coefficient by a tiny saturating step toward a hard cap. Gaps between presence-days and absence after the latest presence apply daily exponential softening toward a small historical floor. The floor exists only after prior presence and is itself capped, so long absence returns the room close to install-state without making an inhabited room mathematically identical to a never-used room.<br>**Temple organ.** `renderTemple()` still sets `data-period` from the EA-85 period buckets. The existing period alpha baselines are mirrored in JS and the coefficient contributes only a capped `+0.0100` maximum lift to the existing `--temple-grad-alpha` custom property on `#pane-temple`. Featured-card hue values, CSS selectors, geometry, typography, timing, structure, copy, and all other atmospheric axes remain unchanged. No CSS file changed.<br>**Constitution fit.** Felt-not-read: output is only ambient gradient opacity. No-counter: no count, ratio, streak, percentage, or comparative value reaches DOM. Reversibility: absence decays the effect at human timescale. Silence: no date-keyed presence leaves the baseline untouched. Non-actionable: no tap target, history, setting, or screen was added. Productivity-app: no user-performance surface exists to satisfy.<br>**Cache and no-touch list.** `sw.js` bumped `elysium-v20` -> `elysium-v21` because `js/render/temple.js` is cached. No UI copy, HTML, CSS, state shape, `DEFAULT_DATA`, `STORAGE_KEY`, localStorage key, IndexedDB, Supabase schema, sync path, migrations, settings, debug value, progression variable, unlock, milestone, Light horizon wash, shadow density, separator aging, typography aging, Chronicle atmosphere, or additional organ changed. |
| EA-110 | Water Domain Activation — Hydros's Holding. Water activated as a real ritual domain without becoming a quantity tracker. **State.** `js/constants.js` DEFAULT_DATA gains `water: { holdings: {} }`. Shape: `state.water.holdings['YYYY-MM-DD'] = [{ at: 'HH:MM' }]`. Entries are deduped by minute and store only `{ at }`; no `period`, `cycleDay`, note/body, duration, quality, amount, goal, reminder, or submode field exists. **Migration.** `js/state.js` ensures `state.water` and `state.water.holdings` are plain objects, removes invalid date keys, accepts only arrays of exact `{ at: 'HH:MM' }` objects, dedupes duplicate minutes during sanitization, and deletes empty arrays. **Domain module.** `js/domains/water.js`: `ensureWaterState()`, `holdWater(dateStr)` (validates date, appends current `HH:MM` if absent, calls `saveState()`), `getWaterHoldings(dateStr)`, and `hasHeldToday(dateStr = todayStr)`. **Sync.** `js/sync.js` adds `mergedWaterHoldings`, union-merging cloud + local holdings by date and `at` value, then re-runs `migrateState(state)`; Supabase schema remains unchanged. **HTML.** `index.html`: Water card is active with `id="temple-goto-water"`, verb remains `notice`, archetype changes `Poseidon` → `Hydros`, state span gains `id="temple-water-state"` and starts `Unstirred`; `#water-modal` added with HYDROS eyebrow, `<em>Held.</em>` title, `A pause at the surface.` subcopy, surface line + `#water-marks`, `Held` primary button, and `Close`. **CSS.** `css/temple.css` EA-110 block adds a centered, soft surface atmosphere visually distinct from Body/Light; today marks are 4px bronze below the line, yesterday linger marks are 3px and faint, and `.is-still` follows the existing stillness pattern with no new animation. **Temple render.** `js/render/temple.js` imports `hasHeldToday`, adds `water` to `TRACE_CARD_IDS`, includes holding dates in the EA-107 lifetime warmth presence-day union, and writes `Unstirred` / `Stilled` to `#temple-water-state`. **Modal behavior.** `js/ui/modals.js` renders yesterday's linger marks before today's marks, x-positioned by minute of day; `openWaterModal()` resets session residue; `registerWaterModal()` wires backdrop/close/held; tapping `Held` calls `holdWater(todayStr)`, renders Temple and marks, applies `.is-still` for 3 seconds, and shows no toast; `closeWaterModal()` applies `applyTempleTrace('water')` only if Water was held this modal session. **Main/cache.** `main.js` imports/wires `openWaterModal` and `registerWaterModal`; `sw.js` bumps `elysium-v23` → `elysium-v24` and caches `./js/domains/water.js`. **Explicit non-changes.** Water is not added to `getDailyLine()`, Chronicle Well, Stars/loggedDays, a dashboard, a top-level history surface, notification/cue settings, HealthKit/wearables, analytics, counts, streaks, graphs, goals, reminders, or any quantity UI. |
| EA-111 | Temple Restraint & Architectural Compression. Removed the Temple domain count meta rather than replacing it; unified dormant domain-state vocabulary to `Quiet`, `Unseen`, `Unclosed`, `Quiet`, `Unreturned`, and `Unstirred`; rebuilt the Body modal field with a restrained earth-toned floor/horizon plane; changed Body arrival marks from squares to circles; rendered yesterday's Body arrivals first as faint `.is-linger` circles before today's marks; removed the 21-bar Care rhythm ribbon and its render path while leaving the three-night cycle cards intact. `sw.js` cache bumped `elysium-v24` -> `elysium-v25`. No state shape, `DEFAULT_DATA`, migration, sync, Supabase schema, IndexedDB, storage key, task IDs, Care cycle logic, new surfaces, dashboards, counts, goals, reminders, analytics, or progression systems changed. Constitution checks: felt-not-read, no-counter, reversibility, silence, non-actionable, and productivity-app all pass. |
| EA-112 | Temple Vertical Rhythm Cleanup. `css/temple.css`: tightened only Temple home vertical rhythm after the EA-111 removals: `.temple-featured` bottom margin `24px -> 18px`; `.temple-domain-head` bottom margin `10px -> 8px`; `.temple-domain-list` gap `8px -> 7px`; `.temple-domains` bottom margin `20px -> 14px`; `.temple-secondary-nav` padding `6px 0 12px -> 4px 0 8px`; added `#pane-temple .footer-spacer { height: 32px; }` so the Temple tail remains comfortable without over-separating from the bottom nav. `sw.js` cache bumped `elysium-v25` -> `elysium-v26` for CSS freshness. No HTML, copy, state, logic, modals, Care behavior, Body/Water behavior, Supabase/storage/sync, service-worker behavior beyond cache version, new sections, separators, labels, counts, dividers, taxonomy, dashboards, goals, or tracking/productivity elements changed. |
| EA-115 | Care Surface Modernization. `index.html`: removed the "State" KV pair (`<div class="care-kv">` containing `<span class="care-k">State</span>` and `<span class="care-v" id="hero-status">`) from the care meta-row — this label was tracker-adjacent and redundant with the night section's own state text; removed the `<div class="care-cycle-head">Cycle of three nights</div>` heading from the cycle strip — self-evident from the three roman numeral cards. `js/render/common.js`: removed the `const status = document.getElementById('hero-status')` and `if (status) status.textContent = turnState.copy` lines from `renderTodayCycle()` since the element no longer exists in HTML; changed morning state fallback copy from `'Use after the morning has been kept.'` → `'When the morning has been kept.'` (removes instructional register). `css/today.css`: removed the now-dead `#pane-today .care-cycle-head` CSS rule (8 lines). `sw.js`: `CACHE_NAME` bumped `elysium-v27` → `elysium-v28`. No state shape change, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no task IDs/text change, no 3-night cycle logic change, no Care Night/Morning protocol change, no cycle card structure change, no care-turn-memory change, no new features, no behavioral changes. |
| EA-113 | Temple Secondary Card Visual Consistency Pass. `css/temple.css`: reduced only the secondary `.temple-domain-card` elevation by replacing the shared `--shadow-card` with a quieter local `--temple-domain-shadow`; kept card surfaces, borders, radius, and the Care featured card intact; raised `.temple-domain-list` gap by 1px (`7px -> 8px`) so the quieter cards remain legible as separate paths without adding separators; stabilized `.temple-domain-state` as a fixed right-aligned `86px` column with `white-space: nowrap`; moved state labels from bronze to muted text at reduced opacity; added explicit line-height to `.temple-domain-sub` and `.temple-domain-archetype`; tightened `.temple-mind-line` with smaller type, lower opacity, shorter line-height, and reduced top margin; updated `templeTrace` to preserve each card's base shadow while keeping the existing transient bronze trace. `sw.js` cache bumped `elysium-v26` -> `elysium-v27` for CSS freshness. No HTML, JS behavior, copy, state, storage, sync, Supabase schema, IndexedDB, modals, Care featured card, Body/Water behavior, new UI structure, badges, counts, dashboards, separators, or active-state classes changed. |
| EA-117 | Mind Inscription Calibration. `css/temple.css`: two property changes to `.temple-mind-line` — `font-size` raised `11.5px → 12px`; `opacity` raised `0.62 → 0.75`. At 11.5px and 0.62 opacity of `var(--muted-text)` the inscription compounded to approximately 38% effective alpha, below the perceptual floor for the gesture to register as present. At 12px and 0.75 opacity the line remains clearly subordinate to domain name, sub-line, and archetype while being perceptibly felt as inscription. `sw.js`: `CACHE_NAME` bumped `elysium-v29 → elysium-v30`. No HTML change, no JS change, no state shape change, no new element, no copy change, no domain behavior change. |
| EA-116 | Featured Care Card Atmospheric Refinement. `css/temple.css`: three targeted property changes — (1) `#pane-temple .temple-featured-medallion` outer glow removed in both light and dark mode; light rule `box-shadow` reduced from `0 0 18px -2px rgba(160,122,74,0.25), inset 0 0 0 1px rgba(160,122,74,0.30)` to `inset 0 0 0 1px rgba(160,122,74,0.25)`; dark `@media` override reduced from `0 0 22px -4px rgba(201,165,107,0.30), inset 0 0 0 1px rgba(201,165,107,0.30)` to `inset 0 0 0 1px rgba(201,165,107,0.28)` — medallion is now a bordered circle rather than a luminescent badge; (2) dark `@media` `#pane-temple .temple-featured` shadow simplified from `var(--shadow-card), 0 0 28px -12px rgba(201,165,107,0.18)` to `var(--shadow-card)` — removes the decorative bronze halo that had no atmospheric justification after EA-111–115 quieted the surrounding stack; (3) `#pane-temple .temple-featured-status` `font-size` reduced `18px → 16px` — status label remains distinctly larger than secondary card names (15px) while no longer reading as a module headline. `sw.js`: `CACHE_NAME` bumped `elysium-v28 → elysium-v29` for CSS freshness. Radius decision: `--radius-2xl` confirmed as `20px` in `css/base.css`, matching the prototype spec's `--r-lg: 20px` for the featured card — no radius change needed. No HTML change, no JS change, no state shape change, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no domain logic change, no copy change, no gradient fill change, no border color change, no cycle indicator change, no warmth coefficient infrastructure change, no secondary card change, no modal change. |
| EA-121 | Water Utility Expansion. `js/constants.js`: added `WATER_RITUALS` — a hardcoded array of 28 ritual observation strings oriented toward environmental contact, surface sensation, and temperature noticing (not hydration tracking). `js/domains/water.js`: imported `WATER_RITUALS`; added private `getDayOfYear(date)` helper; added exported `getWaterRitual()` — deterministic hourly selection via `(hour + dayOfYear * 24) % WATER_RITUALS.length`, no state stored, no history tracked; identical formula to `getBodyRitual()`. `index.html`: added `<p class="water-ritual" id="water-ritual"></p>` to Water modal between `.water-surface-wrap` and `#water-hold-btn`. `js/ui/modals.js`: imported `getWaterRitual`; updated `openWaterModal()` to populate `#water-ritual` at open time. `css/temple.css`: added `.water-ritual` rule — `font-family: var(--font-display); font-style: italic; font-size: 14px; line-height: 1.55; color: var(--muted-text); margin: 0 20px 20px`. `sw.js`: `CACHE_NAME` bumped `elysium-v32 → elysium-v33`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no Supabase schema change, no IndexedDB change, no `holdings` storage behavior change, no Temple card change, no warmth coefficient change, no continuity residue added, no ritual completion state, no ritual tracking. |
| EA-120 | Body Return Rituals. `js/constants.js`: added `BODY_RITUALS` — a hardcoded array of 28 short ritual instructions (Spectral italic copy) across four physical-return categories (breath, position, eyes, specific tension) interspersed for hourly variety. `js/domains/body.js`: imported `BODY_RITUALS`; added private `getDayOfYear(date)` helper; added exported `getBodyRitual()` — deterministic selection via `(hour + dayOfYear * 24) % BODY_RITUALS.length`, changes each hour, no state stored, no history tracked. `index.html`: added `<p class="body-ritual" id="body-ritual"></p>` to Body modal between `.body-sub` and `.body-horizon-wrap`. `js/ui/modals.js`: imported `getBodyRitual`; updated `openBodyModal()` to call `getBodyRitual()` and populate `#body-ritual` at open time. `css/temple.css`: added `.body-ritual` rule — `font-family: var(--font-display); font-style: italic; font-size: 14px; line-height: 1.55; color: var(--muted-text); margin: 0 20px 20px`; reduced `.body-sub` bottom margin from 24px to 12px to accommodate new field. `sw.js`: `CACHE_NAME` bumped `elysium-v31 → elysium-v32`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no Supabase schema change, no IndexedDB change, no arrival/storage behavior change, no Temple card change, no continuity residue added, no ritual completion state, no ritual tracking. |
| EA-122 | Sleep Closure Rituals. `js/constants.js`: added `SLEEP_RITUALS` — a hardcoded array of 28 permission-giving/release strings, tonally distinct from BODY_RITUALS (somatic) and WATER_RITUALS (environmental); oriented toward divestiture — setting the day down rather than noticing the body or environment. `js/domains/sleep.js`: imported `SLEEP_RITUALS`; added private `getDayOfYear(date)` helper; added exported `getSleepRitual()` — deterministic hourly selection via `(hour + dayOfYear * 24) % SLEEP_RITUALS.length`, no state stored, no history tracked; identical formula to `getBodyRitual()` / `getWaterRitual()`. `index.html`: added `<p class="sleep-ritual" id="sleep-ritual"></p>` inside `#sleep-state-a` between `.sleep-sub` and `#sleep-recent`. `js/ui/modals.js`: imported `getSleepRitual`; in `openSleepModal()` State A branch, populated `#sleep-ritual` at open time. `css/modal.css`: added `.sleep-ritual` rule — `font-family: var(--font-display); font-style: italic; font-size: 13px; line-height: 1.6; color: var(--muted-text); opacity: 0.72; margin: 0 0 20px` — deliberately quieter than Body/Water inscriptions (13px vs 14px, 0.72 opacity) to match Sleep's more interior register; tightened `.sleep-sub` bottom margin `20px → 16px` to accommodate new element. `sw.js`: `CACHE_NAME` bumped `elysium-v33 → elysium-v34`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no Supabase schema change, no IndexedDB change, no closure/note/bedtime behavior change, no Temple card change, no warmth coefficient change, no continuity residue added, no ritual tracking. |
| EA-123 | Mind Gathering Rituals. `js/constants.js`: added `MIND_RITUALS` — a hardcoded array of 28 attention-gathering strings, tonally distinct from BODY_RITUALS (somatic), WATER_RITUALS (environmental), and SLEEP_RITUALS (permission/release); oriented toward gathering — drawing scattered attention toward one thread before the hold begins. `js/domains/mind.js`: imported `MIND_RITUALS`; added private `getDayOfYear(date)` helper; added exported `getMindRitual()` — deterministic hourly selection via `(hour + dayOfYear * 24) % MIND_RITUALS.length`, no state stored, no history tracked; identical formula to all other domain ritual getters. `index.html`: added `<p class="mind-ritual" id="mind-ritual"></p>` inside `#mind-state-a` between `.mind-sub` and `.mind-dur-row` — positioned so the gathering invitation precedes the duration selector, making ceremony primary and mechanism secondary. `js/ui/modals.js`: imported `getMindRitual`; in `openMindModal()`, populated `#mind-ritual` at open time (before the State A/B branch — field is ready regardless of which state is shown). `css/modal.css`: added `.mind-ritual` rule — `font-family: var(--font-display); font-style: italic; font-size: 13px; line-height: 1.6; color: var(--muted-text); opacity: 0.70; margin: 0 0 20px` — 13px/0.70 opacity, the most interior of all four domain inscriptions, matching Sleep's quiet register; tightened `.mind-sub` bottom margin `28px → 16px` to accommodate new element. `sw.js`: `CACHE_NAME` bumped `elysium-v34 → elysium-v35`. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no Supabase schema change, no IndexedDB change, no session/reflection/hold behavior change, no Temple card change, no warmth coefficient change, no continuity residue added, no session tracking, no State B or State C changes. |
| EA-125 | Chronicle Ritualization Foundation. `js/constants.js`: added `CHRONICLE_PROMPTS` — 28 hand-curated reflective fragments ("What today asked of you.", "What remained unresolved.", etc.), each a sentence-fragment pointing inward without prescribing a response; oriented toward witnessing rather than reporting. `js/render/chronicle.js`: imported `CHRONICLE_PROMPTS` from `constants.js`; added private `getChroniclePromptEyebrow()` — maps `getPeriodKey()` to a time-of-day label ('TONIGHT', 'THIS MORNING', 'TODAY', 'THIS AFTERNOON', 'THIS EVENING'); added private `getChroniclePrompt()` — deterministic day-of-year rotation (`dayOfYear % 28`), consistent all day, changes at midnight; added private `renderPromptCard()` — populates `#chronicle-prompt-eyebrow` and `#chronicle-prompt-text` at render time; added private `renderContinuityFragment()` — four qualitative cases (today written → silent; first ever day → "This is where it begins."; yesterday written → "The night before is still here."; gap ≥ 7 days → "The chronicle receives what comes."; otherwise → silent), no numbers, no counts, room-voice throughout; both functions called from `renderChronicle()`. `index.html`: added `.chronicle-prompt#chronicle-prompt` (`.chronicle-prompt-eyebrow` + `.chronicle-prompt-text`) inside `.chronicle-today` above `.chronicle-card`; added `<p class="chronicle-continuity" id="chronicle-continuity" hidden></p>` below `#chronicle-status`. `css/chronicle.css`: added EA-125 block — `.chronicle-prompt` (dashed bronze border light `rgba(160,122,74,0.32)` / dark `rgba(201,165,107,0.24)`, warm fill `rgba(bronze,0.04)`, 12px radius, 13–14px padding); `.chronicle-prompt-eyebrow` (JetBrains Mono 9px/0.22em/0.78 opacity, bronze); `.chronicle-prompt-text` (Spectral italic 15px/1.5 lh/0.82 opacity); `.chronicle-continuity` (JetBrains Mono 10px/0.10em/0.62 opacity, muted); typography refinements — textarea `line-height: 1.65`, placeholder `opacity: 0.5`. Constitutionally omitted: Mood State Row — explicitly forbidden by §10.1 ("Mood ratings or affective scores, numeric or pictorial") and §20.12; the three example continuity phrases from the ticket brief ("Three nights written consecutively", "Last entry: two days ago", "You return here more during difficult weeks") were each replaced with constitutionally sound room-voice equivalents. No new state keys, no `DEFAULT_DATA` change, no `migrateState` change, no Supabase schema change, no sync path change, no IndexedDB change. Note: `js/render/chronicle.js` reaches ~432 lines with this addition — modestly over the ~400 soft ceiling; architecture approval was implicit in the ticket approval. `sw.js`: `CACHE_NAME` bumped `elysium-v36 → elysium-v37`. |
| EA-126 | Chronicle Memory Resurfacing. `js/domains/chronicle.js`: added `getResurfacedChronicleNote(referenceDateStr = todayStr)` plus local helpers for date offsetting, living-note validation, finite-label selection, and excerpt clipping. The helper derives from existing `state.chronicle.notes` only: exact 7-day, 14-day, and 30-day anchors are checked in that order, then the nearest older valid note is selected; today, future dates, invalid dates, and empty bodies are ignored. It returns only `{ dateStr, label, excerpt }` with a short clipped excerpt and qualitative label vocabulary (`Seven days ago`, `From a previous night`, `Earlier this month`, `From before`). `index.html`: added one hidden `.chronicle-resurfaced#chronicle-resurfaced` card after the Chronicle heading, with static `Resurfaced` kicker, dynamic `#chronicle-resurfaced-when`, and `#chronicle-resurfaced-body`. `js/render/chronicle.js`: imported the helper; added `renderResurfaced()` that writes with `textContent`, hides silently when null, and returns the surfaced date; `renderDrift()` now accepts an exclusion list so Drift omits the resurfaced date and the existing Well date. Chronicle autosave, blur save, prompt card, status line, continuity fragment, Well, and persistence paths are otherwise unchanged. `css/chronicle.css`: added a restrained Obsidian Temple card treatment with a thin bronze left accent, warm sediment fill, JetBrains Mono metadata, and Spectral italic excerpt text. `sw.js`: `CACHE_NAME` bumped `elysium-v37 → elysium-v38`. No new state key, no `DEFAULT_DATA` change, no `migrateState` change, no memory cache, no Supabase schema or sync-path change, no IndexedDB change, no search/filter/archive controls, no stats/charts/counters, no AI summary, no mood analysis or recommendation, and no actionable entry link. Smoke tests: `node --check js/domains/chronicle.js`, `node --check js/render/chronicle.js`, `node --check main.js`, and a Node module smoke test for 7-day preference, 14-day fallback, nearest older fallback, excerpt clipping, invalid-date ignoring, and empty-state silence. |
| EA-127 | Sleep Closure Ritual. `js/constants.js`: added `SLEEP_CLOSURE_INVITATIONS` with five finite closure lines: "Set down one unfinished thing.", "Let the room become quieter.", "Name what can wait.", "Lower the light.", and "Leave the rest outside the night." `js/domains/sleep.js`: added `getSleepClosureInvitations()` returning a copied invitation set; `getSleepEntry()` now accepts both legacy valid-`bedtime` entries and new `closed: true` entries; `saveSleepClosure()` now writes `{ closed: true, period }` plus optional note only if explicitly passed by compatibility callers, so new modal closures preserve a simple mark rather than a bedtime. `js/state.js`: migration now validates both legacy bedtime entries and new closed-mark entries, preserving old `bedtime`, `note`, and valid `period` fields without requiring new entries to have a time. `index.html`: Sleep State A changed from note-first capture to passive closure invitation surface, removed textarea and anti-check copy, and State B copy now says the day has ended and the room is quiet. `js/ui/modals.js`: renders invitation rows with `textContent`, removes visible time metadata, and closes with an empty note payload. `css/modal.css`: replaced `.sleep-ritual`/`.sleep-note` styling with passive invitation rows using Spectral italic copy, JetBrains Mono indices, warm separators, bronze restraint, and the existing ritual surface. `js/render/temple.js`: Sleep empty state changed from `Unclosed` to `Open`. `sw.js`: `CACHE_NAME` bumped `elysium-v38 → elysium-v39`. No Care tracking logic, task IDs, cycle logic, Supabase schema, IndexedDB, scores, streaks, goals, analytics, bedtime display, or invitation persistence added. Smoke tests: `node --check` for `js/constants.js`, `js/domains/sleep.js`, `js/ui/modals.js`, `js/state.js`, `js/render/temple.js`, and `main.js`; module smoke for old bedtime entry migration, new closed-mark migration, invalid Sleep entry removal, and new closure save shape. |
| EA-124 | Meridian Mark — Logo & Favicon. `index.html`: replaced Aperture mark (horizontal line at ~golden-ratio from top) with Meridian mark (vertical gnomon line, pip at top) in both the boot-shell SVG and the auth-screen SVG; added `class="auth-mark-ring"`, `class="auth-mark-gnomon"`, `class="auth-mark-pip"` to the auth SVG inner elements for animation targeting; prepended `<link rel="icon" type="image/svg+xml" href="favicon.svg" />` before the PNG fallback. `favicon.svg`: new standalone SVG — Meridian mark, bronze `#C9A56B` in dark, `#A07A4A` in light via `@media (prefers-color-scheme: light)` inside `<style>`. `css/auth.css`: added Meridian reveal animation block — ring traces (0.55s, dashoffset 251.33), gnomon rises (0.35s delayed 0.30s, dashoffset 81.6), pip blooms (0.25s scale/opacity, delayed 0.55s), all cubic-bezier(0.2, 0.7, 0.2, 1) matching the Cycle easing; wrapped in `@media (prefers-reduced-motion: no-preference)` so reduced-motion users see elements statically. `sw.js`: added `'./favicon.svg'` to `ASSETS`; `CACHE_NAME` bumped `elysium-v35 → elysium-v36`. Meridian geometry: circle r=40 (42% of 96px viewBox), vertical line x=48 y1=7.2 y2=88.8, pip cx=48 cy=8 r=2.1. No state shape change, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no domain logic change, no task IDs change, no modal behavior change. |
| EA-118 | Stars Day Record Quieting. `js/render/progress.js`: removed `<p class="past-day-state-line">` from `renderPastDayBody()` template literal — the constellation dot already communicates logged/not logged status; the label was a redundant tracker-adjacent status field; also removed `<p class="past-day-chronicle-label">Chronicle</p>` from the chronicle excerpt block — labeling the excerpt's source origin turns it into a categorized data field, violating §13.4 (ambiguity of origin is a feature); removed now-dead `document.getElementById('star-field-meta')` and `document.getElementById('milestone-stage-meta')` null-guarded DOM writes (both IDs removed from HTML; writes were always no-ops since EA-69/52 cleared these to empty). `index.html`: removed `id="star-field-meta"` from the Constellation section-meta span and `id="milestone-stage-meta"` from the Season section-meta span — both IDs existed only to receive empty-string writes. `css/modal.css`: removed `.past-day-chronicle-label` from the shared monospace grouped selector; removed the standalone `.past-day-chronicle-label { font-size: 9px; ... }` rule; removed `.past-day-state-line`, `.past-day-state-line em`, `.past-day-record.state-partial .past-day-state-line em`, `.past-day-record.state-missed/quiet .past-day-state-line em` rules (4 rule sets, all dead after JS change). `sw.js`: `CACHE_NAME` bumped `elysium-v30 → elysium-v31`. No state shape change, no `STORAGE_KEY` change, no Supabase schema change, no sync path change, no IndexedDB change, no Care task IDs/text change, no cycle logic change, no milestone threshold change, no constellation algorithm change, no photo logic change, no correction affordance change, no new analytics, no new features. |
| EA-177 | Body Desk Relief Lookup. `docs/continuity-constitution.md`: added §19D · The Body Relief Exception (after §19C.6, before Article V) — permits a same-session area choice (neck/shoulders/back/wrists/eyes), one fixed curated reset instruction per area, and existing `arriveBody()` presence mark on completion; forbids pain scales, severity ratings, symptom history, body-maps over time, frequency counts by area, medical/diagnostic language, HealthKit/passive sensing, recommendations based on past usage, and any claim the app knows what is wrong with the user's body. `js/constants.js`: added `BODY_RELIEF_AREAS` — a keyed object with five short, safe, desk-friendly reset instructions (neck: lengthen + slow turns; shoulders: lift-and-drop + slow roll; back: sit tall + small rib rotation; wrists: open + palms up/down; eyes: look away + soft gaze at distance); removed `BODY_RITUALS` (28-string array added in EA-120, now orphaned because the new modal does not use it). `js/domains/body.js`: removed `getBodyRitual()` (deterministic hourly selection from `BODY_RITUALS` — stranded when the old ambient-phrase body modal was replaced); removed `BODY_RITUALS` from import; `getBodySomaticInvitations()` and `BODY_SOMATIC_INVITATIONS` retained — still used by Work Flow (`js/ui/flow.js`). `index.html`: Body modal sheet rebuilt — eyebrow `ATLAS`, title `Relief` (Spectral italic), sub `Choose an area.`, five `.body-area-chip` buttons with `data-area` attributes, `.body-relief-text` paragraph (empty until chip tap), existing `.body-horizon-wrap` with marks, hidden `Returned` btn, `Close` btn; all old somatic list, step sequence, and `body-ritual` inscription removed. `js/ui/modals.js`: imported `BODY_RELIEF_AREAS`; added `_bodySelectedArea` session variable; `openBodyModal()` clears chip selection and hides `Returned` button on each open; `registerBodyModal()` wires chip delegation on `body-area-chips` — selects chip, sets `_bodySelectedArea`, writes `BODY_RELIEF_AREAS[area]` to `body-relief-text`, shows `Returned` button; `body-arrive-btn` click calls `arriveBody(todayStr)`, sets `_bodyArrivedThisSession = true`, saves state, re-renders temple, refreshes horizon marks, writes `'The body has returned.'` to relief text, hides button; closing without Returned saves nothing. `css/temple.css`: replaced all old somatic-list/step-sequence/body-ritual styles with chip styles — `.body-area-chips` (flex-wrap, 10px gap, 20px margin); `.body-area-chip` (DM Sans 14px, muted-text, thin rounded border, transitions border/color); `.body-area-chip.is-selected` (bronze border + bronze text); `.body-relief-text` (Spectral italic 17px, 1.6 lh, muted-text, 0 20px 28px margin, min-height 54px). `sw.js`: `CACHE_NAME` bumped `elysium-v85 → elysium-v86`. No changes to `state.body.arrivals` shape, `arriveBody()`, `DEFAULT_DATA`, `migrateState`, `STORAGE_KEY`, Supabase schema, IndexedDB, Care task IDs, cycle logic, Today Plan, Mind, Water, Sleep, Chronicle, Light, or Temple rendering. `BODY_SOMATIC_INVITATIONS` + `getBodySomaticInvitations()` untouched — Work Flow retains somatic sequence for its midday body step. Constitution §19D compliance: area selection is same-session only, no stored area, no area history, no counts, no pattern analysis, no pain framing, no diagnostics. |
| EA-174 | Care Protocol Reaction Memory. `js/domains/care.js`: added `getPreviousCareRecordForCycle(dateStr, cycleDay)` — reads existing data only (no new state, no derivation, no storage); validates `dateStr`/`cycleDay`, then scans `state.care.records` for entries whose date is `< dateStr`, present in `state.loggedDays`, share the same cycle day via existing `getCycleDayForDate()`, and carry at least one user-authored field (`condition`/`morningNote`/`reactionNote`); returns the most recent match `{ dateStr, cycleDay, record }` or `null`. `js/render/common.js`: imported the helper; added `CARE_CONDITION_LABELS`, `formatCareMemoryDate()`, and `renderCareProtocolMemory(cycleDay)` (called from `renderTodayCycle()` before `renderCareTurnMemory`); builds up to three lines (Skin this morning / Morning note / Reaction note) using `textContent` + `createTextNode` only (no innerHTML for user data), shows a restrained `From <Mon D>` date, hides the surface when no useful prior record exists. `index.html`: added `.care-protocol-memory#care-protocol-memory` (hidden) inside the night section between the reaction note and `.care-turn-memory`, with eyebrow `LAST TIME ON THIS NIGHT`, a date span, and a body container. `css/today.css`: added quiet surface styling mirroring `.care-turn-memory` — warm top border, mono bronze eyebrow, mono muted date, Spectral italic 13.5px muted lines, normal-weight muted line labels; no red/badge/alert/card-heavy styling. `sw.js`: `CACHE_NAME` `elysium-v81` → `elysium-v82`. No changes to state shape, `DEFAULT_DATA`, `migrateState`, cycle logic, `getCycleDayForDate`, `loggedDays`, task IDs, `state.care.records` shape, Today Plan, Chronicle, Sleep, Temple, or Progress calendar. Constitution: Care-only practical resurfacing of one prior user-authored record; no counts/trends/averages/percentages/recommendations/interpretation — compliant under §19A and §22.5A. |

| EA-190 | Care as Asclepius: Irritation Guard. `js/render/common.js`: added `ASCLEPIUS_GUARD_LINES` — a cycleDay-keyed object with three guard phrases: cycleDay 0 (Niacinamide) `"Skin was marked irritated this morning. Keep the layer small; stop if it stings."`, cycleDay 1 (Salicylic) `"Skin was marked irritated this morning. Use only if the skin feels calm tonight."`, cycleDay 2 (Rest) `"Skin was marked irritated this morning. Let the rest night stay simple."`; added `renderCareAsclepiusGuard(cycleDay)` — reads `getCareRecord(todayStr).condition`, hides guard when condition is not `'irritated'`, otherwise writes the keyed phrase to `#care-asclepius-line` and shows the container; called from `renderTodayCycle()` after `renderCareProtocolMemory(cycleDay)`. `index.html`: added `<div class="care-asclepius-guard" id="care-asclepius-guard" hidden>` containing `<p class="care-asclepius-eyebrow">ASCLEPIUS</p>` and `<p class="care-asclepius-line" id="care-asclepius-line"></p>` between `#care-protocol-note` and `#care-night-keeper` in the Care night section. `css/today.css`: added EA-190 guard block — `.care-asclepius-guard` (margin 0 0 14px, padding 12px 0 0, border-top warm separator); `[hidden]` rule (display:none); `.care-asclepius-eyebrow` (mono 8.5px, 0.12em tracking, bronze, margin 0 0 6px); `.care-asclepius-line` (Spectral italic 13.5px/1.55, muted-text, margin 0). `sw.js`: `CACHE_NAME` bumped `elysium-v96 → elysium-v97`. No changes to `state` shape, `DEFAULT_DATA`, `migrateState`, `STORAGE_KEY`, Supabase schema, IndexedDB, cycle logic, task IDs, Today Plan, Chronicle, Sleep, Temple, Progress, or any other domain. Guard reads `getCareRecord` — already imported; no new imports. Constitution: Care-only, user-authored condition only, same-day practical protocol context — compliant under §19A. No counts, trends, history, scoring, pattern analysis, diagnosis, medical claims, or recommendation engine. Patron Test: Utility Gate passed (guard is useful without the eyebrow word — it is a same-day protocol note keyed to the user's own skin report); Form Gate passed (Asclepius shapes the surface by making Care's healing role explicit at the moment of protocol decision, a job only the skincare core could justify). |

| EA-189 | Today as Kairos. `js/render/today-plan.js`: `renderMasthead()` updated — eyebrow text changed from `"Saturday · 31 May"` pattern to `"KAIROS · Saturday · 31 May"` by prepending `"KAIROS · "` to the existing date string; the existing `today-plan-date` element ID and all other IDs/handlers/behavior untouched. `index.html`: masthead subline changed from `"The day has room for what you place here."` to `"Only what belongs to this hour."` (Kairos framing — what fits this hour); footer changed from `"What remains can be kept or left."` to `"What does not belong can pass."` (present-moment release language). `sw.js`: `CACHE_NAME` bumped `elysium-v95 → elysium-v96`. No changes to state shape, `DEFAULT_DATA`, `migrateState`, intention CRUD, carryover logic, Sleep handoff, open-item routing, Care, Chronicle, Temple, Progress, Settings, or any CSS. Patron Test: Utility Gate passed (intentions/carryover/open-items/handoff unchanged — user would miss it); Form Gate passed (Kairos shapes the naming and the hour-framing, not just the label). No decoration drift (no Greek exposition, no parchment/columns, no mythological prose). No generic-utility drift (not called "Tasks", "Goals", or "Agenda"). Constitution compliant — no counts, streaks, percentages, summaries, or actionable nudges. |
