# Elysium — Agent Handoff

`CLAUDE.md` is the authoritative context document for every Claude session working on this codebase.
Read it before inspecting any source file or making any change.

It contains: app overview, architecture (modular `js/` + `css/` tree), state shape, critical functions,
hard constraints, design tokens and tone guidance, the full EA ticket log, and the required ticket workflow.
